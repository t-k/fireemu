# Execution status: compatibility program

Auth claim-source, MFA and Limits native updates remain local-only; executor `571d5a6bb63289893661d88470d47e8ffb4870a2` is source-only evidence. The Auth05 held attempt remains unresolved.

## Fresh request-byte production attempt (`FS-LIMIT-API-REQUEST-BYTES`, fresh02)

The fresh02 production attempt was an authentic, source-bound but incomplete observation from source `2a1e95a9835094ad640b4f012c8dddf9557e5647` and retained artifact SHA-256 `2bf7267b841dc879d7c2eb98ea7a93b4b27279494ab86318282ad797fedbeabe`. The exact boundary inputs `10,485,759`, `10,485,760` and `10,485,761` bytes returned HTTP 200/200/200 in production; the native comparator returned 200/200/400, so the over-boundary result is `over:unexpected-success` and the condition is not accepted. The collection recorded 105 observations, 153 recovery operations and four preflight management calls: 262 charged calls, 51 typed absence proofs, and three postflight management slots `NOT_RUN`. At publication, the receipt was `collection-incomplete`, `mayHaveCreated=true`, `postflightComplete=false`, `releaseEligible=false`, and its recorded reservation state was `held`; cleanup safety evidence existed but contractual completion and release were absent.

Before the terminal transition, the independently reviewed zero-network escalation proposal was pending execution. The later commander transition completed after its freshness/expiry checks; the current canonical state is recorded below. The current cumulative stable-task allocation is 606 micro-USD across the prior 303 micro-USD attempt and this 303 micro-USD allocation; this is a conservative allocation, not billing. Review evidence is `docs.local/reviews/requestbytes-production-2a1-fresh02.md`; receipt, reservation, Gate, routes and collection hashes remain private evidence.

Subsequent commander execution completed the reviewed zero-network terminal transition: the canonical row is now `closed-after-escalation`, with record digest `eea2e95ba02f013ef1273550d6721a2d55baf43ead2d1ef67eb144cba9bb8f58`, zero network calls and zero credential reads. The 606 micro-USD spent allocation is unchanged. This terminal transition does not release the historical receipt or convert its `held`/`releaseEligible=false`/`postflightComplete=false` facts, and it does not accept the compatibility condition; the `over:unexpected-success`, 51 typed-absence proofs and three `NOT_RUN` postflight slots remain recorded.

The bounded condition `fs.aggregations.saved-20260907.v1` is independently approved at source `0b4fa6ae`: 23 MATCH, 0 MISMATCH and 0 INDETERMINATE under the historical index configuration. This supersedes neither the original 17 MATCH / 6 MISMATCH initial-index-config run nor its historical record, and does not classify that result as a runtime defect. The artifact SHA-256 is `8245b80ea941344e114fe8f61cd7721d2519739509779e7504c295c1bbb66849`; the exact 430-entry runtime map digest is `bdeeb69292c6fab6c0a587588f920192ce3ee249c77605af680780d1b74d616b`. The durable replay confirmed cleanup and process closure, but no launch receipt was emitted by the legacy adapter. This is bounded saved-production, REST, strict-profile normalized evidence only, with no fresh production, Rules/user-token, SDK/gRPC, broad query/index or parent-promotion claim; `COMPAT_VERIFIED` remains `0 / 14`.

Plan: [execution-plan.md](execution-plan.md). Scope: [emulator-scope.md](emulator-scope.md). This page records the latest verified checkpoint, what was executed, what was pushed and what remains. Counts are not compatibility percentages.

## Validated continuation checkpoint (2026-09-22)

### Current bounded verification correction (as of 2026-09-22)

`AUTH-CORE-SAVED-REFERENCE-REPLAY-08` is accepted as a saved-production replay from candidate source `7a916b75d`/`5d3796835`, integrated at `732d`: the independently reviewed private run-03 matched 4 corpora × 12 cases (48 MATCH), with executable, launch-copy and artifact SHA `6a159493ce1393c22b822adec05a54518985e68416f37a5a8d943f4c324bc077` binding, the 429-input build binding, and cleanup, exits and listeners verified. The prior run-02 provenance failure remains immutable. This is not fresh production, a parent promotion, SDK evidence or the Auth-19 run.

The known First46 current-artifact runner remains INCOMPLETE because empty Commit acknowledgement handling is under repair; this is not a native argv-409 mismatch and carries no condition acceptance.

Current bounded checkpoint as of integration source `53063bbf817b8923fb5dea5426c8e257dfc48837`: acceptance is condition-focused and does not require whole-G1 completion; `COMPAT_VERIFIED` remains `0 / 14` because the parents remain incomplete and the finite condition total is not yet aggregated. For stable family `AUTH-CREDENTIAL-TOKENS-01`, canonical Ledger lifecycle is explicit: attempt 01 has one terminal `aborted-no-data` row with no active reservation and released locks; attempt 02 remains held with its source, plan and nonce binding intact, so cleanup, close and retry remain blocked pending forensic responsibility proof. Attempt 01 made four management HTTP requests. Attempt 02 made ten management HTTP requests, with one charged pre-send refusal at signup dispatch 11; its 19 not-run cases remain pending forensic proof. The same-family allocated markers are cumulatively US$0.10, not actual billing, and the packet marker is US$0.05; no third attempt occurred. Auth worker `919` is integrated at `53063` with 58 main checks passed, source-only. G0 argv `409` is integrated through `3b` with 301 passed and 2 skips, local-only. The condition `FS-LIMIT-FIELD-TRANSFORMS-PER-DOCUMENT` is accepted for the REST/native 500/501 split-write path from source-derived actual-production summary `031c74bfe`: historical 13 MATCH and 4 semantic mismatches remain immutable, while the repaired saved 17-row comparison matches; retained artifact `5878b9fac` and pilot `81f917` provide the joined source `429` map. This is not a byte-exact or all-campaign claim. Saved-condition acceptance remains ongoing; no production closure is claimed. Historical executions remain immutable. The historical Rust checkpoint `40de0aebe73452760a5ed9cdc36b5fd5655ca502` remains 3,191 passed with 81 configured skips and 0 failures, and the historical Python checkpoint `0537398e5dfc0a471b8b00d378709b2a971349dd` remains 7,593 passed, 4 failures and 40 skips. Its portable private-data-off run separately records 9 explicit skips, not successful private-evidence checks. Known failures, skips and not-run work remain separately classified; no current full-CI green claim is made, and the per-task budget policy, scope and denominator remain unchanged.

### Current verification boundary (as of 2026-09-21T19:48:25Z)

At the historical verification checkpoint recorded here, the reviewed integration source was `5fb29d0e881430dddd9da7869a4631e7f0c6804e`. The fixed Rust verification at `28e90c0b3` passed 3,165 tests with 81 configured skips and workspace Clippy passed; no Rust-bound source inputs changed between that fixed verification and `5fb29d0e`. This is reusable Rust-bound evidence, not a claim that the current Python inventory is complete. At that checkpoint, normal CI run `35643173410` completed successfully, while compatibility-inventory run `35643173963` remained in progress; its feature-integrity job succeeded but its offline-acquisition job had pending checks. The older inventory run `35638650070` for `68016ee3c` completed with failure in its offline-acquisition check and is historical, not evidence for `5fb29d0e`.

No successful current-source G0 refreeze is accepted: the earlier attempted run is recorded above with 12 observations and eight cleanup refusals. The current bounded condition `fs.g0.saved-68012694.v1` is separately accepted from native run `docs.local/runs/g0-native-20260922-ddab` and comparison `docs.local/runs/g0-compare-20260922-ddab`: all 12 normalized rows match (12 MATCH, 0 MISMATCH, 0 INDETERMINATE) on artifact SHA-256 `8245b80ea941344e114fe8f61cd7721d2519739509779e7504c295c1bbb66849`. Its runtime source is `e896132a`, current source is `ddab9856`, and the 430-entry runtime map is exactly equal with canonical digest `bdeeb69292c6fab6c0a587588f920192ce3ee249c77605af680780d1b74d616b`. The pinned original saved-production reference hash is `47672f4e3162b4a0ddfb7baaab622007602aeed6c1fa3d6e5e84034bcbb87772`.

The native session's top-level cleanup count is zero because G0 delegates its work to the Python collector; acceptance relies on the typed child cleanup evidence instead. The `partial` child has eight observation rows and nine cleanup rows; `transaction-field` has four observation rows and three cleanup rows. Every conditional delete returned 200 and each final absence read returned 404; the Gate records 24 total slots (12 observation, 12 recovery), both children complete, no inflight work, all four nonce-owned resources absent, and no creation proofs. The process exited 0 with stopped state and a closed listener. Independent review approves only this saved-production, local-only, strict-profile normalized status/resource projection; it is not a fresh production observation, byte-exact parity, SDK/gRPC, Rules/user-token, concurrency or parent promotion, and does not aggregate the condition into parent closure.

The current-source shared inputs also remain pending for the native MFA refreeze. The four stale Rules provenance failures, the limits lost-apply Must Fix, the parent producer digest mismatch and the unaccepted settlement/issuer work remain blockers for their respective lanes. These classifications do not alter immutable historical records.

G1/G2 and all 14 parent promotions remain open. This checkpoint integrates the independently reviewed transport framing/deadline repair, complete request-byte transport provenance with an immutable historical-generation allowlist, independently anchored MFA runtime comparison, and Auth shared-resource ownership. Historical receipts were not rewritten. Rules transport and issuance-proof preparation is integrated, but its production launcher remains unavailable. Action's canonical transport and six recovery operations are integrated only behind an explicit loopback-only constructor; production hosting is unavailable. Listen has a reviewed preparation-only admission boundary and remains unregistered for production.

- The earlier attempted current-source G0 refreeze remains unaccepted; the accepted condition above is a separate native run integrated through merge `8b6c8935`.

- At `afb7d04a7`, the workspace Rust gate passed 3,165 tests with 81 configured skips and no failures; workspace Clippy passed. The later OIDC test changes the all-crates runtime-input digest, as described below. This is not a claim that skipped obligations or the complete Python inventory passed at the current head.
- SDK publication now uses an actual matching-source build at `e0d53f912`: 18 Node cases, 18 browser long-polling cases and 18 browser streaming cases completed locally. Independent validators passed 29 Python and 324 Node tests; integration binding checks passed 29 tests.
- The fresh request-byte artifact at source `7d001e9a2` completed 241 requests across 258 logical rows, with 17 skipped deletes after the over-boundary refusal. Under/exact inputs were accepted; over-boundary input was a typed refusal. Cleanup, resource absence and source/configuration bindings were verified. The published record is local-only; its fresh packet still requires independent O7/O8 acceptance and current-source CI before production launch.
- The MFA shadow at `5866bc5e4` completed 33 rows and 178 requests with no disagreements, verified cleanup and restored configuration. Artifact, configuration, source and run identity were independently checked. A full lane run found 574 passes and 17 new process-fixture failures caused by the new provenance subprocess interacting with an old global `Popen` patch. A test-only repair retained all lifecycle controls; independent process/binding checks passed 33 tests. The corrected full lane at `968e92cd5` subsequently passed 591 tests with no failures or skips and two existing pytest-option warnings.
- Limits-03 was actually rerun and refrozen. Its executed source `5076c77df` was preserved in the integration ancestry rather than replacing the recorded hash after an erroneous worker amend. Package validation passed 19 tests. Rows 47–50 remain pending in that historical-index run; the subsequently integrated nx-local profile and its pending native run are distinguished below.

Subsequent reviewed integrations include the Auth Credential shadow at `d06ebf9f1` (19 cases, three owned accounts removed, 563 lane tests and 72 integration checks), a mutation-validated OIDC client-configuration continuation regression (16 integration tests), and a typed management-only Ledger finalizer (110 integration checks). The nx-local limits execution at `8f129b10a` and its reconstructed publication records are independently verified and integrated through `2e9cfec8b`: 89 observations, 87 recovery rows, no pending rows or semantic differences, and all 29 owned resources absent. Rows 47–50 are no longer pending in this new local run; the historical-index record remains historical. The source correction aligns retained configuration validation with the producer's canonical digest. Package/shadow integration checks passed 79 tests. This is a genuine new execution, not a profile-label change or production observation.

The OIDC test changes the all-crates runtime-input digest even though runtime implementation is unchanged. Genuine request-byte and transaction-expiry reruns at `1157aebca` therefore replaced their current records: 241 request-byte requests with verified cleanup, and 50 transaction rows with five owned resources deleted and absent. Both used the same retained source-bound executable; 66 integration evidence checks passed with the fresh transaction artifact explicitly enabled. These are local observations, not production comparisons.

There have been no new production calls or reservations in this continuation. The previous request-byte allocation remains 303 micro-USD under the same stable task. The packet at fixed source `f0c6f832b` passed conditional independent O7/O8 technical review and its full lane passed 614 tests, but it is not approved for launch. A subsequent operational review found that the historical uncertain-stop recovery procedure excludes additional readbacks from Ledger allocation and does not bind delegated attestation authority to permission. A bounded recovery-allocation and authority successor must be implemented and reviewed before a new launch generation. The packet's additional 303 micro-USD claim remains only a preview; its seven management revalidation calls remain planned, not observed.

Saved Commit replay was independently accepted and integrated through `87dcecbbe`: the immutable production reference retains 13 matching rows and four historical mismatches, while the actual repaired local run matches all 17 rows. The historical compiler, local artifact/configuration/source, and exact sealed file set were independently verified; deletion, extra-file, symlink and mutation controls refuse. Independent and integration suites each passed 54 tests. This is a repaired local comparison with saved production evidence, not a new production observation or parent promotion. The CI results in earlier named checkpoints below remain historical, not results for this checkpoint.

The reviewed Rules wire matrix was pushed through `9d420fc19`: seven fixture issuance exchanges, 33 observations and three principal transitions (43 loopback requests), with 18 independent and 18 integration tests passing. The genuine native shadow at that source was independently verified and integrated through `78c8fec86`: 33 rows, two Rules releases, account/document cleanup and tenant deletion, with all owned processes stopped. Independent and integration Rules suites each passed 710 tests, closing the prior stale-source evidence failure. Action and FD-safe evidence-read integration at `38b4491bb` passed 176 tests with two explicit opt-in native Action-shadow skips. These skips are not successful native observations. The recovery-allocation backend remains unintegrated after independent review found incorrect parent/child accounting and insufficient durable authority/settlement binding; none of its proposed recovery allocations has been made in the canonical Ledger.

The broad Python run excluding the separately verified MFA lane, fixed at `afb7d04a7`, finished with 6,740 passes, 17 skips, six passing subtests and one publication-path-hygiene failure. The failure identified a hardcoded personal path in a test; a later rerun found the same category in four test files. The portable repair is independently reviewed and integrated through `79cc12f36`, without relaxing the hygiene scanner. Private evidence now requires explicit inputs, invalid explicit paths fail, and loopback tests run without an agent-specific port tool. Author verification passed 142 tests with real private inputs and no skips; integration passed 36 focused tests with the historical and saved-reference positives explicitly enabled. The default private-data-off run has nine explicit skips, not nine successful private-evidence checks. These results do not establish a current-head full Python pass. The changed Rules test sources were followed by a genuine shadow at `79cc12f36`, independently verified and integrated through `1d71927a3`; both independent and integration Rules suites passed 710 tests. Its 33 rows, two Rules releases, cleanup and process/origin closure remain local-only evidence.

The recovery-plan compiler is integrated through `383220d44` with 13 passing integration tests. It independently derives the canonical parent resource set and the 17 inspection, 17 conditional-delete and 51 absence slots. Its tariff estimate is 45 micro-USD, while the existing Gate's per-request rounding requires an 85-micro-USD conservative reservation; these amounts are not interchangeable. This is preparation-only: the real Gate correctly refuses deletion without a trusted parent-attempt/live-read ownership proof, and no recovery allocation, executor or production authority is supplied by this compiler. An upfront combined plan was rejected because its exact 1,378.5-second projection exceeds the unchanged 1,200-second Gate/Ledger cap.

The bounded index-lifecycle client is independently reviewed and integrated through `5af1b6600`, with 15 independent and 15 integration tests passing. It refuses unsupported baselines, binds operation routes, accounts for a lost apply response, and bounds observation and restoration with separate absolute deadlines. Its production entrypoint still refuses: the limits-03 descriptor, management slots and Ledger/O8 execution have not yet been connected. This is local client evidence, not an observed production index change or restoration.

A new MFA-only regression run at `28e90c0b3` failed with 550 passes, 36 failures and five setup errors. A fresh-process reproduction identifies the newer shared Auth-resource validation rejecting MFA's older route-only recovery projection. The earlier 591-test success remains valid only for its named source; it is not a current-source pass. Repair must preserve canonical Auth ownership checks rather than bypass the shared validator. The parallel non-MFA full Python run at fixed `28e90c0b3` is still in progress. Neither shard establishes a full current-source pass. The proposed request-byte recovery facade was independently rejected for caller-supplied parent claims and unbound ownership callbacks, and has not been integrated or used for production.

## Continuation checkpoint (2026-09-22, pushed through `8654df660`)

G1 and G2 remain incomplete, with `COMPAT_VERIFIED 0 / 14`. The following are independently reviewed, integrated and pushed local results, not new production observations:

- Shared O8 partition tests now preserve non-creating read settlement and negative controls (237 integration tests passed). Query Gate response validation was repaired after finding that non-creating classification skipped malformed-response validation; the full partition-cursor lane passed 446 tests and its current local shadow was regenerated.
- MFA's actual local campaign completed 33 rows and 178 requests with no disagreements, verified cleanup and restored configuration. Its record was pushed at `4f11e691a`. Final-runtime identity binding is a separate follow-up; the earlier local shadow is not proof of a final production comparison.
- Limits' old Shared Gate assertions were corrected with ambiguous-create, typed-absence and semantic-mismatch controls. The two-file suite passed 136 tests; independent and integration checks passed five selected controls. The limits-03 shadow and package refreeze remain pending shared-source stabilization.
- External LOCAL-CONTEXT-FD-014 was integrated with descriptor-based, bounded context reads and symlink/FIFO refusal. F99 log-parser corrections preserve unresolved ambiguous node IDs. The combined local-assist integration suite passed 360 tests with one skip and 38 passing subtests.

The additional F99 transport repair is locally integrated at `b51e42a4c`, with 52 passing integration tests; affected current-record regeneration is pending before its next push. Request-byte transport provenance and Auth resource hosting remain under independent security review, with Must Fix findings blocking their integration and production use. No fresh production request, reservation or approval has been made in this continuation.

Actual local SDK runs completed 18 Node cases, 18 browser long-polling cases and 18 browser streaming cases. Their retained-binary provenance is explicit; publication under the existing equal-source contract requires a matching-source rebuild and rerun, which is pending. AUTH-ACTION's local shadow completed and cleaned up, but the Admin unknown-email request differs from its local plan expectation. A saved end-user response does not establish the Admin result, which remains unobserved. Current-artifact Commit replay is still `INDETERMINATE`: the historical and current compilers emit different request bodies, so a source-bound historical-plan replay is required without altering historical evidence.

At pushed source `66610ce82`, normal CI run `35627452622` succeeded only for its selected `pr` job; other normal workflow jobs were skipped by conditions. Inventory run `35627452639` was still in the offline-acquisition step, with subsequent steps not run. Neither this result nor the older workspace result below establishes a final current-source workspace or inventory pass.

## Resumption checkpoint (2026-09-22, source `f99a11c0a`)

The integration checkout and fetched `origin/feat/compatibility-inventory` both resolve to `f99a11c0aab9c48964100a217cf83e32c86ba78e`; the checkout was clean at intake. Historical sections below describe their named sources and are not current execution claims. External deliverables 006–013 are already integrated and must not be reapplied.

The absolute-deadline transport repair is integrated through `8d6157a8c..f99a11c0a`. The next request-byte attempt remains blocked on binding the actual transport modules into packet provenance, a fresh fixed-source packet and nonce, and independent O7/O8 review. The previous attempt reached only the first management slot and provides no request-byte boundary observation. The canonical Ledger was checked at resumption: its six rows are terminal, including request-byte row `7875eabf28a5` in `aborted-no-data`; no active reservation was found.

GitHub Actions `ci` run `35621222318` succeeded for this source. Inventory run `35621222281` was still running at the resumption check; its feature-inventory job succeeded and its offline-acquisition job had not completed. The recorded workspace result at `ec16b0110` was 3164/3165, with the Functions snapshot cancellation test passing only in a separate run. That is not a full workspace pass at this source. The stale MFA and limits-03 records and old Shared Gate test expectations are assigned for reproduction, repair and independent review; their classification as non-runtime failures is not assumed without checking.

G1 and G2 remain incomplete, with `COMPAT_VERIFIED 0 / 14`. Current work prioritizes transport provenance, MFA shadow regeneration, limits-03 shadow/refreeze, Shared Gate contract tests and saved-production replay connections. Scope v3 remains authoritative; this continuation adds neither mobile SDK obligations nor managed backup/PITR work.

## Checkpoint 1 (2026-09-21, pushed `475175e88`)

- Base of this execution: `2dd6d9d9d`. G0 baseline at that commit: `cargo nextest run --workspace --profile pr` 3062 run, 3054 passed, 8 failed, 81 skipped; workspace clippy `-D warnings` failed on six pre-existing lints in `fireemu-core-auth`; `pytest tools/compat-broad` 6048 passed, 2 failed (the known `test_local_supervisor.py` volta-shim pair), 20 skipped; compat-check, traceability-check, config-schema-check and the Quint evidence contract passed. The 8 failures shared one cause (runner discovery under the session target layout) and the clippy failure was lint-only; both were fixed in this checkpoint.
- Merged and independently reviewed: TP-AUTH-D-02 lifetime matrix (11 tests), FS-WRITE-002 duplicate-document wording, FS-WRITE-006 parity test, FS-TXN-002 transaction regressions, AUTH-CLIPPY-TIDY-001, RUNNER-DISCOVERY-001.
- Gate at `b30d0ff36`: local regression gate 3080 run, 3080 passed, 0 failed, 81 skipped (profile `pr`, clean tree); workspace clippy clean; compat-check ok; traceability ok with 3 pending artifacts; the Rust-bound shadow records (`fs-transaction-expiry-retry-04-local-shadow.json`, `fs-request-bytes-local-shadow.json`) regenerated on the same tree.
- Remote CI for `475175e88`: `ci` and `compatibility-inventory` in progress at the time of writing.

## After checkpoint 1

- Merged, reviewed, not yet pushed: TP-AUTH-E-01 tenant isolation matrix and TP-AUTH-E-01-FIX (`44e02c183`). The fix removes an application-visible mismatch: a tenant user's refresh token in the pinned Web SDK request shape (securetoken `v1/token` with an API key and no `tenantId`) was refused with `INVALID_REFRESH_TOKEN`; it now renews the tenant session, while every cross-tenant refusal is unchanged. Local evidence only; production tenant observation remains a campaign.
- Ledger corrections (FS-LEDGER-001): the four write-path limits are `implemented`, not `unsupported`; the cursor tickets are repaired or withdrawn; the Rules comparator statement names the v2 acquisition comparator.

## History rewrite (2026-09-21)

One commit of checkpoint 1 (`ec1707dbe`, runner discovery fix) carried an attribution trailer the owner does not permit. It was replaced by `a3d20ed18` (identical tree, author and parent) and every later commit was re-created with `git rebase --rebase-merges`; the resulting tree at the rewritten head is byte-identical to the pre-rewrite head `02265eda4`, all commits are signed, and the branch was force-updated with the owner's explicit instruction. Old commit ids therefore no longer exist on the branch: `b30d0ff36` is now `4e83c6d2b`, `e2d0a18e8` is `4c5c71c6f`, `475175e88` is `77a06ef68`, `44e02c183` is `d292dd161`, `0cd79d3f4` is `3273ec98f`, `02265eda4` is `d622c413a`. The two Rust-bound shadow records were regenerated on the rewritten history because their evidence tests resolve `runtime.sourceCommit` through git; the Listen shadow records cite lane commits that were not rewritten. A pre-rewrite mirror is retained privately. Gate reports recorded above for `b30d0ff36` apply to the identical tree at `4e83c6d2b`.

## Checkpoint 6 (2026-09-21/22, pushed through `3b3f2cbd8`)

- Merged since `a2d2db49c` (each lane independently reviewed or externally delivered with a verification lane): Rules comparator pre-redaction principal bindings (collector v4, comparator v5); Storage admin Basic capability reuses the browser-metadata rule; runner-independent txn admission test; partition-cursor journal evidence gating with `raw.complete`, abnormal-exit receipts; pilot Commit field-transform case (evidence kind `documented-production-outcome-reference`), per-case evidence kinds, bounded lossless HTTP handling; lifecycle comparator contract v3; Firestore index planner (vector modes are not equality prefixes; index JSON escaped); core JSON parser (`\u` escapes must be hex digits; linear string scan); ID-token verifier requires `iat` and rejects future `iat`/`auth_time`; management transport failure taxonomy and bounded socket timeout; local-assist log parser (five owner reviews); shared Gate/Ledger backlog items 7, 6, 8, 1/12 (request-bytes-shaped no-data kinds), 14 (`attach_evidence`); MFA campaign API-key project preflight, `providerConfigs` normalisation, atomic recovery-file writes; limits-03 restore-record receipt binding.
- Production: one request-byte launch from the packet at `a2d2db49c` (O7 waiver of the CI condition recorded): no-data stop at the first management slot (`oauth-tokeninfo` answered 200 with an incomplete body), nothing created, row retired `aborted-no-data`, 303 micro-USD charged to `FS-LIMIT-API-REQUEST-BYTES`. Cause: the transport discarded the failure reason and its socket timeout could not fire before the coordinator's kill; fixed on the branch (008), with the absolute-deadline and partial-body diagnostics (owner review of `a54c5abc8`) in progress. The next packet is a new nonce and a new BIND after that lands.
- Gate: lane suites green on the head at each merge; commit-bound shadow records regenerated three times (6, 6b, 6c) after the Rust changes; the workspace gate at `ec16b0110` was still running when this section was written and is reported in the work log.
- Known red tests on this head, all bound-record or pinned-old-behaviour follow-ups, none a runtime regression: MFA shadow v2 stale (regenerate with the lane's local campaign); limits-03 frozen manifest stale (shadow run + refreeze) and two `test_limits_03_o8` cases that pin the pre-item-7/8 "cannot close" outcome; the same class may exist in fs-query-partition-cursor, fs-rules-publication and o8-core (being enumerated).
- Not attempted (shared lane): `receiptKind` required on new plans (six lane descriptors rely on the default), clock injection for the two time-dependent admission tests, auth-mfa and fs-config-lifecycle no-data receipt kinds.
- Parent state unchanged: `COMPAT_VERIFIED 0 / 14`. The pilot's Commit case adds a local regression check only.

## Production observation frame

- Authorization (owner, 2026-09-21, corrected the same day): US$10 per production observation task. A task is identified by its stable `observationTaskId` (the campaign id, for example `FS-LIMIT-API-REQUEST-BYTES`); preparation, failed attempts, retries, post-repair re-checks and recovery of that task all count against its US$10. Independent tasks each carry their own US$10 (A at US$8 and B at US$8 are both admitted; A at US$8 followed by a retry of A at US$3 is refused). The earlier reading of one cumulative US$10 for the whole program was wrong and is withdrawn; the program-wide total is still reported below for transparency but is not a stop condition.
- Enforcement: the shared Ledger's `reserve` sums every allocation of the same task (held, released, aborted or closed) with the new claim and refuses beyond the cap (`task-budget-exceeded:<task>`), in addition to the per-envelope limits; landing through the single-writer shared lane.
- Per-task allocations to date (micro-USD, conservative ceilings): FS-DATA-WRITE-LIMITS-02 44,000; FS-WRITE-TXN-PRECEDENCE-01 1,303,500; FS-DATA-WRITE-COMMIT-TRANSFORMS-03 397,110 (three reservations, two aborted before data); broad batch `0cbedb4b` 305,127 (planning estimate); Auth probes `e4d34ccc`, `97dd49dc` unpriced (allowance 10,000 each). Program-wide total of these: about US$2.09. No invoice has been measured anywhere.
- Next observation: `FS-LIMIT-API-REQUEST-BYTES`, prior task allocation 303 micro-USD, retained after the `a2d2db49c` attempt was retired as `aborted-no-data`. The earlier zero-allocation/no-request statement described preparation at `2dd6d9d9d` and is superseded by that attempt. The previous packet's hard ceiling was US$0.50; a replacement must bind its own claim while preserving the task's cumulative allocation and smaller packet limit. No replacement approval or reservation has been created at resumption. Transport provenance, a fresh packet/nonce and independent review are required before another launch.

## Parent state

`COMPAT_VERIFIED 0 / 14`. Nearest closure: FS-DATA-WRITE, waiting on the request-byte and limits-03 observations, a current-artifact saved-reference replay and the closure review. No parent is promoted by the local work above.
