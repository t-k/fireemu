<!-- Generated by compat-check --write-inventory. Do not edit. -->

# Firestore Enterprise / Native

Target: this source tree; not a release attestation. No feature-level execution receipts have been accepted. Existing tests and historical matrices are references, not evidence that this tree or the npm release passed those cases.

Labels summarize only linked capability declarations. They do not establish that the entire feature or its applicable editions are implemented. Requirement counts refer to the existing ledger, which includes broad invariants as well as behavior requirements; they are not atomic specification coverage. Missing mappings are debt, not unsupported-product verdicts.

| Feature / applicability | Capability-derived label | Requirements: mapped / unfinished | Production | Official emulator | SDK | Known limitations |
| --- | --- | --- | --- | --- | --- | --- |
| FS-PIPELINE: Pipeline validation and finite local read execution / Enterprise / Native; owner-authorized local latest reads with collection, optional single where(equal(top-level field, scalar literal)), optional select, optional non-negative int32 offset and optional limit | Partial, unverified | 1 / 1 | Not attested | Not attested | Not attested | Only collection / optional single where(equal(top-level field reference, scalar literal)) / optional top-level field-reference select / optional non-negative int32 offset / optional limit execute locally in that order. Offset skips filtered rows once across the existing paged stream. Null and NaN use unary predicates; missing fields do not match. All other functions/stages, reversed operands, nested/metadata fields, array/map/expression literals and consistency selectors remain unsupported. Production execution, ordering and exact production error parity remain unobserved; parser acceptance is not execution support.; execution outside collection / optional single where(equal(top-level field, scalar literal)) / optional select / optional offset / optional limit latest reads; transaction and read&#95;time execution, nested field extraction and metadata expressions; all functions other than exact equal in the finite where shape (including eq), general expressions, reversed operands, array/map literals and all other execution stages; text search functions against the text index catalog; proxy / record-only modes; Standard edition answers FAILED&#95;PRECONDITION FS&#95;PIPE&#95;EDITION; owner-only while rules are enforced; The finite local read subset is tested through tonic. Enterprise production execution and exact production error parity remain unobserved. Unsupported consistency selectors, metadata expressions and execution stages are refused, never silently ignored. |
| FS-TEXT: Text index definition validation / Enterprise / Native; local text-index definition/control configuration | Validation only | 0 / unknown | Not attested | Not attested | Not attested | Backfill, posting data, search scoring and local search execution are not established by definition validation.; backfill lifecycle (advanceBackfill, completeBackfill, failBuild, repair answer UNIMPLEMENTED without changing state; FS-TEXT-IDX-1); posting data and local search (Milestone G); Enterprise edition only |

## Traceability links

All links below are mappings. Test names and corpus paths must be reviewed for their actual assertions before a feature-level execution claim can be made.

### Pipeline validation and finite local read execution

Sources: FS-EDITIONS; FS-PROTO. API surfaces: FS-PIPELINE.

Requirements: INV-PIPE-001.

Contract claims: FS-CLAIM-PIPELINE.

- FS-PIPE-RPC-1: partial ([manifest](../../crates/fireemu/src/capabilities.json)).

### Text index definition validation

Sources: FS-EDITIONS; FS-INDEXES. API surfaces: None mapped.

Requirements: None mapped.

Contract claims: FS-CLAIM-TEXT-INDEX.

- FS-TEXT-VAL-1: implemented ([manifest](../../crates/fireemu/src/capabilities.json)).

[Requirements and artifact references](requirements.md) · [Sources](sources.md) · [Surface inventory](surfaces.md) · [Evidence policy](evidence-policy.md)
