{
  "name": "ftd-sdk-smoke",
  "private": true,
  "type": "module",
  "description": "Runs the real firebase-admin SDK against a running firebase-testd (manual / CI smoke).",
  "scripts": {
    "smoke": "node smoke.mjs"
  },
  "dependencies": {
    "firebase-admin": "^13"
  }
}
