<!-- Generated by compat-check --write-inventory. Do not edit. -->

# Firestore administration

Target: this source tree; not a release attestation. No feature-level execution receipts have been accepted. Existing tests and historical matrices are references, not evidence that this tree or the npm release passed those cases.

Labels summarize only linked capability declarations. They do not establish that the entire feature or its applicable editions are implemented. Requirement counts refer to the existing ledger, which includes broad invariants as well as behavior requirements; they are not atomic specification coverage. Missing mappings are debt, not unsupported-product verdicts.

| Feature / applicability | Capability-derived label | Requirements: mapped / unfinished | Production | Official emulator | SDK | Known limitations |
| --- | --- | --- | --- | --- | --- | --- |
| FS-ADMIN-INVENTORY: Database, index and retention administration / Database/index administration; TTL, backup/restore, PITR, import/export and operation lifecycle | Implemented, unverified | 1 / 0 | Not attested | Not attested | Not attested | Admin discovery fields and long-running operation semantics remain unclassified; configuration acceptance does not imply managed-service execution.; local-only bounded adapter surface; it does not claim production parity; Enterprise databases, create/update/delete, indexes, backups, import/export and long-running operations are outside this capability; the time-to-live field configuration is FS-FIELD-CONFIG-1; database metadata projection is the stable local field subset documented in docs/compatibility/firestore-admin-inventory.md; unsupported identity and billing fields are omitted; conformance input: spec/compatibility/upstream/2026-09-09-retry/discovery.json and the historical database projection v2 reference |

## Traceability links

All links below are mappings. Test names and corpus paths must be reviewed for their actual assertions before a feature-level execution claim can be made.

### Database, index and retention administration

Sources: FS-ADMIN; FS-DISCOVERY-V1. API surfaces: FS-DATABASES.

Requirements: REQ-FS-PARITY-01.

Contract claims: FS-CLAIM-REST.

- FS-ADMIN-INVENTORY-1: implemented ([manifest](../../crates/fireemu/src/capabilities.json)).

[Requirements and artifact references](requirements.md) · [Sources](sources.md) · [Surface inventory](surfaces.md) · [Evidence policy](evidence-policy.md)
