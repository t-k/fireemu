<!-- Generated by compat-check --write-inventory. Do not edit. -->

# Authentication

Target: this source tree; not a release attestation. No feature-level execution receipts have been accepted. Existing tests and historical matrices are references, not evidence that this tree or the npm release passed those cases.

Labels summarize only linked capability declarations. They do not establish that the entire feature or its applicable editions are implemented. Requirement counts refer to the existing ledger, which includes broad invariants as well as behavior requirements; they are not atomic specification coverage. Missing mappings are debt, not unsupported-product verdicts.

| Feature / applicability | Capability-derived label | Requirements: mapped / unfinished | Production | Official emulator | SDK | Known limitations |
| --- | --- | --- | --- | --- | --- | --- |
| AUTH-BLOCKING: Blocking authentication functions / Identity Platform; configured before-create and before-sign-in hooks | Implemented, unverified | 1 / 0 | Not attested | Not attested | Not attested | Hook ordering, rollback and configuration need source-section and live-oracle review.; Realtime Database and Remote Config triggers (no such service in the daemon); protobuf CloudEvents payloads (JSON is what firebase-functions consumes); Blocking Functions use a bounded synchronous loopback bridge with the production seven-second response deadline; explicit Functions SDK error classes retain their canonical status in the Identity Toolkit error envelope, an unhandled handler or transport failure is unavailable, and an elapsed deadline returns production's opaque 503 Error code: 47 without publishing speculative user state; firebase-tools 15.28.2 configures only beforeCreate and beforeSignIn in the Auth emulator; Firebase Functions SDK beforeEmail / beforeSms and beforeEmailSent / beforeSmsSent hooks are production-only and are not a Local Emulator Suite parity gap; Eventarc and Cloud Tasks support listeners start with a configured Functions runtime on the official default ports 9299 and 9499; selecting either support service without a Functions codebase is an accepted no-op, matching firebase-tools 15.28.2; no fireemu-only alert-injection route exists: firebase-tools has none either, and its Emulator UI fires an alert by POSTing to /google/publishEvents like any other client; Eventarc trigger definitions are bounded to 256 KiB after project substitution, the registry to 4096 entries and 16 MiB including owned lookup and display indexes, each publish to 256 input events and 256 deliveries after fault expansion, and all active Functions events to 4096 records and 64 MiB; Eventarc is capped at 3072 records and 48 MiB so other sources keep a reservation, refusal is atomic, full custom channel names retain the project boundary, successful official google routes expose CORS only to a loopback origin, and OPTIONS follows the pinned emulator route miss; Eventarc removal treats JSON object member order as semantically irrelevant, and a source reload rebuilds the manifest-owned table at the next exact region/name/generation key rather than retaining manually duplicated stale-generation registrations; a task may not name a URL of its own (TaskOptions.uri): only this emulator's function URL is dispatched to, because a local emulator making an arbitrary outbound request on a caller's say-so is a request-forgery surface the rest of fireemu does not offer; the pinned emulator bounds each pending queue at 10000 tasks but retains completed task names forever; fireemu additionally bounds all outstanding task data to 10000 tasks and 64 MiB, with completed duplicate-history queue and task names retained inside that byte budget and capped at 65536 entries and 8 MiB of identifier text across all queues, returning 429 when the process-wide safety budget is exhausted; task retry backoff runs on real time, not the virtual clock, as it does upstream and in production; the default unit is 100 ms, so three attempts cost 300 ms rather than a clock advance, and await-idle waits for a task across its attempts |
| AUTH-IDP: Federated identity-provider fixtures / Local fixture assertion handling; OAuth/OIDC/SAML protocol and real provider verification are distinct | Mock/fixture only | 0 / unknown | Not attested | Not attested | Not attested | A fake JWT or JSON SAML assertion is not a real provider credential exchange.; the official emulator stores no SAML / OIDC provider configuration (defaultSupportedIdpConfigs / inboundSamlConfigs are in its OpenAPI spec but not served), so a saml.&#42; / oidc.&#42; provider needs no registration and there is none to round-trip; conformance: measured by crates/fireemu-adapter-http/tests/auth&#95;flows.rs and crates/fireemu-core-auth/tests/store.rs against the pinned firebase-tools operations.js / widget&#95;ui.js; no gated corpus scenario was added in this pass |
| AUTH-SMS-MFA: SMS multi-factor authentication / Identity Platform; configured MFA and first-factor eligibility | Implemented, unverified | 0 / unknown | Not attested | Not attested | Not attested | Local verification codes do not attest real SMS delivery, retries, anti-abuse or production MFA configuration.; phone second factors: mfaEnrollment:start/finalize/withdraw with phoneEnrollmentInfo / phoneVerificationInfo, mfaSignIn:start/finalize with phoneSignInInfo; codes come from /emulator/v1/projects/{p}/verificationCodes; up to five factors per user; measured against firebase-tools 15.28.2: enrollment is refused with UNSUPPORTED&#95;FIRST&#95;FACTOR for an anonymous, phone, custom-token or Game Center session, with UNVERIFIED&#95;EMAIL for an unverified email and with SECOND&#95;FACTOR&#95;EXISTS for a number already enrolled, on both the start and the finalize step and without creating or consuming a verification code; a pending-credential response obfuscates the number to its last four digits; the spec declared SMS out of scope; the user asked for Identity Platform parity; conformance: auth/mfa-error-shapes, auth/mfa-enrollment-eligibility (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production |
| AUTH-TENANTS: Tenant isolation and configuration / Identity Platform; project and tenant client/Admin REST routes | Implemented, unverified | 1 / 0 | Not attested | Not attested | Not attested | Per-tenant provider, MFA and policy combinations need additional source and oracle mappings.; nothing is mailed or texted: codes are read from the emulator routes and are deterministic from the session seed; reCAPTCHA tokens are accepted unchecked; error envelopes measured against firebase-tools 15.28.2: EMAIL&#95;NOT&#95;FOUND / INVALID&#95;PASSWORD by default and INVALID&#95;LOGIN&#95;CREDENTIALS under improved email privacy, MISSING&#95;EMAIL before MISSING&#95;PASSWORD, the Not Found envelope with status NOT&#95;FOUND for an unknown path, kind fields on every response, absent rather than null optional fields, 28-character generated local ids; one route table (crates/fireemu-adapter-http/src/identity&#95;toolkit/routes.rs) drives dispatch, the App Check bypass classification and the bounded operation labels; a route cannot be dispatched without a label; transient credentials are bounded: email action codes expire after an hour and phone codes after ten minutes of virtual time, pending second-factor sign-ins after an hour, and each kind refuses with QUOTA&#95;EXCEEDED past 1000 outstanding codes per project or 32 pending sessions per user; expired entries are swept before every request and never observable; refresh tokens are not swept; Admin batchCreate (importUsers) verifies a rawPassword or the emulator's fakeHash form; any other passwordHash is kept out and the account is created without a password credential, which is observably what the official emulator does with a hash it cannot compare; Admin accounts:query preserves the official emulator's unbounded, ignored-limit behavior under the emulator profile; strict applies the documented Identity Platform default and maximum page size of 500, offset ordering, count semantics and field validation as an explicit production-oriented boundary; conformance: auth/sign-up-and-sign-in-errors, auth/identity-toolkit-error-shapes, auth/oob-code-shapes, auth/admin-account-lifecycle, auth/client-account-flows (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production |
| AUTH-TOKENS: Tokens and sessions / Authentication; Secure Token and Admin verification paths; project/tenant binding | Implemented, unverified | 0 / unknown | Not attested | Not attested | Not attested | Revocation behavior must be checked separately for each verifier and service; no release-specific evidence is attached.; with session-rsa every surface (Identity Toolkit, Firestore rules, Storage rules) refuses unsigned or foreign-signed tokens; the Firebase Admin SDK accepts only alg none while FIREBASE&#95;AUTH&#95;EMULATOR&#95;HOST is set, so verifyIdToken through the Admin SDK needs unsigned-emulator; conformance: auth/sign-up-and-sign-in-errors (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production |
| AUTH-TOTP: TOTP enrollment and sign-in / Identity Platform; explicit auth.totp configuration; supported verified first factor; tenant-specific settings | Implemented, unverified | 6 / 0 | Not attested | Not attested | Not attested | The existing Auth production corpus has no TOTP program. Linked invariants do not yet cover all eligibility, unenrollment and provider constraints.; window and enrollment TTL are local policies; an expired enrollment session answers SESSION&#95;EXPIRED for one further session lifetime and is then reaped; the verified-email requirement of phone enrollment is not applied to TOTP enrollment: the official emulator has no TOTP route to measure it against, and production behaviour is not inferred from the SMS result; default snapshots carry no shared secret (INV-AUTH-003, ADR-034 amendment): an enrolled factor is captured with a detached secret and rebound on restore to the secret the live store still holds; a factor withdrawn in between is dropped from the restored account and reported on stderr, never restored unusable; conformance: auth/mfa-error-shapes (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production |
| AUTH-USERS: User lifecycle and email/password / Authentication / Identity Platform; client and Admin REST; configured project | Implemented, unverified | 3 / 0 | Not attested | Not attested | Not attested | Broad route and lifetime invariants only; atomic per-operation source mappings and target-version observations remain open. Real email/SMS delivery and anti-abuse are separate boundaries.; nothing is mailed or texted: codes are read from the emulator routes and are deterministic from the session seed; reCAPTCHA tokens are accepted unchecked; error envelopes measured against firebase-tools 15.28.2: EMAIL&#95;NOT&#95;FOUND / INVALID&#95;PASSWORD by default and INVALID&#95;LOGIN&#95;CREDENTIALS under improved email privacy, MISSING&#95;EMAIL before MISSING&#95;PASSWORD, the Not Found envelope with status NOT&#95;FOUND for an unknown path, kind fields on every response, absent rather than null optional fields, 28-character generated local ids; one route table (crates/fireemu-adapter-http/src/identity&#95;toolkit/routes.rs) drives dispatch, the App Check bypass classification and the bounded operation labels; a route cannot be dispatched without a label; transient credentials are bounded: email action codes expire after an hour and phone codes after ten minutes of virtual time, pending second-factor sign-ins after an hour, and each kind refuses with QUOTA&#95;EXCEEDED past 1000 outstanding codes per project or 32 pending sessions per user; expired entries are swept before every request and never observable; refresh tokens are not swept; Admin batchCreate (importUsers) verifies a rawPassword or the emulator's fakeHash form; any other passwordHash is kept out and the account is created without a password credential, which is observably what the official emulator does with a hash it cannot compare; Admin accounts:query preserves the official emulator's unbounded, ignored-limit behavior under the emulator profile; strict applies the documented Identity Platform default and maximum page size of 500, offset ordering, count semantics and field validation as an explicit production-oriented boundary; conformance: auth/sign-up-and-sign-in-errors, auth/identity-toolkit-error-shapes, auth/oob-code-shapes, auth/admin-account-lifecycle, auth/client-account-flows (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production |

## Traceability links

All links below are mappings. Test names and corpus paths must be reviewed for their actual assertions before a feature-level execution claim can be made.

### Blocking authentication functions

Sources: AUTH-OVERVIEW; AUTH-V2. API surfaces: None mapped.

Requirements: AUTH-BLOCKING-001.

Contract claims: AUTH-CLAIM-BLOCKING-FUNCTIONS.

- FN-EVT-1: implemented ([manifest](../../crates/fireemu/src/capabilities.json)).

### Federated identity-provider fixtures

Sources: AUTH-REST; AUTH-EMULATOR. API surfaces: None mapped.

Requirements: None mapped.

Contract claims: AUTH-CLAIM-IDENTITY-TOOLKIT.

- AUTH-OAUTH-0: implemented ([manifest](../../crates/fireemu/src/capabilities.json)).

### SMS multi-factor authentication

Sources: AUTH-V2. API surfaces: None mapped.

Requirements: None mapped.

Contract claims: AUTH-CLAIM-MFA.

- AUTH-MFA-SMS-0: implemented ([manifest](../../crates/fireemu/src/capabilities.json)).

### Tenant isolation and configuration

Sources: AUTH-V1; AUTH-V2. API surfaces: None mapped.

Requirements: AUTH-TENANT-001.

Contract claims: AUTH-CLAIM-IDENTITY-TOOLKIT.

- AUTH-CORE-1: implemented ([manifest](../../crates/fireemu/src/capabilities.json)).

### Tokens and sessions

Sources: AUTH-SESSIONS; AUTH-REST. API surfaces: AUTH-REFRESH.

Requirements: None mapped.

Contract claims: AUTH-CLAIM-TOKENS.

- AUTH-TOKEN-1: implemented ([manifest](../../crates/fireemu/src/capabilities.json)).

### TOTP enrollment and sign-in

Sources: AUTH-TOTP; AUTH-DISCOVERY-V2. API surfaces: AUTH-ENROLL-START; AUTH-ENROLL-FINALIZE; AUTH-SIGNIN-FINALIZE.

Requirements: INV-AUTH-001; INV-AUTH-002; INV-AUTH-003; LIVE-AUTH-001; INV-AUTH-006; INV-AUTH-007.

Contract claims: AUTH-CLAIM-MFA.

- AUTH-MFA-TOTP-1: implemented ([manifest](../../crates/fireemu/src/capabilities.json)).

[Account disable / re-enable: diagnostic candidate](auth-disabled.md). Eighteen route observations include three visible differences in disabled-token errors and refresh reuse after re-enable. Baseline, unaffected-account and fresh-signin controls complete, but this is not approved compatibility coverage.

[Account disable / re-enable: corrected artifact recheck](auth-disabled-recheck.md). All 18 semantic projections match the unchanged saved production observation on the new local artifact. This is not a new production run or an approval, and the original mismatch remains preserved.

[Account disable / re-enable: scoped approval](auth-disabled-recheck-approval.md). These 18 observations are verified and approved only for the recorded artifact, settings and operation order against saved production observations. This approval supersedes the candidate status for that exact subject, not all session validity or Auth behavior.

[Deleted-account credential observations](auth-deleted.md). Twelve route observations compare fixed credentials before and after end-user deletion, with an unaffected successful account as control. Exact error classifications and any differences remain visible; no approval is inherited.

[Deleted-account credentials: corrected artifact recheck](auth-deleted-recheck.md). All 12 semantic observations match unchanged saved production results on the new artifact, including USER_NOT_FOUND for deleted ID/refresh credentials and successful unaffected-account controls. This is an unapproved recheck, not a new production run; the original mismatch remains preserved.

[Deleted-account credentials: scoped approval](auth-deleted-recheck-approval.md). These 12 observations are verified and approved for the recorded artifact, settings and operation order against saved production observations. Identifier-based lookup authorization is explicitly excluded from this approval; no complete Auth claim follows.

[MFA pending credential across an explicit revocation: production candidate](auth-pending-revocation.md). Eight production-only observations from one saved run: a pending credential issued before an explicit validSince update was accepted by mfaSignIn:start and :finalize after the update and its readback, and the returned tokens served lookup and refresh. Phone MFA with test numbers, no tenant, no blocking hook, configuration restored; no local artifact ran this corpus and no propagation-time claim follows.

[MFA pending credential across an explicit revocation: scoped approval](auth-pending-revocation-approval.md). These 8 observations are verified and approved only for the recorded conditions, operation order and single run. The approval matches the local implementation, which does not compare a pending credential's start time with validSince, and does not extend to password changes, other MFA methods, tenants, blocking hooks, SDK checkRevoked, Rules or credential expiry.

[Blocking function that disables the account: production candidate](auth-blocking-disable.md). Twelve production-only observations from one saved run with a beforeSignIn function deployed for the run: when the function answered disabled: true, a password sign-in and a phone MFA finalize were both refused with USER_DISABLED on the same request (the refused rows record HTTP status and classified errors; token absence was not independently checked); the control completed before and after. The function was removed and the configuration restored. fireemu now refuses the same request; no local artifact ran this corpus.

[Blocking function that disables the account: scoped approval](auth-blocking-disable-approval.md). The twelve-case frame (eight executed rows, four conditionally skipped) is verified and approved only for the recorded function shape, conditions, operation order and single run: USER_DISABLED on the same request for a password sign-in and a phone MFA finalize, the immediate readback of the flag as recorded (false for one account, true for the other), and the recorded removal and restore. It does not extend to token absence inside the server, immediate persistence on every path, created-then-disabled accounts, beforeCreate, tenants, SDK or Rules.

[Current lookup authorization regressions](../../tools/auth-lookup-authorization/README.md) separate token-verified self lookup from authenticated Admin identifier search. These local regressions correct the identifier bypass independently of the approved deletion observations; no production error-code parity or complete Auth authorization claim is made.

[Current OOB issuance authorization regressions](../../tools/auth-oob-authorization/README.md) restrict returned action credentials to authenticated Admin routes and require verified end-user identity for verification delivery. These are local regressions, not new production observations or complete Auth authorization coverage.

[Supplementary-character boundary: scoped human approval](auth-password-unicode-boundary-approval.md). The three recorded patterns are verified and approved: 4095/4096 UTF-16 units accepted, 4097 refused, with credential/state and cleanup controls. Only the pinned subject, artifact and settings are approved; no general Unicode or other-route claim follows.

[Supplementary-character password boundary: candidate](auth-password-unicode-boundary.md). Three newly observed patterns at 4095/4096/4097 UTF-16 units agree on both targets, with branch-specific credential/state and cleanup checks. Separate accounts and random prefixes are used; no human approval or general Unicode guarantee is implied.

[Unicode password recheck: scoped human approval](auth-password-unicode-recheck-approval.md). Eight recorded input patterns are verified and approved as a corrected-artifact comparison against unchanged saved production observations, not a new production run or universal Unicode coverage. Only the pinned subject and recorded controls are approved.

[Unicode password upper boundary: corrected artifact recheck](auth-password-unicode-recheck.md). Eight redacted input results agree between the corrected local artifact and the unchanged earlier production observation. Candidate only, not human-approved; no fresh production run or universal Unicode claim. The original artifact's mismatch remains in the historical record below.

[Unicode password upper boundary: unapproved diagnostic](auth-password-unicode.md). Eight generated inputs completed credential and cleanup controls on both targets, but the astral over-boundary input differs: production refuses it while the recorded local artifact accepts it. This is an open compatibility difference, not approved coverage or a universal Unicode counting rule.

[Current update-field authorization regressions](../../tools/auth-update-authorization/README.md) cover rejection of four administrator-only fields before state changes. These local handler tests are separate from the approved password observations and do not establish new production-observed authorization coverage.

[Maximum password boundary: scoped human approval](auth-password-maximum-approval.md). The 21 redacted REST cases are verified and approved only for the recorded ASCII inputs, artifact and policy, including exact public-result agreement and preserved fixed credentials after refusal. This supersedes the original candidate status for the pinned subject only. Administrator-only field authorization is explicitly not approved by this observation.

[Maximum password boundary: unapproved observations](auth-password-maximum.md). These 21 redacted REST cases cover a generated 4096-character ASCII update, last-character and prefix signin controls, 4097-character refusal and continued use of fixed credentials under the recorded policy. Candidate only; no earlier approval transfers to this artifact. Unicode counting, all strings, custom policies, SDK, Rules, expiry and fault recovery remain separate.

[No-password-change session continuity: scoped human approval](auth-session-continuity-approval.md). The recorded 34 redacted REST observations are verified and approved for fixed A/B and reference-exchange credentials at finite 0/10/30-second targets, including successful refresh-issued ID lookup and recorded invalid-input, deletion and termination checks. This does not establish permanent validity, actual elapsed expiry, whole token lineage, SDK, Rules, strict causality or exact revocation propagation timing.

[Weak-password rejection: scoped human approval](auth-password-rejection-approval.md). The recorded 16 redacted REST cases are verified and approved: five-character ASCII update refusal under the minimum-six policy, preserved original credentials and selected account fields, final valid change and recorded deletion/termination. This does not establish all policy boundaries, invariance of all internal state, long-term validity, revocation timing, SDK, Rules or fault-injected recovery.

[Original weak-password rejection observations](auth-password-rejection.md). The acquisition-time candidate status is superseded only for the subject and 16 cases in the separate approval above. Prior observations and approvals remain unchanged.

[Exactly-six-character password change: scoped human approval](auth-password-minimum-approval.md). The recorded 12 redacted REST cases are verified and approved for one generated six-character URL-safe ASCII password under the recorded policy, including old-password rejection, new-password signin, update-issued ID/refresh use, selected account fields and recorded deletion/termination. Not all six-character combinations, Unicode, other policy boundaries, expiry/revocation timing, SDK, Rules or fault-injected recovery.

[Original exactly-six-character password observations](auth-password-minimum.md). The acquisition-time candidate status is superseded only for the pinned subject and 12 cases in the separate approval above.

[Original no-password-change session continuity control](auth-session-continuity.md). Its acquisition-time candidate status is superseded only for the subject and 34 observations in the separate approval above. Continuity requires accepted primary and derived-token requests, not matching rejections.

[Auth session token revision 2: scoped human approval](auth-session-v2-approval.md). The recorded 34 redacted REST observations are verified and approved for fixed A/B tokens, changed-response controls, finite 0/10/30-second targets and two invalid-refresh controls. This does not establish universal immediate revocation, exact propagation latency, whole-session lineage, SDK checkRevoked, Rules or actual elapsed expiry. Historical integrity checks are not current-runtime regression evidence.

[Auth session token revision 2 observations](auth-session-v2.md). This page preserves its acquisition-time candidate status; the separate approval above supersedes that status only for the pinned subject and 34 observations. The artifact repeats the bounded A/B experiment with two invalid-refresh controls after separating known revoked sessions from unknown input. Earlier approvals do not transfer to this artifact. [Historical integrity checks](../../tools/compat-history/README.md) preserve the original source-bound records.

[Auth session token diagnostic observations](auth-session-token.md). Fixed pre-change A/B ID and refresh credentials are observed at bounded offsets after password replacement, with fresh-token controls. This unapproved diagnostic distinguishes REST lookup from refresh exchange; it does not establish universal immediate revocation, SDK checkRevoked, Rules or elapsed-expiry compatibility.

[Auth password change: scoped human approval](auth-password-approval.md). Twelve cases verified and approved as redacted observations for fresh end-user REST tokens, no tenant, local strict and recorded authentication/password policy. Covers password replacement and old/new-password signin, not old-token revocation timing, policy boundaries, SDK or broad Auth compatibility. The candidate page preserves acquisition-time history.

[Auth password change candidate observations](auth-password.md). Twelve scoped end-user REST cases cover credential replacement, old-password rejection, new-password signin, update-token use and selected-state preservation. Unapproved redacted observations only; no revocation-timing, SDK or broad Auth claim.

[Bounded Auth basic candidate observations](auth-basic-evidence.md). These sanitized, unapproved observations do not promote the broad feature labels.

[Auth revision 2: signup-token controls and redacted provenance](auth-basic-v2.md). Twelve scoped cases; no automatic approval or broad Auth claim.

[Auth revision 2 scoped human approval](auth-basic-v2-approval.md). Twelve scoped cases verified and approved as redacted semantic observations: email/password REST using end-user tokens, no tenant, local strict profile and recorded production configuration. This does not promote the broad Auth feature labels.

[Auth photo URL candidate observations](auth-profile.md). Twelve scoped cases cover set, replacement, malformed-token refusal, selected-state preservation and clearing; no human approval or broader account-update claim.

[Auth photoUrl updates and deletion: scoped human approval](auth-profile-approval.md). The twelve recorded cases are verified and approved as redacted observations for end-user REST, no tenant, local strict and recorded production configuration. This is not approval of general profile updates or image upload/retrieval. The linked candidate page preserves acquisition-time history.

[Auth displayName candidate observations](auth-display-name.md). Twelve scoped cases cover setting, replacement, malformed-token refusal, selected-state preservation and clearing after verified UID persistence; no human approval or broad profile-update claim.

[Auth displayName updates and deletion: scoped human approval](auth-display-name-approval.md). The twelve recorded cases are verified and approved as redacted observations for end-user REST, no tenant, local strict and recorded production configuration. No fault-recovery, string-boundary, update-token-issuance, SDK or full profile-update claim. The candidate page preserves acquisition-time history.

### User lifecycle and email/password

Sources: AUTH-OVERVIEW; AUTH-REST; AUTH-DISCOVERY-V1. API surfaces: AUTH-SIGNUP; AUTH-PASSWORD.

Requirements: INV-AUTH-004; INV-AUTH-005; AUTH-PRIVACY-001.

Contract claims: AUTH-CLAIM-IDENTITY-TOOLKIT.

- AUTH-CORE-1: implemented ([manifest](../../crates/fireemu/src/capabilities.json)).

[Requirements and artifact references](requirements.md) · [Sources](sources.md) · [Surface inventory](surfaces.md) · [Evidence policy](evidence-policy.md)
