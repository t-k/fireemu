# The official Functions emulator loads `conformance/functions/` as a codebase and resolves
# `firebase-functions` / `firebase-admin` by walking up to `conformance/node_modules`, so the
# tree has to be flat. This is also what `tools/sdk-smoke` gets from npm.
node-linker=hoisted
