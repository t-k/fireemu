# Feature-group development queue

Baseline:8f2e4850. This queue supersedes the execution order in earlier historical checkpoints; it does not revise their results. Goal owners carry source review, finite cases, execution, cause analysis, evidence-backed fixes and regressions through a review-ready commit. Only the integration owner pushes. No new Cloud authentication, reads or data operations are authorized.

| Goal | Finite target and evidence | Owner / editing boundary | Completion and current state |
| --- | --- | --- | --- |
| G0 | The two existing BatchWrite recipes, production response and post-state comparison | Coordinator; frozena35f85b4 and existing execution input only | waiting-owner; technical admission already complete. Owner/window/newnonce/baseline/pricing/recovery acceptance missing. Never add new runtime changes or cases to this frame |
| G1-local | REST custom-token persistent/session claim overlap, subsequent rightful refresh, cross-tenant rejected refresh with both namespace states and positive control | gpt-5.6-luna; isolatedgoal-g1 worktree, Auth core and Auth REST modules | review-ready atd7d68d83; targeted Auth regressions and independent security review required. Local invariants and documented semantics are separate from unobserved production combinations. No new production tenant/key, signed/unsigned and strict/emulator conditions remain distinct |
| G2-local | Multiwrite Commit failure with document post-state, transaction/lock state after failure, explicit rollback and later independent write | gpt-5.6-luna; isolatedgoal-g2 worktree, Firestore core and existing gRPC transaction test module | review-ready atc517dba0 (including9071d0f3); targeted core/gRPC regressions and independent atomicity review required. Excludes G0recipes, unknown error precedence and automatic retry behavior |
| G3-local | Existing real SDK conditions selected from auth A-to-B/sign-out, denied write post-state, listener removal; at most three missing conditions | Reused existing agent; model not verifiable after new-agent thread limit. Isolatedgoal-g3 worktree, tools/sdk-smoke only | changes-requested afterbb9939f9; actual owned SDK run and independent review required. SDK/cache/server observations remain distinct; browser/WebChannel require their own executed evidence |
| G4-read | A bounded Enterprise read Pipeline from data acquisition to returned results, using documented existing stages | Reused gpt-5.6-luna G1 owner; isolatedgoal-g4 worktree, gRPC Pipeline/service files | in-progress; latest-read collection, field-reference select and limit; unsupported semantics must be refused. Validation-only is not completion. Full-text, MongoDB, optimizer and arbitrary functions excluded |

Each child Goal is narrower than its parent feature group. A local child may finish without production parity, but the parent production questions stay waiting-oracle. Runtime patches, comparator changes and expectation changes use identifiable separate commits. Existing45/46 receipts, original differences, corrected comparisons, prepared revision3, GAP-AUTH-007 and AUTH-U03 retain their previous evidence classes.

The current session exposes four active slots. Two new owners were explicitly launched withgpt-5.6-luna; a third launch reached the thread limit, so an existing agent was reused without claiming its model. Heavy Cargo compilation is leased serially; each owner uses disjoint target/build directories and mutation outputs. Measured dispatch start:2026-09-13T15:18:12+09:00. Token/cost data unavailable from the agent tool remain unmeasured, not estimated. Private commands, timestamps and review handoffs are underdocs.local/logs/2026-09-13/goals.

Validation commands are finalized against existing tests by each owner before execution:Auth and Firestore use targetedcargo nextest, SDK uses its existing Node/SDK entry and owned local artifact. Integrated checks depend on changed paths; shared runtime changes also require first46/second45 regression. Goal states change to done only after independent review, integration, necessary verification and saved evidence.
