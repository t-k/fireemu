# Identity Platform and Firestore Standard production compatibility

Status: `IN_PROGRESS`

Goal ID: `IP-FS-PRODUCTION-COMPATIBILITY`

This is the current acceptance table for pull request #1. The earlier [merge-readiness record](merge-readiness.md) remains an intermediate historical checkpoint. Passing that checkpoint does not complete this goal.

The target is application-facing Identity Platform and Firestore Standard/Native behavior for the pinned API and SDK versions and declared configurations. Production compatibility means compatible responses, side effects, authorization, errors and notifications for equivalent logical inputs and state transitions. It does not mean reproducing Google infrastructure, external identity providers, delivery networks, billing, SLAs or undisclosed abuse systems. A local trust root or delivery sink may replace an external boundary only when the replacement and its protocol checks are explicit.

The exact REST/API-definition denominator is [version `ip-fs-standard-2026-09-14.v1`](../../spec/compatibility/denominators/ip-fs-standard-2026-09-14.v1.json). Its 2,713 surfaces comprise 2,592 targets, 119 Enterprise-only exclusions and 2 explicit Datastore-mode boundary surfaces. Endpoint and field counts are not semantic coverage percentages. SDK paths, Rules, transitions, equivalence classes and cross-product behavior are tracked below and in the capability, requirement and gap records.

## Acceptance classes

| Class | Meaning | Current decision |
| --- | --- | --- |
| A | Required for this production-compatibility goal | Every target feature group below must reach `COMPAT_VERIFIED` on the final artifact. Existing bounded results may satisfy only their recorded conditions. |
| B | Outside this goal | Firestore Enterprise-only Pipeline, Enterprise full-text search and MongoDB-compatible product surfaces. Existing implementation remains and shared regressions are prevented. Datastore-mode variants remain an explicit product boundary. |
| C | Owner decision | Production campaign identity, project/database/tenant profiles, permission window, fresh nonce, accepted settings and cost limits, recovery responsibility, and any shared-setting changes. Missing decisions block only their production operations, not local work. |

No target feature is moved to class B merely because it is unimplemented, unobserved, preview, available in Enterprise too, or dependent on an adjacent service. A scope change requires an explicit owner decision.

## Feature-group status

`LOCAL_VERIFIED` and `ORACLE_COMPARED` describe evidence for finite conditions. They do not complete the parent group. `COMPAT_VERIFIED` requires the declared conditions, required production comparison, repaired differences, final-artifact regression and independent review.

| Feature group | Required conditions | Current evidence | Parent state | Remaining completion work |
| --- | --- | --- | --- | --- |
| AUTH-ACCOUNT | Create/get/search/list/update/delete, disable/re-enable, email/password/anonymous/phone/provider lifecycle, Admin batch/import/export, protected fields and configured policies | Bounded production account/update receipts and repaired 45/46-row slices; local Admin/client lifecycle and post-state; 4,096 UTF-16 password boundaries; import failure atomicity and rich default/tenant round trips; current-head saved-reference replay of four Auth corpora (48/48 rows) | `IMPLEMENTING` | Provider lifecycle, remaining hash formats, configured policies and unobserved value/state classes need final-runtime production comparison. |
| AUTH-CREDENTIAL | ID/refresh/custom tokens, session cookies, signature/issuer/audience/project/tenant/expiry/revocation and claim composition across client/Admin/Rules | Finite saved production token flows; local session-cookie duration, signature, namespace, revocation and custom/session-claim handoff use signed local and unsigned-emulator trust roots plus pinned Admin SDK 14.3.0; a strict Admin password replacement preserves a refresh session issued in the same whole second while an older session returns `TOKEN_EXPIRED`; a refresh preserves the originating session `auth_time` while advancing `iat` and `exp`, including strict revocation controls | `IMPLEMENTING` | The exact same-second production boundary, production session-cookie/custom-token/refresh-timestamp comparison, remaining claim precedence and SDK/Admin/Rules handoffs are required. |
| AUTH-ACTION | Verify email, reset password, email link/change operations, code ownership, consumption, reuse and expiry | Local OOB authorization and code-shape/lifetime tests; selected saved references | `WAITING_ORACLE` | Delivery-boundary protocol and representative production code transitions remain unobserved. |
| AUTH-MFA | SMS/TOTP enrollment/sign-in/withdrawal, factor limits and tenant/policy/revocation interactions; separate pending/session/code/TOTP lifetimes | Revision 1/2 evidence, revision 3 offline corpus, GAP-AUTH-007 and AUTH-U03 retained | `WAITING_ORACLE` | Age causality, 300/450/600 controls, fresh same-account control, TOTP and interaction matrices remain required. |
| AUTH-FEDERATION | OAuth/OIDC/SAML provider configuration, assertion verification, redirect/link/collision behavior and controlled external boundary | Local fixture-IdP behavior only; project and tenant inbound SAML `idpConfig.signRequest` leaf-mask/default/atomicity is covered by `AUTH-SAML-MASK-001`; `accounts:signInWithIdp` now exposes an inbound OAuth `refresh_token` only when `returnRefreshToken` is explicitly true, with typed input validation; provider configuration work is active | `IMPLEMENTING` | Signed issuer/audience/expiry/replay controls and representative production configuration/flow comparison are required. |
| AUTH-TENANT-BLOCKING | Project/tenant isolation for users, credentials, providers, MFA and policies; blocking events, ordering, claims, refusal, timeout and rollback | Local tenant invariants and selected production blocking-function receipts; an explicit session commits a fresh Auth namespace only after its initial reset, restores the exact compatibility-routed store when creation fails, clears tenant namespaces on project reset, and uses an opaque daemon-incarnation claim to invalidate issued ID tokens across namespace reset or replacement, daemon restart, and a snapshot restore of the parent Auth store; lifecycle transitions also re-key subsequently generated credential and action handles | `IMPLEMENTING` | The unsigned EmulatorMock profile remains caller-provided identity and does not prove issued-credential lifecycle isolation; provider/policy inheritance, event breadth, concurrency and representative tenant production comparison remain. |
| AUTH-CONFIG-SDK | Project/tenant Auth settings, password/email-enumeration/recaptcha policy, client/Admin SDK paths and configuration-dependent results | Configuration digests and finite SDK runs exist; project `updateConfig` mask-scoping, typed atomicity and endpoint-specific empty/omitted-mask behavior are covered locally by `AUTH-CONFIG-MASK-001` | `IMPLEMENTING` | Field parity, policy boundary behavior and declared SDK-version end-to-end comparisons remain. |
| FS-DATA-WRITE | CRUD, BatchGet/List, Commit, BatchWrite, Write stream, values, masks, preconditions, transforms, limits, atomicity and post-state | Broad saved production comparisons; first46/second45 and G0 keep original and repaired results; the frozen second45 runtime/evaluator pair reproduces all 45 saved production rows with exact parent, receipt and source bindings; local REST/gRPC covers Standard values, missing/null masks, preconditions, exact document-size and nesting boundaries with refusal post-state; REST BatchWrite now covers malformed-name and exists-precondition item failures with prefix/suffix and pre-existing-state readback, and preserves an operation-unspecified oneof for the backend's per-item refusal; local Write stream coverage now includes active-transaction contention, atomic no-mutation refusal and post-rollback recovery in `7fb94a56`; the current `20a90212` artifact was replayed against the immutable first46 production reference with 46/46 matching rows, complete recording and cleanup; [reviewed bounded local-artifact document-size/depth shadow](fs-write-limits-local-shadow-20260917.md) proves local controls and recovery only; the [fixed-source limits production observation](../../spec/compatibility/broad-runs/fs-write-limits-02-40dfc0da3-production-result.json) preserves its original differences; the independently reviewed [repaired saved-reference comparison](../../spec/compatibility/broad-runs/fs-write-limits-02-8b33aac4d-saved-result.json) validates acquisition and resolves the finite byte/depth diagnostic repair with one match and fifteen expected nondeterministic rows | `WAITING_ORACLE` | Production stream/transaction precedence, remaining declared Commit/BatchWrite and catalog-limit conditions, final-artifact regression, and independent review remain required. Queue placement does not waive declared parent scope. |
| FS-QUERY-INDEX | Filters, projection, sort, cursor, offset, limit, collection group, aggregation, vector, PartitionQuery, Explain and index acceptance/refusal | The current artifact replays 23/23 saved production aggregation observations. The versioned Explain v3 evaluator preserves the original indeterminate and v2 mismatch results, then compares the repaired runtime with the same frozen production receipt for six bounded query/aggregation Explain cases and six setup/readback rows, all of which match. A fresh v2 local shadow from the current source completed the same six cases with 12 observation and 6 recovery rows; it remains local-only until a production receipt is acquired. The current-head shadow at 96be665b is recorded separately as `96be665b-explain-local-shadow.json` with artifact hash a372d74aa99c319019bca992ac9248cc48cfd0a27b04f337353a56b70a34e657. Local PartitionQuery controls, bounded Standard vector execution and the 20-case strict-profile index-merge SDK regression in `244dc525-index-merge-local.json` cover REST/gRPC, transactions and index admission. The index-merge record is local-only and remains separate from the recorded production observations. | `IMPLEMENTING` | Standard vector/index behavior, partitions and uncovered filter/aggregation/Explain conditions still need representative production comparison. |
| FS-TRANSACTION | Read-only/read-write snapshots, token ownership, read/write/query sets, conflict/retry/expiry/rollback and post-state | Local REST/gRPC atomicity, lock, paging and cancellation controls; bounded retry-token replay and 60-second idle-expiry controls with post-state and lock-release readback; limited saved references | `WAITING_ORACLE` | Production conflict/error/retention behavior and SDK retry semantics need bounded observations. |
| FS-RULES | Principals/claims/tenant, request/resource/get/exists/getAfter, query proofs, multiwrite budgets, rule limits and publication consistency | Local real-gRPC matrices cover A/B/anonymous principals, tenant/custom claims, get/exists/getAfter and atomic multiwrite/transaction refusal; request-shape regressions now prove anonymous `request.auth` is explicit null while inapplicable `request.resource`/`request.query` members are absent (core and real gRPC); selected SDK interactions remain separate | `WAITING_ORACLE` | User-token production Rules behavior and Ruleset transition cases cannot be substituted with administrator REST evidence. |
| FS-LISTEN-SDK | gRPC Listen, WebChannel and declared client SDKs; event order, resume, reconnect, cache, pending writes, unsubscribe and auth switching | Local gRPC/WebChannel and finite Node SDK reconnect/switch controls | `WAITING_ORACLE` | Browser and declared mobile SDK paths plus production notification/reconnect equivalence remain. |
| FS-CONFIG-LIFECYCLE | Default/named Native databases, project boundary, indexes/exemptions, TTL, import/export, backup/PITR and management-operation contract | Database projection v2 and configuration preservation evidence; API surfaces enumerated; the importer preflights every official managed-export partition, rejects corrupt or duplicate output references and bounds partition enumeration before publishing state; the local named-database export extension preserves default, named and cross-database reference values and rejects malformed database identities before import | `WAITING_ORACLE` | Multi-partition import is local format and atomicity evidence. The named-database extension is local safety evidence only. Data-plane contract versus managed infrastructure responsibilities must be classified and representative management behavior compared. |
| AUTH-FS-CROSS | Auth tokens, claims, tenant and revocation observed through Firestore Rules, transactions, listeners and SDK state changes | Local principal/project/tenant and SDK state invariants | `WAITING_ORACLE` | Cross-product production scenarios and final-artifact comparisons remain. |

## Existing evidence boundaries

- First46 retains its original 35 matches/11 mismatches and corrected 46/46 saved-reference comparison.
- Second45 retains its original 32 matches/13 mismatches. Frozen runtime source `22320fc0` and evaluator `ec4e1fa1` reproduce a separate 45/45 saved-reference comparison with exact source, anchor, parent and receipt bindings. A later `d0732fc2` local run completed all 45 rows and cleanup, but its saved comparison remains indeterminate because that new parent evidence has not been published under the immutable source anchor; it is not promoted by the older result.
- G0 ran exactly once at fixed checkout `a35f85b4`, retaining the original 10/2 result and repaired 12/12 comparison. Its consumed permission and nonce authorize no rerun.
- The final-runtime aggregation replay compares 23/23 valid saved production HTTP observations. Missing and transport-failed rows remain indeterminate, and the original production matrix is unchanged.
- Local SDK, Rules, Listen, Query Explain and other invariants are not presented as production comparisons.
- The bounded `reads/read-time` replay at `c9201bf6` reuses the immutable Firestore production reference and compares the current local artifact across ten read-time operations. All ten rows match with complete local recording, state readback and cleanup; this is saved-production-reference evidence only and does not promote the parent Firestore groups.
- Raw observations, original mismatches, collector versions, hashes, approvals and failed execution records remain immutable. Re-evaluation creates a separate result.

## Current candidate verification

The current source-bound runtime candidate is `c34fa1567a2be6ce6b0a06514e1a6875986459e1`. Its focused Auth verification ran `cargo nextest run -p fireemu-adapter-http --test auth_flows --test auth_oidc_assertions --profile pr` with 112 passing tests, including the typed `accounts:signInWithIdp.returnRefreshToken` fixture contract. Package Clippy with `-D warnings`, rustfmt and the compatibility Explain suite (117 tests) also passed. The refresh-token behavior is local/spec-aligned evidence only; signed provider and representative production federation comparisons remain open. The latest feature head `d4329b735248d35727cdfabe6189810785f70618` contains documentation-only follow-ups after that verification.

After refreshing the source-bound `CompatibilitySelection` Quint evidence, the isolated final workspace verification at this candidate ran `CARGO_TARGET_DIR=target/current-candidate cargo nextest run --workspace --profile pr --no-fail-fast` with 2,279 passing tests and 81 documented skips. The same isolated target passed `cargo clippy --workspace --all-targets -- -D warnings`. Quint refresh ran all 14 model/connect/mutation/evidence gates and traceability before atomic publication; three pre-existing pending artifact records remain explicit.

The previously recorded full workspace verification at `c55fe8f1180466bd2c4cac4a8d755b730bbbefcd` ran `cargo nextest run --workspace --profile pr` with 2278 passing tests and 81 documented skips; workspace Clippy with `-D warnings` and rustfmt also passed. The pinned Auth and Firestore conformance checks pass, including 23 Auth rows and the regenerated Firestore transform corpus (326 rows agreeing with the recorded oracle; documented production divergences remain separate). The REST inventory coexistence, project/SAML FieldMask, `disableUser:null` continuity and Explain structured-error comparison regressions pass on that source. A focused rerun of the unchanged Federation runtime at `77d45eda` passed 111 Auth flow and signed OIDC assertion tests, including issuer/audience/nonce, tenant pinning, collision atomicity and SAML attribute paths; this remains local evidence pending production comparison.

These checks do not promote local-only or saved-reference evidence to current production compatibility. The candidate verification itself used no production operation; the existing campaign package remained subject to its fixed configuration and API-key admission baseline until the separately recorded bounded run below. PR #1 is Draft and currently clean against `main`; the remaining parent groups and production-only evidence below remain open.

The local Auth custom-token boundary was tightened at `1277fe8`: developer claims are checked against the existing canonical JSON size limit before account lookup or creation, and an oversized claims payload is rejected without creating a user. The focused Auth flow suite and package lint pass on that source. This closes only the bounded local size and no-side-effect condition; exact production error wording, claim composition and the remaining custom-token/tenant cases still require their declared production evidence.

On 2026-09-15, a read-only ADC preflight using `x-goog-user-project: fireemu-35fe6` confirmed the expected project identity, Database `database-settings-v2` projection and Auth configuration digest. The existing API-key resource is still owned by the expected project, but its current key string does not match the digest bound to the frozen Explain campaign. Admission stopped before any campaign data request; the accepted baseline and prior receipts remain unchanged. A subsequent read-only Firebase SDK-config lookup was used only to verify the current web-app API-key binding; it created or mutated no data and changed no configuration. Its digest matched the preflight result and still differs from the frozen campaign baseline, so no campaign was started or rebased.

Under the later bounded autonomous authorization for operations below USD10, the closed broad candidate at `0cbedb4b` was executed exactly once. The production receipt contains all 46 rows (19 Auth and 27 Firestore), configuration postflight remained unchanged, cleanup completed with no unrecovered resources, and the production-versus-local comparison reports 46/46 matches with valid bindings. The planning estimate is USD0.3051264453125; the adapter did not measure an invoice amount. The immutable result summary is [`0cbedb4b-production-result.json`](../../spec/compatibility/broad-runs/0cbedb4b-production-result.json). This candidate result does not promote the broader parent groups to `COMPAT_VERIFIED`, and no automatic retry or follow-up production operation was performed.

The Explain comparator follow-up is recorded separately in commit `0cbedb4b`: complete structured `INVALID_ARGUMENT`, `FAILED_PRECONDITION`, `OUT_OF_RANGE`, `NOT_FOUND`, `ALREADY_EXISTS` and `UNIMPLEMENTED` responses are eligible for semantic comparison when lifecycle and transport evidence is complete. Authentication, quota, transient, malformed and incomplete responses remain indeterminate; prior receipts and comparison counts are unchanged.

On 2026-09-15, a bounded four-step production Auth probe confirmed case-insensitive email ownership for password sign-up/sign-in: mixed-case creation, lower-case sign-in, case-variant duplicate rejection and deletion all completed successfully. The original production receipt is retained separately from the repaired local result in [`97dd49dc-auth-email-case-repair.json`](../../spec/compatibility/broad-runs/97dd49dc-auth-email-case-repair.json). Runtime commits `97dd49dc` and `7d50ad95` canonicalize email ownership and action-code matching across create, update, import, lookup, password reset, email-link and user reactivation paths; core and adapter regressions plus the frozen Explain suite pass. This closes only the observed email-canonicalization condition; the broader `AUTH-ACCOUNT` and `AUTH-ACTION` groups remain `IMPLEMENTING`/`WAITING_ORACLE` until their declared production conditions are compared.

On 2026-09-14, the revision-2 Auth session recorder completed a fresh bounded 34-case production observation with a disposable owned account. Cleanup and configuration preservation succeeded, and the current strict local artifact compared as 34 matches with no indeterminate rows. The redacted candidate is [`e4d34ccc-auth-session-v2-production-recheck.json`](../../spec/compatibility/broad-runs/e4d34ccc-auth-session-v2-production-recheck.json); the original revision-2 receipt and approval remain unchanged. This adds candidate evidence for the recorded REST session conditions only and does not close the broader `AUTH-CREDENTIAL` scope.

## Completion rule

This goal reaches `READY_FOR_COMPATIBILITY_REVIEW` only when every target parent group is `COMPAT_VERIFIED`, all required CI and final-artifact checks pass, saved and new production evidence remains reproducible, independent reviews have no blocker, and owned resources and processes are reclaimed. Until then the repository reports `IN_PROGRESS`, `BLOCKED_OWNER` for the specific owner-dependent production actions, or `BLOCKED_TECHNICAL` for a concrete technical blocker.

## Current local checkpoint (`8afa350d`)

The Firestore `ListDocuments` adapter now rejects `showMissing=true` together with any non-empty raw `orderBy` (including whitespace) immediately after parent parsing, before fault injection, consistency selection or page-token decoding. The valid name-ordered missing-parent path remains covered separately, and page-token binding regressions still pass. Source-bound Quint digests for the affected local adapter were regenerated and the evidence and traceability contracts pass. This is a local contract correction; production listing behavior remains unobserved.

## Current local checkpoint (`0dbf7fef`)

The current feature artifact completed a fresh local shadow of the six-case Query Explain campaign (query and aggregation plan-only, analyze and empty-analyze) with 12 observation rows and six recovery rows. Artifact, observer, comparison-contract and configuration digests are recorded in [`0dbf7fef-explain-local-shadow.json`](../../spec/compatibility/broad-runs/0dbf7fef-explain-local-shadow.json). The owned process and listeners stopped cleanly, cleanup and state verification passed, and `productionExecuted` remains false; this record is not production compatibility evidence.

## Current local checkpoint (`8126f8f3`)

The Firestore local transaction adapter now expires an idle transaction before a contending write waits on its locks. The idle check and rollback occur under one database write lock, so a concurrent activity refresh cannot race with the stale decision. The new idle-holder regression, the existing busy-holder and rollback cases, the core transaction expiry test, adapter/core Clippy and formatting pass. This is local transaction semantics evidence; production conflict, retention and SDK retry behavior remain unobserved.

The Rules matcher regression suite also covers both positions of an allow rule relative to expensive non-matching recursive-wildcard siblings, including leaf blocks that only consume a prefix and version-1 parent/child matching. Six targeted tests pass, including the fail-closed false/unresolved case. Structural non-match pruning and the established-allow budget exception remain in place; this does not claim production compiler or matcher-limit parity.

A fresh current-head Query Explain shadow is recorded in [`11f2adb3-explain-local-shadow.json`](../../spec/compatibility/broad-runs/11f2adb3-explain-local-shadow.json). It completed the six bounded cases, twelve observation rows, six recovery rows, owned cleanup and process/listener shutdown with the current artifact. No production request was made. The existing campaign's API-key baseline remains separate and is not silently rebased by this shadow.

## Current local checkpoint (`41b81ecf`)

The Rules evaluator now isolates caller parameters and `let` bindings from the declaration-time path captures used by a callee, while resolving function names through a shared parent-linked lexical environment. Declarations in one scope no longer create a per-request quadratic reference table. Firestore and Storage regressions cover caller shadowing, nested same-name declarations and environment sharing; the production compiler and memory/performance behavior remain unobserved. The independent correctness and Security reviews reported no blocking finding.

The finite `G4-select-copy-local` child is integrated at `3a173cb0`. It exercises the real Enterprise Pipeline `select`/`limit` entry with 17-document and 257-document fixtures across one and nine pages, preserves aliases and integer types, and records projected copy totals of 612 and 9,252 field bytes. This is local measurement only; it does not establish Enterprise or Firestore Standard production compatibility, process RSS bounds or a global quota.

## Current verification checkpoint (`9ee7f6c1`)

The Rules matcher allow-budget repair and its source-bound Quint evidence are integrated. The workspace formal test gate ran `cargo nextest run --workspace --profile pr --no-fail-fast` with 2,309 passing tests and 81 documented skips after refreshing the `CompatibilitySelection`, `RegexAuthorization` and `RegexEvaluationCache` evidence digests. Workspace Clippy with `-D warnings`, rustfmt and the targeted Rules, REST/Rules, Storage and Auth regression suites passed. The new `GAP-FS-RULES-007` entry records that an established allow survives only matcher-work exhaustion; unsupported operations, expression and call-depth failures, and other budgets remain fail-closed. This is local evidence; production Rules compiler, matcher limits and user-token parity remain unobserved.

## Current local checkpoint (`dccb9f6a`)

The finite `G2-write-stream-transaction-local` child is integrated at `7fb94a56`. A Write stream write contending with an active read-write transaction is refused with `ABORTED` without publishing any item in its multi-write request; after rollback, a fresh stream writes the document successfully. The 27-stream-test suite, integrated 334-test adapter run (one existing skip), adapter Clippy and the independent review passed. This remains local evidence: production stream/transaction precedence, REST/SDK parity and error ordering are unobserved, and the parent `FS-DATA-WRITE`/`FS-TRANSACTION` groups remain open.

## Current verification checkpoint (`13c1cbe3`)

The current feature head revalidated the existing compatibility surface without new production traffic. The official 14-model Quint refresh updated only source-bound digests for `AtomicExportPublication`, `AuthTotp` and the transitively affected `CompatibilitySelection`; all baseline, connect, mutation, evidence, traceability and atomic publication gates passed. Workspace nextest passed 2,290 tests with 81 documented skips, workspace Clippy passed with `-D warnings`, compat-broad passed 583 tests with 10 documented skips, and conformance passed 24 scenarios with 199 gated steps. Auth and Firestore dedicated probes remained at 23 and 326 saved-fixture rows respectively. These results refresh current local and saved-reference evidence only; they do not promote the remaining parent groups to `COMPAT_VERIFIED` or close production-observation gaps.

## Current local checkpoint (`1cf58fe2`)

The Rules evaluator now preserves an established allow when later, non-matching match-path exploration exhausts `FIREEMU-RULES-MATCH-WORK`. The same exception is applied when nested exploration is reached after a successful allow. Expression budgets, call-depth limits, unsupported operations and other evaluator failures remain fail-closed, and a request with no successful allow still reports the matcher budget denial. Firestore and Storage regressions load the rules through `LoadedRules::from_source()` and exercise a successful allow followed by 750 recursive-wildcard nonmatches. The 34-test Rules evaluation suite and Rules Clippy passed on the fixed source. This is local evaluator evidence; production Rules compiler and limit behavior remain unobserved.

## Latest Rules leaf reachability follow-up (`87bfd54a`)

- Added an end-to-end `LoadedRules::from_source()` regression for 750 leaf patterns that match an early prefix but cannot consume the complete request. Firestore and Storage keep a valid allow reachable regardless of whether it appears before or after those siblings; false allow controls remain denied.
- The existing rejection-only leaf prefilter already prevents these partial leaves from entering `match_path`; no runtime change was required. A temporary mutation removing the full-consumption guard failed at the matcher-work budget, confirming the regression catches the prior order-dependent denial.
- Focused Rules verification passed five related tests, including v1 parent/child traversal and fail-closed controls. This remains local evidence; production compiler and matcher-limit parity are unobserved.

## Latest REST resource parsing follow-up (`08b9f9c3`)

- Firestore REST document classification and shared parent decoding now parse `projects/{project}/databases/{database}/documents` by positional path segments. Valid project/database identifiers containing the word `documents` no longer get split inside an identifier and sent to the wrong database.
- End-to-end local coverage exercises create, get, list and `:commit` for `documents-db` and exact `documents` database identifiers, plus decoder coverage for a `documents-project` project. The existing route, authorization and custom-method tests remain passing; production REST parity is unobserved.

## Latest REST malformed-resource follow-up (`7e569264`)

- Decoder coverage now rejects a `documents-extra` suffix, and the real REST handler rejects empty document path segments such as `documents//items` and `documents/items/`. These checks preserve the positional route boundary without changing valid database-root trailing-slash behavior.
- The adapter library suite passed 108 tests with one existing skip and the REST integration suite passed 41 tests. Workspace verification on this source passed 2,335 tests with 81 documented skips, workspace Clippy, rustfmt, the Quint evidence contract and traceability checks. This is local routing evidence; no production observation or historical receipt was changed.

## Latest Rules subtree reachability follow-up (`99628df0` / `d4c08a81`)

- `block_can_match_segments` now follows relative child match paths and only admits a parent branch to the bounded matcher when a direct allow can consume the request or a descendant can consume the remainder. A parent may still match a prefix when a child can complete the path; a partial subtree with no complete path is rejected before matcher-work charging.
- The Firestore and Storage regression covers 750 parent/child siblings, allow placement before and after those siblings, true and false controls, the equivalent partial-leaf case, and the valid version-1 parent/child path. The pre-fix test failed with `BudgetExceeded` at matcher step 65,537; the fixed suite passes and the existing over-budget unresolved case still fails closed.
- `RegexAuthorization.json` and `RegexEvaluationCache.json` were regenerated by the repository Quint tool in `d4c08a81`. This remains local evaluator evidence; production Rules compiler and matcher-limit parity are unobserved.

## Rules reachability review follow-up (`a59cf67b` / `4198bbf6`)

- The prefilter now reports whether the block itself covers the request even when no descendant can reach an allow. This preserves `NoMatchingAllow` versus `NoMatchingRule` while still skipping impossible subtrees.
- Structural prefilter transitions have their own bounded counter (`MATCH_PATH_PREFILTER_WORK_MAX`). On exhaustion the result is treated as unknown and sent through the existing matcher budget, so the prefilter cannot silently reject a potentially valid path. Added controls cover Rules versions 1 and 2 and an abstract query path through a recursive parent.
- Rules production compiler, matcher-limit, and memory behavior remain unobserved.

## Rules parent endpoint follow-up (`adcc68b9` / `cf2b0b02`)

- Parent blocks now retain the relative offsets where a descendant can consume the complete request. The matcher uses a bounded dynamic-programming table to skip recursive-wildcard splits that cannot reach those offsets, while direct allow-only leaves keep the existing matcher-work guard.
- The new Firestore and Storage regression covers 750 parent `**` blocks with a child endpoint that can only complete at the final segment, Rules versions 1 and 2, and both true and false allow controls. Before the fix, the later exact allow was blocked by `FIREEMU-RULES-MATCH-WORK`; the focused and full 45-test Rules suites now pass.
- The independent follow-up correctness review approved the covered-path classification, bounded prefilter fallback, and v1/v2 and abstract-path coverage. This remains local evaluator evidence; production Rules compiler and matcher-limit parity are unobserved.

## Current local repairs and campaign preparation (`abc21754`, `d1f1302c`, `c4bdf30a`)

- Firestore REST `writes` now rejects present scalar or null values as `INVALID_ARGUMENT` without mutating the target. The adapter regression exercises both malformed shapes and verifies target absence; this is local REST evidence and does not establish SDK or production parity.
- Auth Admin `accounts:lookup` now rejects malformed `federatedUserId` entries (non-object items, missing or non-string `providerId`/`rawId`) before lookup and returns no partial `users` result. The focused identity-toolkit suite passed 80 tests. The production error wording and SDK behavior remain unobserved. `CompatibilitySelection.json` was regenerated in `b0c32838`.
- The next Auth refresh/ListCollectionIds campaign adapter was restored from immutable historical source and connected to a loopback-only shadow. Its package records 16 observation rows, 16 recovery requests, `completed: true`, `productionExecuted: false`, `processCleanup: true`, and evidence SHA-256 `c49e1012c4fd49c540ab0854dbede12f15093f510a4e8f8ff06654fe388f9874`. Owner, permission, execution window, nonce, current-environment inputs and production expectation remain unset; the shadow is not production compatibility evidence.
- The shadow now restores the shared batch transport after completion, so a local campaign run cannot change the transport used by later compatibility tests. The regression asserts that the original transport is restored; the full compat-broad suite passed 603 tests with 10 documented skips.

## Current Explain shadow (`5fe5972c`)

The current feature head completed another local shadow of the six bounded Query
Explain cases. The redacted record
[`5fe5972c-explain-local-shadow.json`](../../spec/compatibility/broad-runs/5fe5972c-explain-local-shadow.json)
binds twelve observation rows and six recovery rows to its artifact, observer and
comparison-contract digests. State verification, cleanup, configuration preservation
and process/listener shutdown passed. `productionExecuted` remains false; this is
local evidence for the prepared campaign and does not promote Explain or the parent
query/index group to production compatibility.

## Latest local gap repair (`9e159414`)

The Firestore REST `BatchWrite` adapter now validates `labels` as an object whose values are strings before publishing any writes. A malformed-label request is rejected atomically and the target remains absent. The REST, Identity Toolkit and Rules regression suites, package Clippy and formatting passed on the integrated source. This is a local contract repair; it does not change saved production receipts or promote the parent feature groups to `COMPAT_VERIFIED`.

## Latest input-shape repairs (`0f7ff7ee` / `a3c9baaa` / `f91f07bb` / `9ec439ff`)

Auth import now refuses malformed provider, MFA, lifecycle and configuration members before startup instead of coercing them to defaults. Unknown account, providerUserInfo, MFA/TOTP and Auth config members are retained by the export-format parser for diagnostics but are refused by the live Auth import because the running store cannot preserve them. Import records are preflighted against cloned stores before replacing the live default or tenant store, so duplicate or otherwise invalid later records cannot leave a partial Auth state. These limitations and atomicity guarantees are recorded in the capability contract. Valid password, email-link, federated, MFA and lifecycle round trips remain covered.

Firestore REST `BatchGet` now rejects a scalar `documents` member and non-string array entries before transaction state or reads begin. Structured query `from` and `orderBy` reject present non-array values and malformed array elements rather than defaulting to a kindless scan. Valid omitted and array forms remain covered. These are local transport-contract repairs; production REST and SDK parity remain unobserved, and historical receipts are unchanged.

## Current feature checkpoint (`9eccdc43`)

The current REST decoder accepts ProtoJSON `null` for optional structured-query messages and accepts numeric `DIRECTION_UNSPECIFIED` (`0`) alongside the named and ascending/descending enum forms. The regression enters the real REST request path and keeps malformed arrays and enum values rejected. The Auth-list local shadow now propagates state validation independently from recording completion, and its checked-in package binds the current shadow source, observer digest, execution commit and public result hash. The fresh owned-artifact shadow completed 17 observation and 19 recovery requests with recording, state validation and cleanup complete. This remains local evidence with `productionExecuted: false`; the historical `a24eea35` artifact and all saved production receipts remain immutable.

## Current local checkpoint (`40776482`)

The Auth-list campaign regressions now bind the current package test to the checked-in shared-gate digest and assert the complete immutable SHA-256 of the legacy v2 package. The Auth batch-import null regression also retries an existing `localId` with `allowOverwrite: null` and verifies through the real lookup route that the existing account is not replaced; malformed non-null shapes remain rejected. The targeted Rust test and 25-case auth-list campaign suite pass, and GitHub Actions `ci` run `34996540907` passed. The compatibility-inventory workflow `34996540807` was still running at the checkpoint. These are local evidence and package-integrity protections; no production operation or historical receipt changed.

## Current local checkpoint (`91addedf`)

The immutable v2 campaign test now pins both referenced local receipts in addition to the complete package hash: the legacy fixture receipt is `c49e1012c4fd49c540ab0854dbede12f15093f510a4e8f8ff06654fe388f9874`, and the owned-artifact receipt is `91081dccb9718347847a76f9bb83340de3655fe09155487e1b392f8d2168b926`. The focused package-integrity tests pass (`2 passed`); this is a test-only evidence safeguard and does not rewrite either receipt or perform production communication.

## Current evidence checkpoint (`6531ff86`)

The v3 Auth/ListCollectionIds campaign package is now pinned by a complete SHA-256 assertion (`2aee67fe8d27d0e28ab623915eb7207b9f85f6948aad754fc7288e0b822d1c2b`) in its offline integrity test. The package, local artifact, observer and historical receipts remain immutable; the focused campaign suite passes 25 tests. This closes an evidence-envelope mutability gap only and does not add production observations or promote any parent feature group.

## Current local checkpoint (`8cba48ab`)

The Firestore index reload supervisor now compares stable file content even when size and modification time are unchanged, retains the last-known-good catalog across malformed JSON and invalid UTF-8, retries corrected generations, and records a generation only after the catalog replacement succeeds. The replacement API reports poisoned catalog locks so a failed update remains retryable. The configuration reload suite passes 17 tests, the adapter lock-failure regression passes, and package Clippy passes. This is local lifecycle evidence; production index behavior remains unobserved.

## Current local checkpoint (`b37b78f9`)

The Auth `batchCreate` path now treats ProtoJSON `null` for `allowOverwrite`, repeated `providerUserInfo`/`mfaInfo`, and optional timestamps as unset while rejecting malformed non-null values and null array elements. Real-handler regressions cover omitted, null and empty forms, duplicate local IDs with the default overwrite policy, lookup post-state and failed-row non-mutation. The identity-toolkit suite passed 83 tests and the focused package Clippy check passed. This is local Auth evidence; production import behavior remains unobserved.

The Auth/ListCollectionIds local campaign now rejects malformed parent resource paths and validates the missing-document-parent case plus exact paged collection IDs, parent binding, duplicate pages and omissions before setting `stateValidation`. The historical v3 package remains immutable; a distinct v4 package and owned-artifact result bind the updated shadow source, observer, gate, manifest and result hashes. The focused campaign suite passed 26 tests, all with `productionExecuted: false`. This is local campaign evidence and does not promote a parent group to production compatibility.

The named Firestore Rules reload regression verifies default and named database isolation, observes a malformed named generation through a test-only scan counter, retains the last-known-good generation, and confirms recovery without a fixed sleep. The targeted test and Firestore Clippy passed. This remains local publication and routing evidence; production Rules compiler and reload behavior remain unobserved.

## Current campaign checkpoint (`9dddd497`)

The Auth/ListCollectionIds shadow now rejects non-object request bodies and malformed parent paths with empty or trailing segments before dispatch. Its missing-parent case uses an even document path, while paged results are checked for the bound parent and exact collection IDs without duplicates or omissions. The immutable v3 and v4 packages remain unchanged; v5 binds the hardened shadow, the manifest source anchor and a fresh local-artifact result with exact hashes. The focused campaign suite passed 27 tests and the owned artifact completed locally with recording, state validation, cleanup and process shutdown; production remains disabled.

## Current campaign checkpoint (`22579aee`)

The Auth/ListCollectionIds shadow now rejects embedded empty parent segments as well as malformed bodies, leading or trailing separators and odd document paths. The v4 and v5 package metadata are pinned by immutable package digests, and v6 binds the corrected shadow source, observer, manifest, result and owned-artifact hashes without changing the earlier packages or receipts. The focused campaign suite passes 28 tests, including a real fireemu-artifact shadow with recording, state validation, cleanup and process shutdown. The Auth batch-import null handling remains covered by the 83-test identity-toolkit suite and its independent security review found no blocking issue. These are local evidence and campaign-integrity repairs; production execution remains disabled and no historical observation was rewritten.

## Current saved-reference replay (`7fbb3e85`)

The current source was run through the existing first46 local adapter and compared with the immutable `ab7bd698`/`bc38f392` saved production observations. The new candidate [`7fbb3e85-first46-saved-recomparison.json`](../../spec/compatibility/broad-runs/7fbb3e85-first46-saved-recomparison.json) records valid bindings, complete recording, cleanup and 46 matching rows under the existing comparison contract. The original 35-match/11-mismatch comparison, its receipts and the later repaired candidates remain unchanged; this replay performed no production operation and does not promote `FS-DATA-WRITE` beyond its declared scope.

## Current saved-reference replay (`4d51cb93`)

The current feature artifact was run again through the existing first46 local adapter and compared with the immutable `ab7bd698`/`bc38f392` saved production observations. The new candidate [`4d51cb93-first46-saved-recomparison.json`](../../spec/compatibility/broad-runs/4d51cb93-first46-saved-recomparison.json) records a distinct local observer digest, valid bindings, complete recording, cleanup and 46 matching rows under the existing comparison contract. This is a new saved-reference comparison; the original 35-match/11-mismatch comparison and earlier candidates remain unchanged, and no production operation was performed.

## Current-head first46 replay (`20a90212`)

The current feature artifact at `20a90212d313a41489381b30681e6ca8f6af5385` was run through the existing first46 local adapter after the REST BatchWrite oneof repair. The resulting artifact hash is `2e85b8cb36a3ee4b3e81b48b34ff8e632c05e6af12a5e4441424861758a7f042`, the local receipt hash is `605e3d1918fab541368ac924a96d5e6212d574851c3a3c0caf6b59017f34115d`, and the wrapper manifest hash is `5cccd79458971bc95bf5d5eb67bc4641c6ff750764a04bfb75057aff7a3f3ee2`. The saved-reference comparison [`20a90212-first46-saved-recomparison.json`](../../spec/compatibility/broad-runs/20a90212-first46-saved-recomparison.json) reports valid bindings, complete recording, cleanup complete and 46/46 matching rows under the existing contract. The original 35-match/11-mismatch comparison and earlier replays remain immutable. This is saved-production-reference evidence only; no production operation was performed, and the parent still requires representative production stream/transaction and limit comparisons.

## Current-head Auth saved-reference replay (`78b1da35`)

The four immutable Auth production receipts (`auth-basic-v2`, `auth-profile`, `auth-display-name` and `auth-password`) were replayed locally against one artifact built from source `78b1da35071f93504c872b21dd9f92858d3e1fd3`. The artifact SHA-256 is `ec5a7934c1f5c92fde933ccaca814528f2ff1f7229bd988b7ad9636de2286f12`; the typed comparison digest is `9a2a77091fb2fea11718ff8592df62df7b9154e96f0e415b1f52e5f0d3fc0232`. Each 12-case corpus matched 12/12, for 48/48 saved-reference matches. The replay manifest required one shared artifact, complete owned-process cleanup and closed listeners. The candidate result is [`78b1da35-auth-saved-reference-replay.json`](../../spec/compatibility/broad-runs/78b1da35-auth-saved-reference-replay.json), and the binding spec and replay tools are recorded separately.

This is saved-production-reference evidence only: no production operation or Cloud read was performed, and the original receipts and comparisons remain immutable. It reduces the current-artifact replay condition for the four declared Auth corpora. `AUTH-ACCOUNT` remains `IMPLEMENTING` because provider lifecycle, alternate hash formats, configured policies and unobserved value/state classes still require bounded production comparison.

## Current transaction and campaign validation (`9da3750a`)

The Firestore REST `beginTransaction` decoder now validates the complete request shape before allocating transaction state. Unknown or null top-level bodies and unknown option members are rejected without side effects. ProtoJSON null oneof members are treated as unset, valid numeric and named `concurrencyMode` enum values are accepted, and valid `requestOptions.requestTags` are forwarded to the request model. The REST regression suite contains 51 tests for this path and passes on the current feature head; package Clippy and formatting also pass. This is local transport and state-safety evidence. Production transaction conflict, retention and SDK retry behavior remain unobserved.

The Auth/ListCollectionIds campaign already contains the corrected missing-parent document path and validates structured parent absence, exact collection IDs, page-token placement, duplicate-free pagination and complete result coverage before setting `stateValidation`. The current feature head rerun passed 28 campaign tests. Auth `batchCreate` treats null optional repeated fields, timestamps and `allowOverwrite` as unset while continuing to reject malformed non-null values; its focused real-handler regressions pass 3 tests. These are local campaign and ProtoJSON compatibility checks only; they do not create production evidence or alter saved receipts.

The independent review of the integrated transaction changes approved the current fixed head with no blocker or high-severity finding. The Auth route privilege review also found no blocker. On the current head, isolated workspace nextest ran 2,370 tests with 2,370 passed and 81 documented skips; workspace Clippy with `-D warnings` also passed. The three stale Quint source bindings discovered by that run were regenerated with the repository mutation generator, and the evidence and traceability contracts now pass. PR #1 remains Draft, with feature-integrity, offline-acquisition-integrity and `pr` checks passing; environment-specific matrix jobs remain explicitly skipped. No production operation was performed for this checkpoint, and no owned process, listener or temporary resource remains.

## Current local checkpoint (`e5879bea`)

The current feature head preserves the previously accepted Auth/ListCollectionIds campaign state validation and Auth `batchCreate` ProtoJSON-null semantics; both were rechecked through the existing real-artifact/local handler paths without a new production operation. The campaign suite passes 28 tests, and the two focused Auth null/unset regressions pass.

Two bounded local repairs are integrated. Firestore composite index matching now respects collection versus collection-group scope, with a regression proving that a collection-group index cannot satisfy a collection query. Auth email action links now bind the URL mode to the issued OOB code kind before applying it; both mismatch directions preserve the code and account state, including a `continueUrl` request.

The bound `CompatibilitySelection` Quint evidence was regenerated with the repository generator after the Auth changes. On this fixed head, workspace nextest passes 2,377 tests with 81 documented skips, workspace Clippy with `-D warnings`, rustfmt, Quint evidence and traceability pass, and the independent follow-up review reports no Must Fix or Should Fix findings for the final repairs.

These results are local or saved-reference evidence only. Existing production receipts, original mismatches, approvals and comparison contracts remain immutable. No new production operation or Cloud read was made; the existing Explain campaign remains blocked by its fixed API-key digest baseline mismatch. All 14 parent groups remain below `COMPAT_VERIFIED`, so the top-level status remains `IN_PROGRESS` rather than `READY_FOR_COMPATIBILITY_REVIEW`.

## Current local repair checkpoint (`ea1f700f`)

The Auth pending-credential expiry matrix now separates SMS-code expiry from pending-credential expiry and checks a fresh-code control, independently valid code readback, no-token refusals and post-state cleanup under both the emulator and strict profiles. This is local evidence only; it does not infer a production lifetime or create a new production observation.

Rules reachability now reuses request-local structural path results for equivalent recursive parent and child patterns, preventing repeated prefilter work from exhausting the matcher before a later valid allow. The new 750-branch regression passes through `LoadedRules::from_source()` and evaluation; the existing v1/v2, Storage, false-allow and bounded-failure controls remain covered. Production Rules compiler and matcher-limit parity remain unobserved.

The reported Auth/ListCollectionIds state-validation and Auth `batchCreate` null/unset issues were rechecked through their real local paths: the campaign suite passed 28 tests, the Auth null/unset focused suite passed 3 tests, Firestore REST routing and ProtoJSON null regressions passed 3 tests, and the admin inventory coexistence unit tests passed 2 tests. No production operation or saved receipt rewrite occurred.

The Auth test change required a generated `CompatibilitySelection` evidence digest refresh; it was regenerated with the repository Quint tool. The feature remains below `COMPAT_VERIFIED` for all 14 parent groups and the top-level status remains `IN_PROGRESS`.

## Rules reachability follow-up (`dd253e5a`)

The request-local structural reachability cache now has an explicit 4,096-entry cap. When the cap is reached, the evaluator recomputes the bounded rejection-only prefilter instead of retaining unbounded path-shape data; matching semantics and the independent path-work budget are unchanged. The parent/child reachability regression now covers Firestore and Storage under both Rules v1 and v2. The v1 cases use valid bounded parent/child patterns because v1 rejects non-final recursive wildcards. A unit regression verifies the cache cap.

The core Rules package suite passed 140 tests with no skips, including the new service/version matrix and cache-cap test. The integrated Rules, Identity Toolkit and Firestore adapter suite passed 860 tests with one documented skip. The Auth/ListCollectionIds campaign suite passed 28 tests and the Auth batchCreate null/unset focused suite passed 3 tests. These are local evidence and fixture checks; no production operation, saved receipt, historical comparison or prior artifact was changed. Production Rules compiler and matcher-limit parity remain unobserved, and all 14 parent groups remain below `COMPAT_VERIFIED`.

## Local verification checkpoint (`6dc6d294` / `f7b7e14c` / `b12fe9b3`)

The Rules reachability changes have current Quint evidence for both the authorization and evaluation-cache models. The full workspace nextest run passed 2,380 tests with 81 documented skips; workspace Clippy with `-D warnings`, rustfmt and diff checks passed.

Auth strict-profile token coverage now exercises fresh, exact-expiry and post-expiry ID-token lookups. Expired lookups return `TOKEN_EXPIRED` without changing the account snapshot, and refresh remains bound to the original UID. The focused regression passed and an independent correctness review approved the test-only change; production token lifetime evidence remains separate.

Firestore Admin inventory lifecycle coverage now preserves Standard/Native database metadata across project document clearing and snapshot restore, keeps other projects isolated, and returns `404` for unknown databases. The clear route uses one exclusive admission operation and a project-scoped catalog range, so concurrent admitted requests cannot create a database between catalog capture and reset, and unrelated project entries are not inspected. The REST suite passed 54 tests and package Clippy passed; an independent follow-up review approved the fix. This is local lifecycle evidence and does not establish production Admin API parity.

The Auth/ListCollectionIds state-validation and Auth batchCreate ProtoJSON-null cases remain covered by their existing real local paths (28 campaign tests and the focused null/unset regressions). No production operation or saved receipt rewrite occurred. The Explain campaign remains `BLOCKED_OWNER` on its fixed API-key baseline mismatch, and all 14 parent groups remain below `COMPAT_VERIFIED`.

## BatchWrite transport parity follow-up (`36c7da90` / `2f5c1f75`)

The REST BatchWrite path preserves an operation-unspecified write as a protobuf oneof with no selected member, so the shared backend reports that row as `INVALID_ARGUMENT` while valid prefix and suffix writes still execute. A matching tonic regression verifies the same per-row status ordering and both valid documents' post-state. The REST regression suite also verifies malformed non-array input through the real `/v1/` document readback route, avoiding a route-level 404 that could mask an unintended mutation. These tests confirm local REST/gRPC parity for this bounded malformed-row condition; they do not infer the production rejection unit for every invalid BatchWrite shape.

The complete `fireemu-adapter-grpc` package regression on this head passed 368 tests with one pre-existing skip. This is a local adapter regression result; it does not provide production invalid-BatchWrite rejection-unit evidence.

## Parent-closure checkpoint (27bdc946)

The current feature head `27bdc946` preserves the established Auth batch-import password policy after an exploratory trust-path was rejected by the existing boundary regression. The exploratory `434ee010` and its reverting `27bdc946` commits remain in history; no production evidence or previous artifact was rewritten. On the final source state, `CARGO_TARGET_DIR=target/auth-package-final cargo nextest run -p fireemu-adapter-http --profile pr --no-fail-fast` passed all 361 tests with no skips.

The FS-DATA-WRITE BatchWrite transport condition is locally reduced: REST and gRPC preserve per-write status ordering around an operation-unspecified write, valid prefix/suffix writes remain visible, and malformed REST readback uses the `/v1/` document route. Focused REST/local tests passed, the full `fireemu-adapter-grpc` package passed 368 tests with one pre-existing skip, and the immutable first46/second45/G0 observations remain unchanged. This is local and saved-reference evidence; production stream/transaction precedence and remaining limits still require representative production observations.

`COMPAT_VERIFIED` remains `0 / 14`. The nearest closure candidates are `FS-DATA-WRITE` and `AUTH-ACCOUNT`, each still requiring declared production conditions. No new production operation was performed in this checkpoint. The next production candidate is the bounded Firestore document-size boundary already represented by the local conformance fixture; it remains an owner-authorized campaign input rather than an executed observation.

## Current local repair checkpoint (186bb2c3 / 4d3b29e8)

The Rules query-proof repair now preserves exact equality and finite `in` constraints when they are combined with a same-field range, retains range-plus-`not-in` exclusion proofs, and fails closed when an exclusion membership expression contains an undetermined value. The real Rules integration regression covers equality, membership, negated membership, non-excluded denial and unknown right-hand values. The independent Rules review approved the integrated patch with no Must Fix or Should Fix findings.

The Auth saved-reference replay runner now treats a complete `failed` corpus report as a collected semantic result, so it records the corpus in `run-manifest.json` and allows the comparison stage to classify the mismatch. Replay output is created with exclusive file creation and rejects existing files, symlinks and hardlinks without changing receipts, local reports or manifests. Self-contained real-fixture regressions cover failed-status validation, manifest status tampering and input-byte preservation. The independent review approved the implementation; its earlier test-adequacy suggestion was addressed in the follow-up test commit.

Validation on the integrated source: `fireemu-core-rules` nextest passed 140/140, the `fireemu-adapter-grpc` package passed 369/369 with one documented skip, the `fireemu-adapter-grpc` Rules integration passed 32/32, the Auth HTTP adapter passed 361/361, and the Auth saved-reference replay suite passed 20 with 3 documented skips. Workspace formatting, Rules Clippy and adapter Rules Clippy passed. These are local and saved-reference checks only; no production operation, Cloud read, receipt rewrite or nonce reuse occurred.

`COMPAT_VERIFIED` remains `0 / 14`. `FS-DATA-WRITE` is still the nearest closure candidate, with production stream/transaction precedence and bounded request/operation-limit observations outstanding. `AUTH-ACCOUNT` still requires bounded production provider-lifecycle, alternate-hash, configured-policy and unobserved state/value comparisons. The next production campaign remains owner-bound and is not inferred from these local repairs.

## Current FS-DATA-WRITE closure checkpoint (`4b7fdb0f` / `ed879340`)

Firestore document-size validation now preserves the offending property path in the `INVALID_ARGUMENT` message. An oversized `blob` field is reported as `The value of property "blob" is longer than 1048487 bytes.`, matching the saved production matrix while preserving atomic refusal and post-state checks. The real REST regression and core Firestore suite passed (58 REST tests; 214 core tests with 2 documented skips), Clippy and formatting passed, and the three source-bound Quint evidence files were regenerated by the repository generator. This is local and saved-reference evidence only; no production operation or Cloud read occurred. `FS-DATA-WRITE` remains `IMPLEMENTING` because production stream/transaction precedence and bounded request/operation-limit observations are still required.

## Current FS-DATA-WRITE error-path repair (`681a5872` / `8043607e`)

Nested oversized-value diagnostics now retain field-name segments structurally and render them with canonical escaping. The local REST regression covers dotted and quote-containing map keys inside an array, checks the exact `INVALID_ARGUMENT` message and verifies that refused documents are absent. The aggregate document-size control uses individually valid fields so its existing `FS-LIMIT-DOCUMENT-BYTES` contract remains covered. Related `TransactionConditionalLock`, `AtomicCommitOutbox` and `FirestoreListenRefresh` evidence was regenerated by the repository generator. Post-repair verification passed: REST 59/59, core Firestore 214/214 with 2 documented skips, Quint 128/128 with 48 skipped, and full workspace 2391/2391 with 81 skipped; Clippy, rustfmt and diff checks passed. Independent review approved after the evidence refresh. This remains local evidence only; production stream/transaction precedence and bounded request/operation-limit observations are still open, so `FS-DATA-WRITE` remains `IMPLEMENTING`.

## Current-head saved-reference replay (`9bb76590`)

The current feature artifact was run through the existing first46 adapter and compared with the immutable `ab7bd698`/`bc38f392` production observations. The separate candidate [`9bb76590-first46-saved-recomparison.json`](../../spec/compatibility/broad-runs/9bb76590-first46-saved-recomparison.json) records 46/46 matching rows, valid source and observer bindings, complete recording, cleanup and closed listeners. Its file SHA-256 is `bc12851d1dc680f1dba081d09e93cc791283cf935682c6786296dc8fae135500`; the local observer, manifest and comparison contract digests remain distinct from the historical receipt. The original 35-match/11-mismatch result and all earlier replay artifacts are unchanged. This is saved-production-reference evidence only and does not close the remaining production stream, transaction or limit conditions.

The current Auth OOB authorization regression was also rerun through the real local handler: the three `oob_authorization` tests passed. Complete semantic refusal, ordinary delivery, token-owner selection, no-side-effect behavior and Admin generation remain local evidence; delivery-boundary production transitions are still unobserved.

The Rules query-proof repair at `66a89579` preserves parent `!=` / `not-in` constraints alongside nested child ranges by using a typed partial-map-with-exclusions value. The real gRPC Rules path now covers both filter orders, parent and nested rule predicates, positive and negative range bounds, and the required composite index. The regression and core Rules suite pass; this is local Rules evidence only and does not promote `FS-RULES-INDEXES` without production observation.

The follow-up repair at `33c78650` keeps a parent exclusion when a nested exclusion is added (including parent-first `FieldPath` ordering) and accounts for excluded values inside `RangeExcluding` when proving that a nested map differs from a concrete candidate. Source-level abstraction and evaluator regressions cover both cases, while the existing gRPC query-proof regression continues to exercise the real authorization path. Quint evidence for the affected Rules models was regenerated with the repository generator. These are local proofs only; saved production receipts and their original comparisons remain unchanged.

## Current Rules query-proof repair (`516e0c9a`)

The query authorization proof no longer treats nested integer and floating-point representations as definitely different when Firestore query equality considers them equivalent. The proof-specific equality recurses through arrays, maps and sets while concrete Rules equality remains unchanged. Core Rules regressions cover nested list/map representations and a genuine difference; a real gRPC regression confirms that an owner query can read the stored document while a user direct read and user query remain denied by the same Rules condition. This is local evidence only and does not add production Rules/query parity evidence; the original observations and comparison contracts remain immutable.

## Current local parent-closure checks (`406f980c`)

The Auth import/export corpus now has a direct regression for an opaque, unsupported `scrypt$...` password hash. Import refuses the record before startup and explains that the hash is not in a reversible form; this preserves the existing credential-export boundary without classifying the account as email-link. The focused test and the complete `import_export` test group passed. This is local Auth evidence only and does not establish support for additional production hash formats.

The declared WebChannel boundary was revalidated from the isolated fixed checkout. The adapter integration suite passed 13 tests, the filtered runtime WebChannel unit suite passed 27 selected tests, and the five declared mutations covering expiry, byte admission/refund, replay-copy budget ownership, keepalive no-op handling and serialization commit races were killed by existing tests. Contiguous AID allocation and exact response-loss replay are covered. This remains local harness/runtime evidence; production WebChannel protocol and byte-limit equivalence are unobserved.

`COMPAT_VERIFIED` remains `0 / 14`. `FS-DATA-WRITE` remains the nearest parent candidate and still requires production stream/transaction precedence and bounded request/operation-limit observations. `AUTH-ACCOUNT` retains production provider-lifecycle, configured-policy and unobserved state/value conditions. The next bounded production proposal is a two-document Firestore size-boundary campaign (10 wire operations including recovery), but its owner identity, permission reference, execution window, fresh nonce, current configuration/pricing acceptance, recovery owner and frozen collector/manifest/comparator bindings remain unset. No production operation, Cloud read, saved receipt rewrite or nonce reuse occurred in this checkpoint.
## Current Rules query-proof transitive closure (`89ff069a`)

The nested numeric query-proof repair now carries proof provenance through declaration-scoped user functions, `let` aliases, list and set membership helpers, and set transformations whose cardinality could otherwise be inferred from one numeric representation. Query authorization remains conservative when integer and floating-point representations are equivalent under Firestore query equality; concrete Rules evaluation is unchanged. Core Rules regressions and the real gRPC authorization regression cover direct equality, negation, membership, cardinality and transformed values, with an owner stream read followed by denied user direct and query reads.

The three source-bound Quint evidence files for `RegexAuthorization`, `RegexEvaluationCache` and `RulesetActivation` were regenerated by the repository mutation generator and each passes `verify-evidence`. The integrated workspace nextest passed 2,404 tests with 81 documented skips, workspace Clippy passed with `-D warnings`, and rustfmt and diff checks passed. Independent strict review approved the final runtime and regression changes with no blocker or high-severity finding.

This checkpoint is local evaluator and saved-evidence maintenance only. No production operation, Cloud read, receipt rewrite or nonce reuse occurred. `COMPAT_VERIFIED` remains `0 / 14`; `FS-RULES` and `FS-QUERY-INDEX` still require their declared production observations, and `FS-DATA-WRITE` remains the nearest closure candidate with stream, transaction-precedence and bounded limit conditions outstanding. The next document-size-boundary campaign remains `BLOCKED_OWNER` until its owner, permission reference, execution window, fresh nonce, current configuration and pricing acceptance, recovery owner, and fixed collector/manifest/comparator bindings are supplied.

## Rules query-proof numeric access closure (`1a944cf1`)

The query-proof evaluator now fails closed before representation-sensitive numeric access. Query-derived numeric indices and slice bounds return `Unknown`, and query-derived numeric path bindings are rejected before path interpolation, including values propagated through function and `let` aliases. Concrete Rules evaluation and constant index/path behavior are unchanged. Core regressions pair integer query representatives with floating-point concrete values and retain valid integer/string controls. The real gRPC regression verifies an owner query can return the stored floating-point document while the user direct read and user query are denied, including stream termination.

The affected `RegexAuthorization`, `RegexEvaluationCache` and `RulesetActivation` Quint evidence files were regenerated with the repository mutation generator and pass `verify-evidence`. The full workspace nextest passed 2,410 tests with 81 documented skips; the `fireemu-core-rules` package passed 152 tests and the `fireemu-adapter-grpc` package passed 375 tests with one documented skip. Workspace Clippy with `-D warnings`, rustfmt and diff checks passed. Independent Rules review approved the final code and evidence, and the Security Specialist reported no remaining Must Fix or Should Fix findings.

This checkpoint is local query-proof and saved-evidence maintenance only. No production operation or Cloud read occurred, and historical receipts, comparisons and nonce bindings remain immutable. `COMPAT_VERIFIED` remains `0 / 14`; `FS-RULES`, `FS-QUERY-INDEX` and `FS-DATA-WRITE` still require their declared production observations.

## Current Firestore transaction closure checkpoint (`ff2d4b0b`)

The feature branch now records two bounded local REST transaction conditions: replay of a consumed retry token is rejected while a fresh retry transaction can read and commit, and a transaction idle for 61 logical seconds returns `ABORTED` without mutating the document before its lock is released. The retry and expiry regressions assert post-state and cleanup through the real REST adapter. Full REST integration passed 61 tests, with package check, formatting and diff checks passing; independent strict review approved the final test coverage.

These results are local Firestore evidence only. Production conflict precedence, expiry/error wording, retention and SDK retry behavior remain unobserved, so `FS-DATA-WRITE` and `FS-TRANSACTION` remain below `COMPAT_VERIFIED`. No production operation, Cloud read or historical receipt rewrite occurred. The next document-size-boundary campaign remains owner-bound until its concrete credential, permission, window, nonce, configuration/pricing and recovery bindings are available.

## Current-head Auth saved-reference replay (`9d3f085a`)

The four immutable Auth production receipts were replayed through source `577eaad53c7e914f141405a715b246248dcd6ae8` using one local artifact. `auth-basic-v2`, `auth-display-name`, `auth-password` and `auth-profile` each matched all 12 rows, for 48/48 `MATCH`. The candidate result is [`577eaad5-auth-saved-reference-replay.json`](../../spec/compatibility/broad-runs/577eaad5-auth-saved-reference-replay.json), bound to artifact `95ffc5825108db895c0e298d10865c288ffe423038eee8d6cef74f0a6ba774e6`, run-manifest digest `5cb9484c8b16f5dac23e22e26e07ff84459ecea77b2bd8b8dae78d67b595cdbb` and comparison digest `ef054f4ee9ef0daea008042466b6081dc693be3cfb023e3b649cbc9a1ba7aaff`.

This is saved-production-reference evidence only. It does not promote `AUTH-ACCOUNT`; provider lifecycle, alternate hash formats, configured policies and the remaining credential, MFA, OOB, tenant, blocking and SDK conditions still require their declared evidence. No production operation, Cloud read or historical receipt rewrite occurred.

## Current-head Rules provenance sink closure (`8f456536`)

The Rules query-proof evaluator now keeps declaration-scope isolation for static provenance lookups and preserves numeric provenance for known List/Set and partial-list containers before member and index access. Regression coverage includes caller `resource` shadowing of the global resource in both dot and bracket access, and list-derived numeric values used in comparison, membership and map indexing. The prior query-derived arithmetic, direct comparison, type-test, path and function/let alias guards remain in place.

The affected RegexAuthorization, RegexEvaluationCache and RulesetActivation Quint evidence was regenerated with the repository generator. Post-refresh `fireemu-core-rules` nextest passed 158/158; `traceability-check`, `compat-check`, rustfmt and diff checks passed. This is local evaluator and formal-evidence maintenance only. No production operation, Cloud read, receipt rewrite or nonce reuse occurred.

`COMPAT_VERIFIED` remains `0 / 14`. `FS-RULES` and `FS-QUERY-INDEX` still require declared production comparisons, while `FS-DATA-WRITE` remains the nearest closure candidate with production stream/transaction precedence and bounded request/operation-limit observations outstanding. The next document-size-boundary campaign remains `BLOCKED_OWNER` pending owner identity, permission reference, execution window, fresh nonce, current configuration/pricing acceptance, recovery owner and fixed collector/manifest/comparator bindings.

Updated feature head: `8f456536cf6cec322bec3deb6e72eb1975f75d6e`.

## Auth selector authorization closure (`05c8b263`)

The bounded AUTH-D1 local condition is now covered through the real Identity Toolkit handler. End-user `accounts:update` requests that include `localId` together with privileged fields (`customAttributes`, `emailVerified`, `disableUser`, MFA and provider-link fields) are rejected without mutation or token issuance, while the owner credential path remains successful. The integrated `identity_toolkit` suite passed 85/85 and the adapter Clippy check passed.

This closes only the local selector-authorization condition. Provider lifecycle, configured policy, credential/hash classes, tenant/blocking interactions and their required production comparisons remain open; `AUTH-ACCOUNT` is not promoted. Historical receipts and saved Auth comparisons are unchanged.

## Current checkpoint: full workspace and Auth selector closure (2026-09-16)

At feature head `4941e6364b549a471714e21b86494b6c848cdbb5`, the full workspace verification passed 2,420 tests with 81 documented skips across 175 test binaries. The Auth Identity Toolkit suite passed 85/85, the Rules core suite passed 158/158, the bounded Firestore document-size/nesting control passed 1/1, and the relevant Clippy, format, diff, traceability and capability checks passed. The three source-bound Quint evidence files were regenerated by the official generator in `8f456536`; all 14 model gates completed.

The Auth-D1 selector authorization condition now has real handler regressions: end-user `accounts:update` requests that select an account by `localId` while carrying privileged fields are rejected without mutation or token issuance, while the owner credential path remains successful. This is local runtime evidence only. It does not promote `AUTH-ACCOUNT`, and historical production receipts, saved comparisons and nonce bindings remain unchanged.

The parent state is still `COMPAT_VERIFIED 0 / 14`, with no `REVIEW_READY` or `ORACLE_COMPARED` parent and seven `WAITING_ORACLE` parents. `FS-DATA-WRITE` remains the nearest closure candidate; its production stream/transaction precedence and bounded request/operation-limit observations are still required. The next campaign remains `BLOCKED_OWNER` pending concrete owner, permission, window, fresh nonce, configuration/pricing and recovery inputs. No new production operation or Cloud read was performed in this checkpoint.

The current-head Explain local shadow is also recorded as `spec/compatibility/broad-runs/3ab6dc85-explain-local-shadow.json`, bound to source `3ab6dc8581ea0a6c823caf3b80fb231511521738`, observer `9e5b2f61`, manifest `8f9ea90e`, comparison contract `b0f1a1d0` and artifact `b4ab486c`. It records 12 observed rows, six recovery rows, complete recording/state/cleanup and `productionExecuted: false`; it is not production evidence.

The current feature head is `cfa8e796a0a39c571b971bbd5b0289d06cdce7c5`; this follow-up commit only binds the sanitized shadow projection and leaves the preceding runtime artifact source binding explicit.

## Tenant policy and vector index closure checkpoint (2026-09-16)

The Auth runtime now applies tenant `disableAuth`, password-signup and email-link policy checks to end-user lookup, OOB generation and password reset before dispatch. A focused real-handler regression verifies that a denied reset does not consume an existing OOB code, that the same code succeeds after policy restoration, that disabled tenants reject OOB generation, and that tenant admin batchGet remains available. These are local Auth observations; production policy propagation and delivery behavior remain unobserved.

Firestore field override parsing now validates `vectorConfig.dimension` and `flat`, and Standard/Production `findNearest` planning consumes a matching single-field vector index when the query has only the vector field with exact scope and dimension. Additional filters continue to require a matching composite vector index. Parser-to-planner and planner regressions cover the positive and filtered cases. This is local Firestore evidence; production vector-index behavior remains unobserved.

The integrated feature head passed the Auth `auth_flows` suite (114/114), Firestore index tests (33/33), and the `fireemu` package (332 passed, 28 documented skips). Independent strict review found no Must Fix finding in these changes. `COMPAT_VERIFIED` remains `0 / 14`; `FS-DATA-WRITE` is still the nearest parent and requires its owner-bound production campaign for stream/transaction precedence and bounded limits. No production operation or Cloud read occurred, and historical receipts, comparisons, artifacts and nonce bindings remain immutable.

## Rules numeric success/error provenance closure (2026-09-16)

The query-proof evaluator now distinguishes raw numeric provenance from canonical numeric normalization when evaluating representation-sensitive success or error paths. Integer-only timestamp and duration builtins remain fail-closed for raw query representatives, and unary negation at the signed integer boundary remains fail-closed when the stored equivalent may overflow. Explicit `int()` or `float()` normalization remains provable through direct calls, declaration-scoped helper returns and `let` aliases; concrete Rules evaluation still uses checked integer arithmetic and is unchanged. Core Rules regressions cover raw integer/double controls and helper/let normalization for both `i64::MIN` and `-2^63` double values.

The integrated focused checks passed: `fireemu-core-rules` eval 66/66, `fireemu-adapter-grpc` Rules 37/37, REST 61/61, workspace Clippy with `-D warnings`, rustfmt and diff checks. The final workspace nextest passed 2,427 tests with 81 documented skips across 175 binaries. The repository Quint generator refreshed `RegexAuthorization` and `RegexEvaluationCache`; the checked-in evidence contract and all 14 model registrations pass. This is local evaluator and formal evidence only; it does not create new production compatibility evidence.

`COMPAT_VERIFIED` remains `0 / 14`. `FS-DATA-WRITE` is still the nearest parent and requires bounded production stream/transaction precedence and request/operation-limit observations. The current production lane remains `BLOCKED_OWNER` because the fixed API-key digest differs from the saved campaign binding and the next collector/manifest/owner envelope is not frozen. No new production operation, Cloud read, receipt rewrite, nonce reuse or configuration change occurred; historical G0, 45/46-line receipts and saved comparisons remain immutable. Runtime repair commit: `e8ee3fe5`; final verification source before this documentation checkpoint: `23545758`.

## Historical candidate (2026-09-16, superseded on 2026-09-17)

This section records the 2026-09-16 candidate. Earlier sections are historical checkpoints and retain their original claims; the 2026-09-17 section below is the current-head evidence.

- Base SHA: `d987d5ddcc1374cee425f08d6d819157fc2ab5`.
- Current head: `43b5778c65b93c3468a80b1bdd3b0b850467c443` (`fix(compat): harden protobuf denominator integrity`), with the preceding Auth replay integrity commit `180b39946de74bc68a1edaf58d7d0268d9ab0889`.
- The v7 Auth/ListCollectionIds candidate remains a local owned-artifact shadow only. Its result SHA-256 is `fea698df09b58d66eb5f67f885c487d0154aa768983a3ef6465352a314e761f7`, its package metadata binds shadow commit `34bcbdfbe1ca79305bd5cc5b591380b21f99458e`, and `productionExecuted=false`. The immutable v6 result remains `107ca4e8b20acc9e414f90881cbe99e24808c3e255fa8c973627a99c1f5fba84` with package history unchanged.
- The Firestore v1 protobuf denominator v2 is checked in at `spec/compatibility/denominators/firestore-v1-grpc-2026-09-16.v2.json`. It records 502 structural surfaces, 468 Standard/Native targets, 34 explicit Enterprise exclusions and bounded SDK debt. Generator/source/parent digests are recomputed and fail closed; output creation is exclusive; CI runs the validator and focused tests. These are structural inventory facts, not compatibility evidence.
- Auth saved-reference replay now preserves complete semantic failures through `run-manifest.json` and rejects output collisions against production receipts, local reports and manifests, including symlink and hardlink paths, without changing input bytes. Explain historical recompare binds both receipt execution commits to the selected checkout HEAD. These are local integrity repairs; historical observations remain immutable.
- Verification on this candidate: workspace `cargo nextest run --workspace --profile pr` passed 2,435 tests with 81 documented skips; workspace Clippy with `-D warnings`, rustfmt, diff checks, protobuf denominator checks (11 passed), acquisition/inventory checks (211 passed, 2 skipped), Auth replay/Explain/Auth-list focused checks (74 passed, 3 skipped), SDK pin checks (3 passed) and listener runtime checks (1 passed). The formal Quint refresh at `1ec065bd0` passed all 14 model/connect/mutation/evidence gates and traceability; no Quint-bound source changed after that refresh.
- Parent promotion remains `COMPAT_VERIFIED 0 / 14`, `REVIEW_READY 0`, `ORACLE_COMPARED 0`, with seven parents `IMPLEMENTING` and seven `WAITING_ORACLE`. The nearest closure remains `FS-DATA-WRITE`, requiring production Write-stream/transaction precedence and bounded request/operation-limit observations. No parent is promoted by local, formal, SDK or saved-reference evidence alone.
- Current production campaign is none approved or running. O1-O6 preparation artifacts remain preparation-only with `productionExecuted=false`; Gatekeeper is `BLOCKED_OWNER` pending owner identity, permission/window, accepted project/database/tenant and configuration, fresh nonce policy, recovery owner/reserve, and complete cost/request/time/resource envelope. No new production operation, Cloud read, credential use, configuration change or nonce reuse occurred in this cycle.
- Historical G0, 45/46-line observations, original mismatches, saved references, approvals, collector identities and comparison contracts remain unchanged. The current candidate does not claim full Identity Platform or Firestore compatibility, Firebase-wide compatibility, or production parity for unobserved surfaces.
- Security status is pending the fresh current-head review; earlier Must Fix findings for incomplete Auth-list cleanup status, historical Explain commit binding and legacy gate accounting are resolved in the integrated history. Any new Must Fix blocks further promotion.

## Canonical current candidate (2026-09-17, feature head `c384f55169eb328eaf8bc87af1c8ee609295475d`)

The Auth settings integration is implemented on the feature branch. The current candidate keeps the existing password policy behavior and adds strict `fireemu.json` parsing, project/tenant namespace overrides, atomic project and tenant configuration updates, client permissions, email privacy projection, blocking-function selection and token forwarding bounds, and deterministic sign-up quota simulation. The duplicate-email setting now retains its declaration state during export/import: an omitted `signIn.allowDuplicateEmails` member preserves the destination value, while an explicit `false` is applied and exported as an explicit value.

| Setting | Config/API configurable | Applied in runtime | Production compared |
| --- | --- | --- | --- |
| Password policy | Yes: `auth.passwordPolicy`, overrides, project/tenant GET/PATCH | Yes: registration, password changes, reset and password sign-in notification/force paths | None for this candidate |
| Account linking / duplicate email | Yes: `auth.signIn.allowDuplicateEmails` and project config API | Yes: provider/email ownership and lookup behavior | None |
| Client permissions | Yes: `auth.client.permissions` and namespace overrides | Yes: end-user signup/deletion gates with Admin bypass | None |
| Email privacy | Yes: `auth.improvedEmailPrivacy` and `emailPrivacyConfig` projection | Yes: sign-in, lookup and reset privacy paths | None |
| Blocking Functions | Yes: explicit trigger and per-token forwarding configuration | Yes: owned-function selection and global/project/function/credential forwarding checks | None |
| Sign-up quota simulation | Yes: temporary quota and `quotaSimulation` local configuration | Yes: project/IP fixed-window reservation, commit and release | None; official counting and abuse controls remain unobserved |

Local verification on this candidate passed `fireemu-core-auth` (157), `fireemu-core-export` (92), `fireemu-adapter-http` (377), `fireemu` (350 after the import-duration and assertion fixes), relevant package Clippy with `-D warnings`, rustfmt, the Auth settings SDK/Oracle harness (2), and the password-policy SDK harness (2). These are local or SDK-harness observations; they do not establish production parity. The current artifact was not sent to Google/Firebase and no production credential or configuration was used.

The Auth settings SDK/Oracle campaign remains `WAITING_ORACLE` with `productionExecuted=false`. O1-O6 preparation is complete for the bounded local shadow, but O7 Gatekeeper remains `BLOCKED_OWNER` until owner identity, permission reference, project/database/tenant, execution window, fresh nonce, accepted configuration, recovery owner/reserve, and request/resource/time/cost/concurrency bounds are frozen. No campaign is approved or running; no production MATCH, SEMANTIC_MISMATCH or INDETERMINATE result was added in this cycle.

Parent closure is unchanged: `COMPAT_VERIFIED 0 / 14`, `REVIEW_READY 0`, `ORACLE_COMPARED 0`. The nearest parent remains `FS-DATA-WRITE`, with production stream/transaction precedence and bounded request/operation-limit observations outstanding. Auth settings local work reduces implementation conditions but promotes no Auth parent without its declared production evidence, mismatch repair, final-artifact regression and independent review.

The current candidate has no new owner decision beyond the blocked campaign envelope. Historical production receipts, saved-reference comparisons, approvals, collector and comparator bindings, nonces and prior artifacts remain immutable. The fresh security review for this fixed source is pending; any Must Fix finding blocks promotion.

## Current Auth settings integration checkpoint (2026-09-17, source `1ff74a679`)

Auth settings are now integrated through the canonical configuration parser, namespace registry, management routes, Auth runtime and the pinned SDK shadow harness. The implementation includes password policy, duplicate-email/account linking, end-user client permissions, email enumeration privacy, owned blocking-function selection and credential forwarding bounds, and deterministic sign-up quota simulation. Tenant creation and project/tenant updates publish metadata, policy and inherited configuration atomically; omitted fields preserve the current namespace values, conflicting tenant selectors are rejected, and an unknown API key fails closed once additional projects are registered while retaining the single-default emulator compatibility path. Rules query-proof provenance also traverses partial containers conservatively; this is a separate local repair and does not change the Auth production evidence boundary.

| Setting | Config/API configurable | Applied in runtime | Production compared |
| --- | --- | --- | --- |
| Password policy | Yes: `auth.passwordPolicy`, overrides and project/tenant GET/PATCH | Yes: registration, credential changes, reset and password sign-in notification/force paths | None for this candidate; Admin operation semantics and Unicode/backend limits remain unobserved |
| Account linking / duplicate email | Yes: `auth.signIn.allowDuplicateEmails` and project config API | Yes: provider/email ownership, linking and lookup | None |
| Client permissions | Yes: `auth.client.permissions` and namespace overrides | Yes: end-user signup/deletion gates with Admin bypass | None |
| Email privacy | Yes: `auth.improvedEmailPrivacy` and `emailPrivacyConfig` projection | Yes: sign-in, lookup, reset and provider disclosure paths | None |
| Blocking Functions | Yes: owned trigger selection and per-token forwarding configuration | Yes: beforeCreate/beforeSignIn selection and global/project/function/credential bounds | None |
| Sign-up quota simulation | Yes: temporary quota and `quotaSimulation` local configuration | Yes: project/IP fixed-window reservation, commit and release | None; official counting and abuse controls remain unobserved |

Local verification on this source passed `fireemu-core-auth` 163/163, `fireemu-adapter-http` 383/383, `fireemu` 350/350 with 28 documented skips, `fireemu-core-export` 92/92 and `fireemu-core-rules` 164/164. Clippy passed for `fireemu`, `fireemu-adapter-http`, `fireemu-core-auth`, `fireemu-core-export` and the Rules library with `-D warnings`; rustfmt, diff checks, inventory checks, Quint evidence refresh, the Quint evidence contract and traceability tests passed. The SDK smoke checks passed four local harness tests. These are local, formal or SDK-harness observations and do not establish production parity.

The Auth settings campaign remains `WAITING_ORACLE` with `productionExecuted=false`; O7 Gatekeeper is `BLOCKED_OWNER` because owner identity, explicit permission reference, target project/database/tenant, execution window, fresh nonce, accepted configuration, recovery owner/reserve and the request/resource/time/cost/concurrency envelope are not frozen. No production request, Cloud read, credential use, configuration mutation, receipt rewrite or nonce reuse occurred. Historical production receipts and saved comparisons remain immutable.

Parent closure remains `COMPAT_VERIFIED 0 / 14`, `REVIEW_READY 0`, `ORACLE_COMPARED 0`; the nearest parent is `FS-DATA-WRITE`, whose stream/transaction precedence and bounded request/operation-limit observations remain open. The Rules partial-container repair and Auth settings integration reduce local implementation conditions but do not promote a parent. Unresolved security-review conditions are compatibility evidence for Admin password operations and lifecycle documentation; no Must Fix finding is currently reported. Additional local breadth that does not reduce a declared closure condition remains Backlog.

## Auth settings security and selector closure checkpoint (2026-09-17, source `50bb3a969`)

The Auth settings integration remains on the feature branch and now rejects duplicate and malformed query selectors before namespace selection. `tenantId`, `key`, and `apiKey` duplicates (including the `key`/`apiKey` alias family), missing `=`, empty values, and invalid percent escapes fail with `INVALID_ARGUMENT`; valid percent-encoded selector names and unrelated query parameters retain their existing behavior. No store selection, Auth operation, account, token, OOB code, metadata or policy mutation occurs before these failures. Effective tenant metadata continues to be derived from the atomically published runtime configuration, and the Quint evidence bound to the changed Auth source is refreshed after integration.

| Setting | Config/API configurable | Applied in runtime | Production compared |
| --- | --- | --- | --- |
| Password policy | Yes: `auth.passwordPolicy`, namespace overrides and project/tenant GET/PATCH | Yes: registration, credential changes, reset and password sign-in notification/force paths | None; Admin create/update, Unicode/backend limits and projection details remain unobserved |
| Account linking / duplicate email | Yes: `auth.signIn.allowDuplicateEmails` and project config API | Yes: provider/email ownership, linking and lookup | None |
| Client permissions | Yes: `auth.client.permissions` and namespace overrides | Yes: end-user signup/deletion gates with Admin bypass | None |
| Email privacy | Yes: `auth.improvedEmailPrivacy` and `emailPrivacyConfig` projection | Yes: sign-in, lookup, reset and provider disclosure paths | None |
| Blocking Functions | Yes: owned trigger selection and per-token forwarding configuration | Yes: beforeCreate/beforeSignIn selection and global/project/function/credential bounds | None |
| Sign-up quota simulation | Yes: temporary quota and `quotaSimulation` local configuration | Yes: project/IP fixed-window reservation, commit and release | None; official counting and abuse controls remain unobserved |

Local verification after the selector fixes passed `fireemu-adapter-http` 388/388, the full workspace nextest, workspace Clippy with `-D warnings`, rustfmt, diff checks, SDK smoke checks, offline compatibility-inventory checks, Quint evidence refresh/evidence contract and traceability. The security re-review at the integrated selector source reported no Must Fix findings and approved with conditions. Encoded alias, malformed selector, duplicate selector and unrelated-query regressions are now covered by `d2efaf791` and `cb4d8407b`; the remaining condition is Admin password-policy behavior, which remains a production-unobserved compatibility condition rather than a local security defect.

The Auth settings campaign is still `WAITING_ORACLE` with `productionExecuted=false`; O7 Gatekeeper is `BLOCKED_OWNER` because owner identity, explicit permission reference, target project/database/tenant, execution window, fresh nonce, accepted configuration, recovery owner/reserve and request/resource/time/cost/concurrency bounds are not frozen. No production request, Cloud read, credential use, configuration mutation, receipt rewrite or nonce reuse occurred. Historical production receipts and saved comparisons remain immutable.

Parent closure remains `COMPAT_VERIFIED 0 / 14`, `REVIEW_READY 0`, `ORACLE_COMPARED 0`; `FS-DATA-WRITE` is still the nearest closure candidate, with production stream/transaction precedence and bounded request/operation-limit observations outstanding. These local, formal and SDK-harness results do not promote an Auth parent or the password-policy child goal to production compatibility.

## Current Auth saved-reference replay (`515f0cf4`)

The four immutable Auth production corpora were replayed through the current feature artifact at `515f0cf4afa1b12e887b84552aa20851ab3142ae`. The owned runner built artifact `5a337e6ad0a74ec0e769f91bcff9195fde91f55a3f9602c3635406e76204f4ae`; every corpus completed with cleanup and closed listeners, and the saved-reference comparison reports 48/48 `MATCH` rows under the existing contracts. The checked-in result is [`515f0cf4-auth-saved-reference-replay.json`](../../spec/compatibility/broad-runs/515f0cf4-auth-saved-reference-replay.json), with comparison digest `b7c3e6498472fc3531c5ea83898737902ddd45e17b4a2251cc88c7a4a0545529` and run-manifest digest `d44371687a080043235a5dc868bec929de37bd2c640b7a3ec0c61e9128c94eba`. This is saved-production-reference evidence only; it does not add a production observation or promote an Auth parent.

Parent closure remains `COMPAT_VERIFIED 0 / 14`, `REVIEW_READY 0`, and `ORACLE_COMPARED 0`. `FS-DATA-WRITE` remains the nearest closure candidate. Its next finite campaign is still the exclusive Write-stream/transaction-precedence and bounded-limit preparation from O3; no owner-approved manifest or production operation is available.

## Fixed-artifact limits repair evaluation (`8b33aac4d`)

The observed byte/depth diagnostic repair is resolved by the separately versioned, acquisition-bound saved-reference comparison. Its semantic kernel intentionally delegates acquisition validation to the outer unchanged v1 validator; that separation is not an acquisition failure. The historical v1 resource-identity differences remain preserved, while the reviewed v2 comparator normalizes only the current request resource slot and still detects all eight pre-fix runtime wording mismatches. No new production request was made.

The Evidence evaluation leaves `COMPAT_VERIFIED` at `0 / 14`: six parents are `IMPLEMENTING`, eight are `WAITING_ORACLE`, and none is `REPAIRING`, `REVIEW_READY`, or parent-level `ORACLE_COMPARED`. `FS-DATA-WRITE` is next. Stream/transaction precedence and unresolved declared limits still need finite production comparisons; final-artifact verification and independent review remain parent gates. Historical scheduling backlog is not a scope exclusion. The bounded byte/depth repair condition is reduced by one; no parent is promoted.

## Current saved-reference and regression checkpoint (`cce4a4f9b` / `e526aa908`)

The Auth saved-reference replay at `cce4a4f9b7369938c89bd32a5106e8d3cab59f83` reports 48/48 MATCH on retained artifact `be2771b9f2093cced55e8158d8d5a72ed35e6ac5e32edddb068daa45511e12ae`. The [derived result](../../spec/compatibility/broad-runs/cce4a4f9b-auth-saved-reference-replay.json) and [binding](../../spec/compatibility/broad-runs/cce4a4f9b-auth-saved-reference-replay-binding.json) preserve comparison-file, manifest, collector/comparator and immutable reference hashes. Independent evidence review verified these bindings and cleanup. This updates the finite saved-reference replay subcondition of `AUTH-ACCOUNT-LIFECYCLE-1`; `AUTH-ACCOUNT` remains `IMPLEMENTING`. This supersedes neither historical receipts nor their original preparation contracts. Configuration projections remain separate evidence.

At fixed source `e526aa908`, `cargo nextest run --workspace --profile pr` completed with 2,623 passed, six slow and 81 skipped. The skips retain the [existing classification](ignored-test-audit-20260917.md); they are not successful executions. The broad local checkpoint at `cce4a4f9b` completed with 986 passed and 11 skipped. Remote CI on that source exposed 17 Node-runtime permission refusals; the reviewed test-only fixture at `c281337b5` fixes environment ownership while preserving the production runtime guard. Remote validation of the newer source is pending.

## Current Explain saved-reference checkpoint

The [v5 Explain replay](query-explain-current-saved-reference-v5.md) independently validates the original production acquisition and a new local shadow from source `cce4a4f9b`. Its retained artifact is `26a0e9a2623073616dd0ce6e8554946f697ff5e919b34c98ef94e6eb0f38c8c6`; this is a separate build from the Auth replay artifact. All twelve observations match: six Explain cases, four setup observations and two post-state readbacks. Cleanup and process termination are separately validated. The original mismatch and historical v1–v4 evidence remain unchanged.

Independent review approved final evaluator, source/input anchors and public summary at `b0c90686a`. Integration `0d72ad853` passed all ten actual-input v5 tests; the owner and reviewer also verified 115 inherited v2/v3 tests. This is saved-production-reference comparison, not a new production observation or a new denominator. `FS-QUERY-INDEX` is not promoted, and final-artifact and remaining declared query/index conditions remain open.

Broad CI at `e26e95b3d` subsequently reported 985 passing tests, 12 skips and one expired-reservation test failure. The worker loaded absent SDK dependencies before checking the dead lease. Independently reviewed repair `6010f06eb` checks the reservation before worker startup, preserving each later wire admission check. The coordinator/bridge suite passed 27 tests with three opt-in skips both without installed SDK dependencies and after integration. Remote CI for the repair is tracked independently.

The separately reviewed Write terminal repair at `215ae10f4` passed 118 Node tests with two live skips and an actual retained-artifact collector run with all five predicates and all three typed cleanup proofs. These results are local runtime/collector evidence, not production Write-stream evidence. The stream production entrypoint, final acquisition binding and owner-approved campaign remain unfinished. No new production operation occurred, and parent promotion remains 0/14.
