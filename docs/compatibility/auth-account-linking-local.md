# Owned local account-linking checkpoint

The owned local case completed both password sign-ups and same-provider sign-in, accepted the cross-provider same-email link, and completed distinct-provider linking and unlinking with account identity preserved and post-delete absence verified. These observations use local emulator provider fixtures and do not establish production parity. The original receipt did not record effective `allowDuplicateEmails`; the additional readback checkpoint below closes that evidence omission without claiming the full setting matrix.

The retained binary is bound to source `8b33aac4ddd09e6b945cc8701a7e757a948223ac` and SHA-256 `76f855367910ad237de6ffd491091f20bcdccdb2e5eb8ee417e80b94bf6ac397`. Harness commit `a4b41062c5b283121f17615b751e36102371bb47` is checked with source digests before and after execution. The private retained receipt SHA-256 is `19a0e4978d077df91bbdbd19489ba7307b4b5819c16617c40e6cba5f5adf0f1b`; it includes installed SDK identity, live listener ownership, control-token challenge results and verified shutdown. The installed SDKs are Firebase 12.18.0 and Admin 14.3.0. The source copies, package manifest and lockfile are retained with the receipt, independently of the removed writer worktree.

Independent review found no remaining mandatory fixes in this bounded harness. Eight Python and four Node tests passed; the integrated Auth harness plus Explain checks passed 129 tests at `00385a668`. An unavailable evidence filesystem remains a recovery error: the runner attempts a second failure record and emits a critical owner instruction if both writes fail. An external supervisor must retain stderr in that situation. No production request, parent promotion or production configuration change occurred.

## Effective configuration readback

Harness source `76adf6a81f2f61d24954660600e86d3c51c20a44` captures the exact owned loopback project configuration before and after the SDK flow. Both actual responses reported `signIn.allowDuplicateEmails: false`. Missing, non-boolean, failed, wrong-namespace or changed readbacks are refused; no default value is substituted. The private retained receipt SHA-256 is `f12daca2b0fa5fb4e1da64b3770c90e81f323a46f364e076207e01262a38e3de`. Full readback responses are retained separately, with the existing source, artifact, SDK, listener and shutdown proof. The runtime artifact is the same manifest-bound `8b33aac4d` binary above.

Independent review approved this bounded addition. Fourteen Python tests and four Node tests passed; the integrated Python suite also passed at `d3425ccdd`. The actual local run completed and its owned process and listeners stopped. This establishes which local setting the existing scenario exercised; it does not establish the `true` mode, migration semantics or production provider behavior.
