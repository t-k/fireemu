[package]
name = "fireemu-adapter-support"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Protocol-neutral HTTP adapter utilities with bounded allocation and secret-safe results"

[dependencies]
bytes = "1"
http-body = "1"
http-body-util = "0.1"
serde_json.workspace = true
subtle = "2"

[dev-dependencies]
http = "1"
tokio = { version = "1", features = ["macros", "rt"] }
tokio-stream = "0.1"

[lints]
workspace = true
