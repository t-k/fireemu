{
  "APPCHECK-CORE-1": {
    "implemented": [
      "project-scoped app registry from canonical configuration (appCheck.apps): project-number binding in both directions, one app ID per project, static debug-token digests",
      "instance-isolated RS256 issuance and verification: a dedicated 2048-bit key per daemon instance from the operating system CSPRNG, kid fireemu-app-check-<public key digest>, never the Auth session key",
      "the production-shaped claim set (iss, sub, both project audiences, iat, exp, jti, private fireemu_epoch) and the full verifier of specification section 11",
      "virtual-clock expiry: iat <= now < exp with no leeway",
      "unpredictable 128-bit project epochs; a token from another epoch or another daemon instance fails with WrongEpoch or UnknownKeyId",
      "jti from the epoch plus an atomic per-epoch counter, so concurrent exchanges never share one",
      "the canonical, transport-neutral X-Firebase-AppCheck classification (missing / present / malformed) of specification section 7.3",
      "BaselineMode and AdmissionDecision (off, unenforced, enforced) and the one transport-neutral admission decision every product adapter asks",
      "epoch rotation on session creation, reset, project deletion and snapshot restore: every token issued before the transition fails with WrongEpoch at its next verification (AC-LIFE-001)"
    ],
    "notes": [
      "reproducible identifiers need an injected test seed (AppCheckKeySource::Seed), which is a constructor seam and is not reachable from configuration or the CLI",
      "the fireemu_epoch claim is a published local divergence: local tokens are deliberately non-portable across daemon instances"
    ],
    "precision": "exact-for-local-semantics",
    "status": "implemented",
    "unimplemented": [
      "dynamic app registration (apps come only from canonical configuration and need a daemon restart)",
      "limited-use tokens (APPCHECK-REPLAY-1)"
    ]
  },
  "APPCHECK-DEBUG-EXCHANGE-1": {
    "implemented": [
      "POST /v1/projects/{project}/apps/{appId}:exchangeDebugToken and the v1beta twin; {project} may be the project ID or the project number and the API key query parameter is accepted and ignored",
      "constant-time SHA-256 digest comparison over the fixed-capacity digest set without an early exit, with equivalent dummy work for an unknown app",
      "UUIDv4 canonicalization: hexadecimal case never changes the credential",
      "one public 403 PERMISSION_DENIED \"App attestation failed.\" for an unknown project, an unknown app, a disabled app and an unknown secret",
      "limitedUse: true -> 501 UNIMPLEMENTED APP_CHECK_REPLAY_UNSUPPORTED, never a reusable session token",
      "400 INVALID_ARGUMENT for a malformed body, an unknown member, a trailing JSON value or a body above 16 KiB",
      "privileged debug-token management: POST / GET /emulator/v1/projects/{p}/apps/{a}/debugTokens and DELETE .../{tokenId}; the control token is required for every method whatever the Origin, the raw secret is returned exactly once and a list shows only the token ID, name, creation time and digest prefix",
      "Cache-Control: no-store on every App Check response"
    ],
    "notes": [
      "the constant-time comparison limits timing differences; it does not claim resistance to a local process-level side channel",
      "debug secrets are credentials: configuration stores only their SHA-256 digest and never a raw secret",
      "conformance: appcheck/exchange-and-jwks (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "precision": "boundary-conformance",
    "status": "implemented",
    "unimplemented": [
      "dynamic app creation and deletion",
      "limited-use exchange (APPCHECK-REPLAY-1)"
    ]
  },
  "APPCHECK-ENFORCE-1": {
    "implemented": [
      "one transport-neutral admission decision (fireemu_core_app_check::admission): the adapters resolve their route and target project, verify their own privileged credential, hand over the App Check field values and render the answer; none of them reimplements JWT semantics, the mode table or the observation shape",
      "off, unenforced and enforced for Firebase Authentication (end-user Identity Toolkit and Secure Token routes), Cloud Firestore (unary gRPC, REST, the Write and Listen streams and the WebChannel transport) and Cloud Storage (the Firebase dialect: metadata, list, upload, download, update, delete, and resumable uploads bound to the app that started them)",
      "stream and channel lifetime (section 13.1): a Write / Listen stream classifies its opening metadata once and is decided as soon as its first request names the database, then stays admitted for its whole life; a WebChannel classifies the opening init header block the same way, binds the channel to the admitted app and the session epoch of that admission, and closes the channel when a later envelope presents a replacement token for another app or one that does not verify. Neither token expiry nor a policy change ends an admitted stream: reconnecting is a new admission",
      "resumable upload binding (section 13.2): an enforced initiation records the admitted app and the epoch of that admission in the upload session, and every later request on that session -- continuation, status query, cancel and finalization -- must present a valid token for the same app. A refused request is refused before the command is read, so it neither advances nor deletes the upload; token expiry mid-upload therefore preserves the resumable state",
      "off is the absence of a policy, so a service configured off structurally cannot parse a header or record an observation",
      "the explicit bypass matrix of specification section 12.2, each surface requiring its own credential and never a path shape: the Firestore owner credential, Identity Toolkit Admin routes with the owner credential, the emulator inspection routes with the control token, the Storage JSON API dialect with the owner credential, and a Storage download URL whose token binds in constant time to the resolved bucket, object name and generation",
      "denial before Security Rules and before every side effect: no user, no issued or rotated credential, no consumed OOB or phone code, no MFA change, no object generation, no upload session, no Firestore mutation and no rules document-access budget consumption",
      "the error contract of section 17: PERMISSION_DENIED with the public code in fireemu-code metadata on gRPC, HTTP 403 Google JSON on Firestore REST and on Auth, and the Firebase Storage JSON error envelope with 403 on Storage; the public code is APP_CHECK_REQUIRED or APP_CHECK_INVALID and never the detailed reason",
      "x-firebase-appcheck on the Auth, Storage and Firestore REST CORS preflights and in the Storage accepted-header list, carried multi-valued so a duplicate can be refused"
    ],
    "notes": [
      "there is no appCheck.services.functions baseline mode by design: callable enforcement is per function and is owned by APPCHECK-FUNCTIONS-1, which is what production does too",
      "a project without a static appCheck.apps registration cannot use App Check at all, so an enforced service denies every request that targets it; this is the fail-closed reading of specification section 14",
      "absence of a ruleset is not a bypass: the owner credential is verified by the App Check path itself, because with Security Rules disabled every Firestore caller is otherwise treated as the owner without presenting anything",
      "the Storage JSON API dialect and the emulator inspection routes bypass Security Rules and their own guard more loosely than they bypass App Check (unauthenticated JSON API traffic, and an emulator route without an Origin); the App Check bypass requires the credential in both cases, so an enforced service cannot be defeated by rewriting a path",
      "an enforced resumable upload needs the App Check field on every continuation, not only on the initiation, and the token has to belong to the app that started the upload",
      "a WebChannel is admitted against the database its opening request names and again, inside the stream, against the database its first message names; both have to pass",
      "the exact wire messages remain boundary-conformance until the fixtures of AC-CONF-001 exist",
      "conformance: appcheck/unenforced-header-matrix, appcheck/enforced-header-matrix (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "precision": "boundary-conformance",
    "status": "implemented"
  },
  "APPCHECK-FUNCTIONS-1": {
    "implemented": [
      "the daemon proxy owns callable verification: it classifies X-Firebase-AppCheck under the canonical contract, forwards a valid token byte for byte as exactly one field, removes an invalid one before the runner can decode it, and leaves a missing one missing",
      "enforceAppCheck: true answers the callable HTTP 401 UNAUTHENTICATED envelope before the runner is reached, so a denial costs no concurrency slot, no invocation record and no handler run; a valid token populates v2 request.app and v1 context.app with the app ID and the decoded claims",
      "callable Authorization integrity: only a Firebase ID token verified against the target project AuthRegistry on the virtual clock and resolved as a user is reinserted. Bearer owner, service credentials, non-Bearer values and duplicate or folded fields are never callable user identities",
      "every caller-supplied copy of a field the daemon owns is stripped before the trusted one is inserted: the App Check field, Authorization, the per-runner secret, and the x-callable-context-auth / x-original-auth channels firebase-functions honours under the debug switch to override v1 callable auth context",
      "ordinary onRequest functions receive the raw App Check field list unclassified, duplicates included: application code owns custom-backend verification there (section 7.3)",
      "the runner runs with FIREBASE_DEBUG_MODE=true and only the skipTokenVerification debug feature, and only while the callable trusted protocol is active; the runner proves at startup that the installed firebase-functions reads those switches as expected and reports the auth-override header names it honours, which must all be ones the proxy strips",
      "the runner HTTP server refuses every request when it has no per-runner secret rather than serving them unguarded, and compares the secret it does have in constant time",
      "with App Check active, a configured functions.manifest is reconciled against discovery: it may not call a real callable an onRequest function, which would route the request around the trust boundary entirely",
      "version-bounded loader instrumentation reads each callable enforceAppCheck and consumeAppCheckToken as the callable is declared, by hooking onCallHandler, withInit and wrapTraceContext -- the one path every v1, v2, builder and genkit spelling passes through",
      "consumeAppCheckToken is three-valued and fail-closed: true fails function discovery with APP_CHECK_REPLAY_UNSUPPORTED whatever else is configured, an undeterminable value fails startup when App Check is enabled, and it is never guessed as false"
    ],
    "notes": [
      "supported firebase-functions majors are 6 and 7; outside that range, or when the internal callable modules move, the instrumentation reports itself unsupported and starting Functions with App Check enabled fails rather than guessing",
      "onCallGenkit is covered because it goes through the same onCallHandler hook",
      "the callable baseline is always unenforced at the daemon: it classifies and records every token so that valid app context reaches callables that do not enforce, and enforcement is the callable option",
      "conformance: appcheck/unenforced-header-matrix, appcheck/enforced-header-matrix (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "precision": "boundary-conformance",
    "status": "implemented",
    "unimplemented": [
      "limited-use consumption and alreadyConsumed (APPCHECK-REPLAY-1)"
    ]
  },
  "APPCHECK-JWKS-1": {
    "implemented": [
      "GET /v1/jwks and /v1beta/jwks: an RFC 7517 JWK Set with this instance's public App Check key only",
      "public on loopback and never App Check protected (bootstrap and public-key discovery)"
    ],
    "notes": [
      "Cache-Control: no-store is an intentional local divergence: a daemon restart creates a new key at the same loopback URL, so a client must not retain the previous instance's JWKS",
      "the response carries no private material: the modulus and the exponent only",
      "conformance: appcheck/exchange-and-jwks (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "precision": "exact-for-local-semantics",
    "status": "implemented"
  },
  "APPCHECK-OBSERVE-1": {
    "implemented": [
      "secret-free observations of every debug-token exchange and of every classified Auth, Firestore (unary, REST, streams and WebChannel), Storage and callable Functions request, with project, service, transport, operation, mode, credential category, privileged failure reason, verified app ID, logical time, policy generation and outcome",
      "one bounded ring and one set of counters per project, created on first use and dropped whole when that project state is cleared or its session is deleted: a registered session project and the default project alike keep the 256 most recent observations of their own traffic, so a flood of requests to one project never evicts another project recent observations",
      "the counters are not derived from the ring: they count every classified request of the project since its state was last reset, including the observations the ring has already dropped",
      "GET /v1/sessions/{session}/appCheck/observations: the session project own counters by service, verified app ID, callable function name for the functions service, category and outcome, plus that project retained observations with their privileged reasons; another session project can neither be read here nor influence what this one is answered; the control token is required for every method whatever the Origin, and the response is Cache-Control: no-store",
      "the Emulator UI App Check page (/ui/appcheck): the registered apps of the selected session project with their configured digest count and their dynamic registrations, the effective baseline mode of every service, this instance JWKS kid, debug-token registration and deletion, and the counters and recent observations of the session",
      "the UI API front: GET /ui/api/appcheck/config summarises the registration (apps and modes, never a digest, a raw secret or an epoch), /ui/api/appcheck/projects/{p}/apps/{a}/debugTokens fronts the privileged management routes, and the observation route answers through the control front for the named session alone; each of them is Cache-Control: no-store, and a generated debug secret reaches the page in the creation response alone -- never in a list, a log, window.__FIREEMU__ or an observation",
      "observation counters and the ring reset with the project state they describe (a reset, a project deletion and a snapshot restore clear both)",
      "unverified identities aggregate into a bounded unknown bucket; no raw JWT, signature, debug secret, digest, private key or epoch value is ever recorded"
    ],
    "notes": [
      "the table of rings is bounded too, because a target project is resolved from a request path before anything validates it: at most 256 projects hold a ring at once, a project ID longer than 63 bytes (which no session could name) opens none, and a project the registry knows displaces the least recently used ring of one it does not rather than being crowded out by unregistered traffic",
      "a project whose counter key space reaches 1024 distinct keys folds further counts into the same key without the app ID and the callable name, so the label set stays bounded without dropping a count"
    ],
    "precision": "exact-for-local-semantics",
    "status": "implemented",
    "unimplemented": [
      "observations are polled, not streamed: there is no server-sent event channel for them"
    ]
  },
  "APPCHECK-PROVIDER-ATTESTATION-0": {
    "notes": [
      "Play Integrity, App Attest, DeviceCheck and reCAPTCHA are production attestation providers and are outside local emulation; a local token proves nothing about device integrity"
    ],
    "precision": "not-applicable",
    "status": "unsupported"
  },
  "APPCHECK-REPLAY-1": {
    "notes": [
      "contract: limited-use issuance, atomic consumption, concurrent replay observation and callable alreadyConsumed propagation",
      "limitedUse: true fails closed with 501 APP_CHECK_REPLAY_UNSUPPORTED and never returns a reusable token"
    ],
    "precision": "unsupported",
    "status": "unsupported"
  },
  "APPCHECK-SDK-WEB-1": {
    "implemented": [
      "tools/sdk-smoke/appcheck.mjs: the real firebase Web SDK obtains a local token through initializeAppCheck + CustomProvider against the local exchange endpoint and sends it to Firestore, Storage (a resumable upload), Auth and an enforceAppCheck callable, with the enforced refusals checked too",
      "it runs in Node without browser shims: CustomProvider touches no browser global and the SDK guards its indexedDB token cache"
    ],
    "notes": [
      "the SDK never dials the local exchange itself -- @firebase/app-check hard-codes the production base URL -- so the documented provider calls the daemon and returns the JWT with an expiry derived from the ttl",
      "the smoke needs tools/sdk-smoke/fireemu.appcheck.json, which registers the app and the SHA-256 digest of a clearly fake debug secret",
      "Android, Apple and Flutter SDKs are not exercised (AC-CONF-001)"
    ],
    "precision": "boundary-conformance",
    "status": "implemented"
  },
  "AUTH-CORE-1": {
    "implemented": [
      "accounts:signUp (email, anonymous)",
      "accounts:signInWithPassword",
      "accounts:signInWithCustomToken (unsigned JWT or strict JSON fake token, uid or user_id)",
      "accounts:lookup",
      "accounts:update (profile, email, password, phone, deleteAttribute DISPLAY_NAME / PHOTO_URL / EMAIL / PASSWORD, deleteProvider phone / password / federated, linkProviderUserInfo; a credential change re-issues tokens in the response)",
      "accounts:delete (the session's own account)",
      "accounts:sendOobCode / resetPassword / signInWithEmailLink (PASSWORD_RESET, VERIFY_EMAIL, EMAIL_SIGNIN, VERIFY_AND_CHANGE_EMAIL; applyActionCode through accounts:update)",
      "accounts:sendVerificationCode / signInWithPhoneNumber",
      "accounts:createAuthUri",
      "GET /v1/projects and /v1/recaptchaParams (the fixed documents the official emulator serves)",
      "securetoken token (JSON and form)",
      "Admin: projects/{p}/accounts create/lookup/update/delete/batchGet/batchCreate/batchDelete/query/sendOobCode and projects/{p}:createSessionCookie; mfaInfo / mfa.enrollments (phone), linkProviderUserInfo / deleteProvider",
      "Emulator routes: /emulator/v1/projects/{p}/oobCodes, verificationCodes, accounts (DELETE), config (GET and PATCH: signIn.allowDuplicateEmails is recorded for export, emailPrivacyConfig.enableImprovedEmailPrivacy changes what signInWithPassword, sendOobCode PASSWORD_RESET and createAuthUri reveal)"
    ],
    "notes": [
      "nothing is mailed or texted: codes are read from the emulator routes and are deterministic from the session seed",
      "reCAPTCHA tokens are accepted unchecked",
      "error envelopes measured against firebase-tools 15.28.2: EMAIL_NOT_FOUND / INVALID_PASSWORD by default and INVALID_LOGIN_CREDENTIALS under improved email privacy, MISSING_EMAIL before MISSING_PASSWORD, the Not Found envelope with status NOT_FOUND for an unknown path, kind fields on every response, absent rather than null optional fields, 28-character generated local ids",
      "one route table (crates/fireemu-adapter-http/src/identity_toolkit/routes.rs) drives dispatch, the App Check bypass classification and the bounded operation labels; a route cannot be dispatched without a label",
      "transient credentials are bounded: email action codes expire after an hour and phone codes after ten minutes of virtual time, pending second-factor sign-ins after an hour, and each kind refuses with QUOTA_EXCEEDED past 1000 outstanding codes per project or 32 pending sessions per user; expired entries are swept before every request and never observable; refresh tokens are not swept",
      "Admin batchCreate (importUsers) verifies a rawPassword or the emulator's fakeHash form; any other passwordHash is kept out and the account is created without a password credential, which is observably what the official emulator does with a hash it cannot compare",
      "conformance: auth/sign-up-and-sign-in-errors, auth/identity-toolkit-error-shapes, auth/oob-code-shapes, auth/admin-account-lifecycle, auth/client-account-flows (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "status": "implemented"
  },
  "AUTH-MFA-SMS-0": {
    "notes": [
      "phone second factors: mfaEnrollment:start/finalize/withdraw with phoneEnrollmentInfo / phoneVerificationInfo, mfaSignIn:start/finalize with phoneSignInInfo; codes come from /emulator/v1/projects/{p}/verificationCodes; up to five factors per user",
      "measured against firebase-tools 15.28.2: enrollment is refused with UNSUPPORTED_FIRST_FACTOR for an anonymous, phone, custom-token or Game Center session, with UNVERIFIED_EMAIL for an unverified email and with SECOND_FACTOR_EXISTS for a number already enrolled, on both the start and the finalize step and without creating or consuming a verification code; a pending-credential response obfuscates the number to its last four digits",
      "the spec declared SMS out of scope; the user asked for Identity Platform parity",
      "conformance: auth/mfa-error-shapes, auth/mfa-enrollment-eligibility (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "precision": "emulator-parity",
    "status": "implemented"
  },
  "AUTH-MFA-TOTP-1": {
    "notes": [
      "window and enrollment TTL are local policies; an expired enrollment session answers SESSION_EXPIRED for one further session lifetime and is then reaped",
      "the verified-email requirement of phone enrollment is not applied to TOTP enrollment: the official emulator has no TOTP route to measure it against, and production behaviour is not inferred from the SMS result",
      "default snapshots carry no shared secret (INV-AUTH-003, ADR-034 amendment): an enrolled factor is captured with a detached secret and rebound on restore to the secret the live store still holds; a factor withdrawn in between is dropped from the restored account and reported on stderr, never restored unusable",
      "conformance: auth/mfa-error-shapes (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "precision": "boundary-conformance",
    "status": "implemented"
  },
  "AUTH-OAUTH-0": {
    "implemented": [
      "accounts:signInWithIdp (verifyAssertion): the credential is read from requestUri and/or postBody (the official getNormalizedUri merges the request URI query, the post body and the URI fragment); providerId is lowercased; the id_token (a fake JWT or strict JSON) or a JSON access_token carries the claims; no OAuth round trip",
      "per-provider assertion shaping (fakeFetchUserInfoFromIdp): google.com reports federatedId https://accounts.google.com/<sub>, firstName / lastName and a granted-scopes rawUserInfo; saml.<name> reads the email from the SAML assertion subject nameId, forces emailVerified true and uses the attributeStatements as rawUserInfo; oidc.<name> and every other provider keep the JSON claims as rawUserInfo",
      "SAMLResponse (a JSON blob, not real XML) is validated for assertion / subject / nameId with the official INVALID_IDP_RESPONSE messages",
      "create-or-link semantics: match by (providerId, rawId), then by a verified assertion email (linking, recycling an unverified-email account by clearing its password, phone and providers and bumping validSince, and reporting emailRecycled), else needConfirmation for an unverified assertion email owned by an existing account, else a new account; linking to a session idToken with FEDERATED_USER_ID_ALREADY_LINKED and returnIdpCredential errorMessage handling",
      "the id_token vs access_token NotImplementedError reproduction (google.com / apple.com and any other provider), and accounts:createAuthUri answering NotImplementedError for a providerId",
      "the identity-provider login widget pages /emulator/auth/handler (the sign-in-via-popup / redirect handoff, listing the accounts already linked at the provider) and /emulator/auth/iframe (the gapi relay page), reproduced from the pinned widget_ui.js",
      "ID tokens carry firebase.identities[providerId] and sign_in_provider = providerId"
    ],
    "notes": [
      "the official emulator stores no SAML / OIDC provider configuration (defaultSupportedIdpConfigs / inboundSamlConfigs are in its OpenAPI spec but not served), so a saml.* / oidc.* provider needs no registration and there is none to round-trip",
      "conformance: measured by crates/fireemu-adapter-http/tests/auth_flows.rs and crates/fireemu-core-auth/tests/store.rs against the pinned firebase-tools operations.js / widget_ui.js; no gated corpus scenario was added in this pass"
    ],
    "fireemuOnly": [
      {
        "behaviour": "the widget /emulator/auth/handler HTML-escapes the account display name, email and photo URL it injects into the reuse-account list; the official page injects them unescaped. Both serve the same page to a loopback caller and mint no credential (that is accounts:signInWithIdp, guarded on its own), so escaping only removes a stored-XSS footgun in a page the developer opens in their own browser.",
        "precision": "strictly-safer"
      },
      {
        "behaviour": "a providerId containing a NUL or other control character, or longer than 256 bytes, is refused with INVALID_CREDENTIAL_OR_PROVIDER_ID before it reaches the store; the official emulator does not validate the providerId. A malformed provider id is never used as a store key, a token claim or widget HTML.",
        "precision": "strictly-safer"
      }
    ],
    "precision": "fixture-idp",
    "status": "implemented"
  },
  "AUTH-RULES-1": {
    "notes": [
      "request.auth from verified ID tokens on every Firestore surface; Bearer owner bypasses",
      "the compatibility profile decides how much verification a caller's token must survive: under firebase the unsigned mock tokens the official emulators admit are admitted too (an unknown sub, an exp nobody reads), so @firebase/rules-unit-testing works unchanged; under strict the issuer, audience, virtual-clock expiry, subject and revocation are all checked",
      "two checks stay on in both profiles, and spec/compatibility/contract.json records them as deliberate divergences of the firebase profile: the audience must name the routed Firestore resource project or Storage bucket project, and a token that claims to be signed must verify against the session key"
    ],
    "status": "implemented"
  },
  "AUTH-TOKEN-1": {
    "implemented": [
      "unsigned-emulator (alg none, the Firebase Auth Emulator format)",
      "session-rsa (RS256 with a 2048-bit key derived from the session seed; JWKS at /.well-known/jwks.json and the securetoken@system.gserviceaccount.com path)"
    ],
    "notes": [
      "with session-rsa every surface (Identity Toolkit, Firestore rules, Storage rules) refuses unsigned or foreign-signed tokens",
      "the Firebase Admin SDK accepts only alg none while FIREBASE_AUTH_EMULATOR_HOST is set, so verifyIdToken through the Admin SDK needs unsigned-emulator",
      "conformance: auth/sign-up-and-sign-in-errors (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "status": "implemented"
  },
  "CTL-1": {
    "implemented": [
      "sessions: POST /v1/sessions {project, buckets?, apiKeys?} creates a session that owns its project (Firestore databases, its default and declared Storage buckets, an Auth store, a fault plan, snapshots, text indexes); the default session owns everything not registered; GET lists, DELETE removes; project-scoped Auth routes, declared API keys, token audiences and refresh tokens pick the project's store; ID tokens are accepted only by the project of their audience",
      "clock:set",
      "clock:advance",
      "clock:advanceTo",
      "sessions/{s}/reset (wipes the session's scope under the exclusive barrier; the default session's reset also resets the shared functions runtime; a store that refuses the wipe is reported as INTERNAL and no store is wiped)",
      "sessions/{s} DELETE (wipes the project's state, then deregisters the session; a store that refuses leaves the session registered and usable)",
      "sessions/{s}/snapshots (POST {name} captures the session's Firestore, Storage, Auth, fault plan and text indexes, plus the clock, both rulesets and the auto-ID generator for the default session; every part is copied before any is retained, so a store that cannot be read refuses the whole snapshot; GET lists with retained / limit / remaining; {name}:restore validates every part, takes a pre-image and applies in hook order under the reset barrier, rolling the hooks it already applied back when a later one refuses, and the default session's restore resets the functions runtime; DELETE releases what the name held)",
      "sessions/{s}/snapshots budget: {MAX_SNAPSHOTS_PER_SESSION} names per session, RESOURCE_EXHAUSTED (429) beyond it with the retained set unchanged; capturing over a retained name is always admitted and releases the old copy",
      "sessions/{s}/faultPlan (PUT the spec 18.2 plan, GET plan + fired history + counters, DELETE; one state per session)",
      "sessions/{s}/functions and functions/{name}:run",
      "sessions/{s}/pubsub/topics/{t}:publish",
      "sessions/{s}:awaitIdle",
      "rules load/replace/drop (admitted like data requests)",
      "health",
      "capabilities",
      "limits"
    ],
    "notes": [
      "fault operations: firestore.commit / read / beginTransaction, storage.upload / read / delete / list / request, functions.deliver (duplicate; a rule with only an eventType), functions.invoke (returnError, timeout, deadLetter, delay, crashRunner, dropConnection; applies to event, HTTP, scheduled and manual invocations); actions that do not apply to an operation are refused; rules match the nth occurrence per operation, or per operation and function when a function is named; a delay keeps the other actions for when the event goes and wakes the runtime when the clock reaches it; dropConnection closes the connection (REST, Storage, functions HTTP) or resets the gRPC stream, is UNAVAILABLE over WebChannel and a failed attempt for event invocations"
    ],
    "status": "partial",
    "unimplemented": [
      "per-session clocks, rules and functions (sessions share them)",
      "per-session admission barriers (a reset of one session waits for every session's requests in flight)",
      "snapshot files on disk (snapshots live in memory for the daemon's lifetime)",
      "structural sharing of captured blobs and MVCC history (every retained snapshot is a full copy of what the session owned, which is what the per-session budget bounds)",
      "capture of outstanding functions work (allowNonQuiescent snapshots drop it on restore)"
    ]
  },
  "CTL-1-AWAIT-IDLE": {
    "implemented": [
      "POST /v1/sessions/{s}:awaitIdle waits for pending / running / retry-waiting events, HTTP invocations and due schedule runs beyond the catch-up cap; events are enqueued inside the commit that produces them"
    ],
    "notes": [
      "reset holds the session admission barrier exclusively across Firestore, Auth, Storage and the functions runtime; the runner is killed and restarted so handlers still running cannot write into the new session"
    ],
    "status": "partial",
    "unimplemented": [
      "causal fences across sessions",
      "Text Index work"
    ]
  },
  "FN-EVT-1": {
    "implemented": [
      "Firestore document created / updated / deleted / written triggers (v2, JSON CloudEvents; v1 document handlers)",
      "Firestore *.withAuthContext variants: authtype / authid of the committing principal (app_user, service_account, unauthenticated, system)",
      "Storage object finalized / deleted / metadataUpdated triggers (v2 and v1)",
      "Pub/Sub topic triggers (v2 onMessagePublished, v1 topic().onPublish) fed by POST /v1/sessions/{s}/pubsub/topics/{t}:publish or /v1/projects/{p}/topics/{t}:publish",
      "Auth user created / deleted events to v1 auth.user() handlers",
      "Eventarc custom events (onCustomEventPublished) fed by POST /projects/{p}/locations/{l}/channels/{c}:publishEvents on the functions port, which CLOUD_EVENTARC_EMULATOR_HOST names, so firebase-admin getEventarc().channel().publish() reaches them unconfigured; the proto CloudEvent is converted the way cloudEventFromProtoToJson converts it and eventFilters are matched as matchesAll matches them",
      "Firebase alerts (the whole onAlertPublished family) fed by POST /google/publishEvents, which is the only door the official emulator has for them too: an alert trigger is an Eventarc trigger on the sentinel google channel, and the CloudEvent is forwarded verbatim",
      "Cloud Tasks queue functions (onTaskDispatched) enqueued with the Admin SDK's getFunctions().taskQueue(name).enqueue(payload) against CLOUD_TASKS_EMULATOR_HOST, which names the functions port; the queue is the function, its defaultUri is that function's own URL, and a dispatch carries the official X-CloudTasks-* header set and retries on the official backoff",
      "retries with virtual-time backoff, dead letters, await-idle",
      "bounded diagnostic retention: the 1000 most recent invocation records, 500 dead letters and 1000 terminal event records are kept (older ones are dropped) while the succeeded / deadLettered / overlapRejected counters stay cumulative; every retained record carries a monotonic sequence inside a generation the reset bumps, and the UI log stream consumes cursor deltas and resyncs when its cursor is expired or from an older generation"
    ],
    "status": "implemented",
    "unimplemented": [
      "blocking identity functions (beforeUserCreated / beforeUserSignedIn, v1 beforeCreate / beforeSignIn): discovered, named and refused; the Auth emulator does not call them",
      "Realtime Database and Remote Config triggers (no such service in the daemon)",
      "protobuf CloudEvents payloads (JSON is what firebase-functions consumes)"
    ],
    "notes": [
      "the Eventarc and alerts routes live on the functions port, not on the official suite's own Eventarc port 9299; the environment variable is exported accordingly, so an SDK reaches them unchanged",
      "no fireemu-only alert-injection route exists: firebase-tools has none either, and its Emulator UI fires an alert by POSTing to /google/publishEvents like any other client",
      "a task may not name a URL of its own (TaskOptions.uri): only this emulator's function URL is dispatched to, because a local emulator making an arbitrary outbound request on a caller's say-so is a request-forgery surface the rest of fireemu does not offer",
      "task retry backoff runs on real time, not the virtual clock, as it does upstream and in production; the default unit is 100 ms, so three attempts cost 300 ms rather than a clock advance, and await-idle waits for a task across its attempts"
    ]
  },
  "PUBSUB-WIRE-1": {
    "status": "implemented",
    "precision": "boundary-conformance",
    "implemented": [
      "the google.pubsub.v1 Publisher and Subscriber services over gRPC on the loopback port PUBSUB_EMULATOR_HOST names (default 8085): CreateTopic / GetTopic / ListTopics / ListTopicSubscriptions / DeleteTopic, CreateSubscription / GetSubscription / UpdateSubscription / ListSubscriptions / DeleteSubscription, Publish, Pull, StreamingPull, Acknowledge, ModifyAckDeadline and Seek (to a timestamp)",
      "topic and subscription state: publish fans a message out to every subscription whose filter admits it; a subscription reports _deleted-topic_ once its topic is deleted",
      "delivery semantics: ack ids and message ids assigned deterministically under the daemon seed, ack deadlines and redelivery of unacked messages on the virtual clock, nack (ModifyAckDeadline 0) for immediate redelivery, ordering keys that hold a key's later messages until the earlier one is acked, subscription filters over attributes (equality, inequality, existence, hasPrefix, NOT/AND/OR), dead-letter forwarding after maxDeliveryAttempts, and seek-to-timestamp replay",
      "input hardening: resource names validated against the documented Pub/Sub rules (3..=255, leading letter, the [A-Za-z0-9._~%+-] alphabet, no goog prefix), message size and attribute count bounds, and NUL rejection in attribute keys/values and ordering keys; topics, subscriptions and retained messages are bounded so a client cannot exhaust memory",
      "a message published through the wire protocol also reaches subscribed Cloud Functions (onMessagePublished v2, topic().onPublish v1) through the existing FunctionsRuntime delivery path, so EVTINFRA-02 flows through real topic state without regressing the control publish route"
    ],
    "unimplemented": [
      "snapshots (CreateSnapshot / GetSnapshot / ListSnapshots / DeleteSnapshot) and seek-to-snapshot: reported UNIMPLEMENTED",
      "push delivery to an endpoint: a push config is accepted and returned but delivery uses pull and streaming pull",
      "UpdateTopic: reported UNIMPLEMENTED, as the official emulator does not support it either",
      "REST/JSON transcoding on the port: the surface is gRPC-only, which is what the @google-cloud/pubsub and firebase-admin clients speak",
      "schemas (Avro or protobuf) and BigQuery / Cloud Storage subscription delivery"
    ],
    "notes": [
      "conformance: the pubsub-probe records 22 parity steps and 1 documented divergence against the official pubsub-emulator 0.8.35 (conformance/pubsub-matrix.json, conformance/PUBSUB-MATRIX.md, conformance/README.md); this records agreement with the official Local Emulator Suite, never with production",
      "the one documented divergence is a superset: fireemu retains acknowledged messages for seek even when retainAckedMessages is false, so a seek never returns fewer messages than the official emulator (conformance divergences.json pubsub-probe/seek-to-time#replayed)",
      "ack deadlines and redelivery run on the virtual clock, so a test advances time with the control clock rather than sleeping, and await-idle stays deterministic",
      "the Pub/Sub broker state lives for the daemon's lifetime and is not yet wiped by a session reset or captured by a snapshot"
    ]
  },
  "FN-HTTP-1": {
    "implemented": [
      "onRequest / onCall (v2) and v1 HTTPS functions proxied to the runner at /{project}/{region}/{function}[/path], with the path and query below the mount point reaching the handler",
      "the official emulator's two 404s: `Not Found` (text/plain) for a path that is not a function route, and `Function <region>-<name> does not exist, valid functions are: <trigger keys>` (text/html) for a route whose function is unknown, with the keys in the official order (grouped by region, export order within a region)",
      "timeoutSeconds: a function that overruns it gets the official answer, HTTP 500 with the body {\"code\":\"ECONNRESET\"} and no content-type, and the emulator logs `Your function timed out after ~Ns.`",
      "CORS for an onRequest function on a loopback origin: the preflight answer cors({origin: true}) produces (204, the origin reflected, GET,HEAD,PUT,PATCH,POST,DELETE, the requested headers, Vary), and Access-Control-Allow-Origin on the ordinary answer"
    ],
    "notes": [
      "needs functions.source (or --functions <dir>) and the bundled Node runner; the codebase must have express installed (a dependency of firebase-functions)",
      "conformance: functions/callable-error-envelope, functions/callable-auth-context, functions/http-routing-cors-and-timeouts (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production",
      "a non-loopback Origin is refused with 403 rather than reflected: the official emulator runs its runtime with enableCors and answers any origin on the internet, which is the one documented divergence of this port",
      "the timeout does not kill the runner; the official emulator kills the runtime process of the one trigger it runs per process, and one fireemu runner serves a whole codebase"
    ],
    "status": "implemented"
  },
  "FN-SCH-1": {
    "implemented": [
      "onSchedule with cron and App Engine schedules driven by the virtual clock (scheduler.catchUp = all | latest | none; all is capped per advance with the remainder kept due; latest and none compute the run they keep without walking the missed ones, and record one skipped summary per job and clock change whose count is exact up to scheduler.maxCatchUpRuns and \"at least N\" beyond it)",
      "manual runs via POST /v1/sessions/{s}/functions/{name}:run",
      "IANA time zones with daylight-saving rules (chrono-tz; gaps skipped, ambiguous times run once)",
      "scheduler.overlap = allow | skip | queue | reject"
    ],
    "status": "implemented"
  },
  "FS-GW-1": {
    "notes": [
      "firestore.indexValidationPolicy = emulator serves queries whose composite index is not configured (FS_EMULATOR_INDEX_ASSUMED warning), like the Firebase Emulator Suite; firebase / conservative keep rejecting them with FAILED_PRECONDITION",
      "the compatibility profile sets that policy: firebase (the default) selects emulator, so the gateway admits what the pinned official emulator admits, and strict selects conservative; an explicit firestore.indexValidationPolicy wins over both",
      "firestore.enforceLimits follows the profile the same way: under strict a Standard query limit violation refuses the query with INVALID_ARGUMENT, under firebase it is reported as an FS_LIMIT_OBSERVED:<limit id> warning and the query runs",
      "conformance: firestore/missing-composite-index (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production",
      "the query limits the pinned official emulator refuses as well -- more than 30 DNF disjunctions ('IN' over 30 values) and a second array-contains clause (FAILED_PRECONDITION) -- stay refused under the firebase profile (fireemu_adapter_grpc::gateway::OFFICIAL_EMULATOR_REFUSES)"
    ],
    "precision": "conservative",
    "status": "implemented"
  },
  "FS-LIM-1": {
    "implemented": [
      "FS-LIMIT-DOCUMENT-BYTES",
      "FS-LIMIT-NESTED-MAP-ARRAY-DEPTH",
      "FS-LIMIT-FIELD-TRANSFORMS-PER-DOCUMENT",
      "FS-LIMIT-COLLECTION-ID",
      "FS-LIMIT-DOCUMENT-ID",
      "FS-LIMIT-TRANSACTION-TOTAL-TIME",
      "FS-LIMIT-TRANSACTION-IDLE-TIME",
      "FS-QUERY-LIMIT-DNF-DISJUNCTIONS",
      "FS-QUERY-LIMIT-ARRAY-CONTAINS-PER-DISJUNCTION",
      "FS-QUERY-LIMIT-ARRAY-CONTAINS-COMBINATION",
      "FS-QUERY-LIMIT-NOT-IN-VALUES",
      "FS-QUERY-LIMIT-NOT-IN-NEQ-COMBINATION",
      "FS-QUERY-LIMIT-INEQUALITY-FIELDS",
      "FS-QUERY-LIMIT-COMPONENTS"
    ],
    "status": "partial",
    "notes": [
      "this list is derived from the runtime, not maintained beside it: fireemu_core_firestore::limits names every enforced limit, tests/enforced_limits.rs fails when it and the catalogs' implemented sets differ in either direction, and the daemon test the_limit_metadata_agrees_with_the_enforcement holds this entry and /v1/limits to the same set",
      "the Standard document and transaction limits are refused under both profiles; the transforms-per-document budget and the query limits are production-only refusals that the firebase profile observes as warnings instead (LimitScope / firestore.enforceLimits), except the two query limits the official emulator refuses as well (more than 30 disjunctions, a second array-contains), which stay refused under both"
    ]
  },
  "FS-LSN-1": {
    "implemented": [
      "query and document targets",
      "live diffs after every commit",
      "once",
      "resume from a resume token or read time: the state at that version is recomputed from the retained MVCC history and only the changes since are replayed, followed by an ExistenceFilter with the current count (an undecodable or future token RESETs)",
      "resume tokens live exactly as long as the read_time retention window: a token whose version has been compacted away is refused explicitly with RESET and a full replay, never resumed against unrelated history",
      "back-pressure: bounded response channel, commits coalesced into one refresh while the client is slow, 1000 targets per stream (RESOURCE_EXHAUSTED beyond)",
      "a target refused on its own merits (missing composite index, malformed or out-of-database query, rules denial) is answered with TargetChange REMOVE carrying that cause and the stream keeps serving its other targets; stream-level errors are reserved for session-wide or database-wide failures"
    ],
    "notes": [
      "existence filters carry the count only (no bloom filter): the replay is exact, so the client never has to guess",
      "conformance: firestore/listen-sequence-with-resume (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "status": "implemented"
  },
  "FS-PIPE-RPC-1": {
    "implemented": [
      "ExecutePipeline / StructuredPipeline decode",
      "database name, consistency selector (non-empty transaction; auto_commit_transaction only with new_transaction) and pipeline option (index_mode) checks",
      "stage identification against the documented registry (collection, collection_group, database, documents, select, add_fields, remove_fields, where, sort, limit, offset, distinct, aggregate, find_nearest, sample, union, unnest, replace_with)",
      "input stage placement, arity, argument shape (paths, integers, boolean expressions, ascending / descending orderings, alias maps, vectors and distance measures, modes) and per-stage option checks (find_nearest limit / distance_field, sample mode, unnest index_field)",
      "write stages (update, delete) refused as FS-PIPE-WRITE-0",
      "unknown stages refused (FS_PIPE_UNSUPPORTED_STAGE), never skipped"
    ],
    "notes": [
      "Standard edition answers FAILED_PRECONDITION FS_PIPE_EDITION",
      "owner-only while rules are enforced"
    ],
    "precision": "strict-validation-only",
    "status": "implemented",
    "unimplemented": [
      "local execution (a valid pipeline is answered with UNIMPLEMENTED FS_PIPE_VALIDATION_ONLY carrying its canonical form)",
      "function name and return type checking inside expressions (a where takes any function)",
      "text search functions against the text index catalog",
      "proxy / record-only modes"
    ]
  },
  "FS-QRY-1": {
    "notes": [
      "orderBy on missing fields excludes documents",
      "range filters are type-restricted",
      "conformance: firestore/values-and-ordering, firestore/missing-composite-index (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production",
      "conformance: the Firestore semantics matrix (conformance/firestore-matrix.json; pnpm -C conformance run firestore:check) gates values and type order, filters with null and NaN edges, cursors, collection groups, aggregations, projections and listings row by row against the pinned official emulator"
    ],
    "status": "implemented"
  },
  "FS-REST-1": {
    "notes": [
      "served on the Firestore port next to gRPC",
      "readTime selectors and findNearest are refused explicitly",
      "conformance: firestore/rest-error-shapes, firestore/lite-rest-transport (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production",
      "REST wire shapes follow the measured proto3 JSON of the official emulator: empty fields are left out, timestamps carry 0/3/6/9 fraction digits, the last RunQuery response says done, the first carries skippedResults, and an unknown route or method answers the plain-text 404 the official adapter answers"
    ],
    "status": "implemented"
  },
  "FS-RPC-1": {
    "implemented": [
      "GetDocument",
      "ListDocuments",
      "CreateDocument",
      "UpdateDocument",
      "DeleteDocument",
      "BatchGetDocuments",
      "BeginTransaction",
      "Commit",
      "Rollback",
      "RunQuery",
      "RunAggregationQuery",
      "ListCollectionIds",
      "BatchWrite",
      "Write",
      "Listen",
      "verify writes",
      "read_time snapshots (microsecond precision, within the past hour)",
      "read-only transactions at a read_time",
      "ListDocuments inside transactions"
    ],
    "notes": [
      "ExecutePipeline is not served by this entry: FS-PIPE-RPC-1 owns it and decodes, identifies and validates a pipeline, then answers a well-formed one with UNIMPLEMENTED FS_PIPE_VALIDATION_ONLY rather than executing it",
      "history retention: a version is kept while any retention root can still reach it, namely the one-hour read_time window (READ_TIME_RETENTION_SECONDS), the read version of every transaction that is still inside its budget, and the newest version or tombstone of every path; everything older is compacted away at the end of the commit that makes it unreachable, and a read_time below the retained history is FAILED_PRECONDITION instead of an answer from another version. Named snapshots are independent copies: restoring one reproduces exactly what it captured, whatever the live database compacted since.",
      "conformance: the Firestore semantics matrix also gates preconditions, update masks, field transforms, batched writes and the transaction lifecycle; conformance/src/firestore-probe/divergences.mjs pins every deliberate difference, including the optimistic transaction model (fireemu aborts the reading transaction at commit where the official emulator locks and refuses the out-of-band writer)"
    ],
    "status": "implemented"
  },
  "FS-TEXT-VAL-1": {
    "implemented": [
      "firestore.textIndexDefinitionFile (schemaVersion 1, Admin API Index subset + xFirebaseTestd) validated at start",
      "control API: POST firestore/text-indexes:load, POST / GET firestore/text-indexes, GET / DELETE firestore/text-indexes/{id} (definitions are kept per project and database; a session sees the projects it owns)",
      "duplicate IDs refused, duplicate shapes warned (FS_TEXT_DUPLICATE_INDEX_DEFINITION, field-order insensitive, apiScope aware), unresolved language override warned, unsupported, unknown (at every level) and output-only fields (index.state) refused"
    ],
    "notes": [
      "Enterprise edition only"
    ],
    "precision": "strict-validation-only",
    "status": "implemented",
    "unimplemented": [
      "backfill lifecycle (advanceBackfill, completeBackfill, failBuild, repair answer UNIMPLEMENTED without changing state; FS-TEXT-IDX-1)",
      "posting data and local search (Milestone G)"
    ]
  },
  "FS-TXN-1": {
    "notes": [
      "serializable: read sets and executed queries are re-validated at commit",
      "conformance: firestore/transaction-read-set-abort (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "precision": "boundary-conformance",
    "status": "implemented"
  },
  "FS-WEB-1": {
    "notes": [
      "WebChannel v8 transport of the browser web SDK (Listen / Write) on the Firestore port; resume as in FS-LSN-1",
      "a target refused on its own merits, including one that rides along with the opening handshake, is delivered as TargetChange REMOVE through the first back channel and the session stays open; only a stream-level error closes it"
    ],
    "status": "implemented"
  },
  "RULES-1": {
    "implemented": [
      "get / create / update / delete against the returned snapshot or the staged commit",
      "list / aggregation / Listen queries proven from equality, in, !=, not-in, array-contains(-any) and inequality (range) constraints plus request.query.limit / offset / orderBy (RULES-QUERY-CONSTRAINTS)",
      "get() / exists() / getAfter() (getAfter over the state the whole commit leaves behind; fails closed on reads and query proofs)",
      "the string, list, map, set, path, timestamp, duration, latlng, math and hashing surfaces, map.diff() and its key sets, range indexing (x[i:j]), path.bind(), and the int / float / string / path conversions",
      "collection groups need a recursive wildcard rule"
    ],
    "notes": [
      "the language is measured rather than assumed: conformance/src/rules-probe installs the same bounded Rules programs into the pinned official Firestore emulator and into fireemu and compares every verdict (680 claims in conformance/rules-matrix.json, 22 whole-program probes in conformance/rules-programs.json)",
      "query proofs are sound, not complete: a rule that reads an unconstrained field denies the query",
      "the principal request.auth is built from follows the compatibility profile on both the Firestore and the Storage Rules surfaces (see AUTH-RULES-1); the rules themselves are evaluated identically under both profiles",
      "request.query.orderBy is a map from field path to \"ASC\" / \"DESC\", request.query.limit is null without a limit, and request.resource and request.query are absent rather than null on a request that has neither",
      "string.matches() / replace(): the RE2 surface the official runtime has -- capturing groups with $0 / $1 expansion, the inline flags (?i) (?s) (?m), POSIX and Unicode classes, hex escapes -- and, as in RE2, no lookarounds and no backreferences; a literal pattern is compiled while the ruleset is, so an unusable one fails the load",
      "a recursive call and a call chain of more than twenty calls are compile errors, with the official compiler's messages",
      "three language rows are documented divergences, all the same one: the official compiler's static type checker rejects at load what fireemu raises at evaluation; the request decision is the same",
      "conformance: firestore/rules-decisions (conformance/README.md); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "precision": "conservative",
    "status": "implemented"
  },
  "RULES-BUDGET-1": {
    "implemented": [
      "RULES-EXPRESSIONS-PER-REQUEST 1000",
      "RULES-FUNCTION-CALL-DEPTH 20 calls (a chain of 21 functions is the deepest that compiles), refused when the ruleset loads",
      "RULES-RECURSION 0: a recursive or cyclical call is refused when the ruleset loads",
      "RULES-DOC-ACCESS-SINGLE 10 per operation and per query proof (get, exists and getAfter of one path each count once)",
      "RULES-DOC-ACCESS-MULTI-TOTAL 20 across atomic commits and BatchGetDocuments"
    ],
    "status": "implemented",
    "notes": [
      "each number is measured against the pinned official emulator, not read off the documentation: ten document accesses are served and eleven are denied, thirty accesses of one path are served, and the call-depth boundary is the one its compiler reports (conformance/rules-programs.json)"
    ]
  },
  "RULES-DIAG-1": {
    "status": "implemented",
    "precision": "boundary-conformance",
    "implemented": [
      "GET /emulator/v1/projects/{project}:ruleCoverage on the Firestore port: the loaded rules files, then a report of one tree per allow condition and per function body, each node naming its sourcePosition (line, column, currentOffset, endOffset) and the values it took with a count each",
      "GET /emulator/v1/projects/{project}:ruleCoverage.html: the same document as a page that underlines every evaluated expression and shows its values on hover",
      "PUT /emulator/v1/projects/{project}:securityRules: the official load route, answering 200 with issues or 400 whose message names the rejected position as L<line>:<column>",
      "GET /v1/sessions/{session}/rules/requests on the control API: the last 100 decided requests, newest first, each with its decision and what every expression evaluated to",
      "the Emulator UI's Rules page lists the decided requests and expands one into its per-expression trace, reading the same control route"
    ],
    "notes": [
      "an expression that raised is reported as undefined carrying the innermost expression that raised and its message; an expression nothing reached has children and no values, as the official report does",
      "the coverage and the traces live with the ruleset they describe, so loading another one replaces both and no stale position is ever served",
      "the two emulator routes take no credential, because the official ones take none and @firebase/rules-unit-testing calls them with no headers; both are loopback-only and both echo the values the ruleset computed, which is what a coverage report is",
      "the control route carries the control token like every privileged route, and never carries a token, a signature or any claim other than the subject the rule saw as request.auth.uid",
      "the denial message fireemu returns is its own: the official emulator answers PERMISSION_DENIED with a per-allow trace of its evaluation, which is a diagnostic rather than an API contract, and fireemu publishes the same information through these routes instead"
    ]
  },
  "RULES-LINT-1": {
    "status": "implemented"
  },
  "ST-OBJ-1": {
    "implemented": [
      "Firebase Storage protocol (/v0/b, the X-Goog-Upload resumable command grammar with terminal denied sessions, download tokens incl. the ?create_token / ?delete_token routes and the official first-read minting, the official plain-status and rules-denial bodies)",
      "JSON API with the official route table (/b short spelling for every object method; /storage/v1 and /download/storage/v1 for reads and deletes; /upload/storage/v1 uploads; object update and copy registered on the short spelling only, exactly as the official emulator registers them, with 501 Not Implemented on the rest; the XML-style GET /{bucket}/{object} fallback, GET /b bucket listing, the multipart/form-data POST /{bucket} upload and the ACL stub)",
      "generation / metageneration preconditions (match / not-match / source) and declared-checksum verification, honoured as production honours them; the official emulator ignores both, and every such step is pinned as a documented divergence in conformance/storage-matrix.json",
      "generation selectors (current generation only, JSON API dialect)",
      "prefix / delimiter listing with the official pagination (all folded prefixes on every page, items paged, the token names the first item of the next page)",
      "MD5 / CRC32C (X-Goog-Hash verified before commit)",
      "Range: bytes=a-b / a- / -n; an unsatisfiable range serves the whole object, as the official emulator serves it",
      "custom metadata keeps the official defined-versus-absent distinction (an empty-but-defined map serializes as metadata: {} on the Firebase dialect)"
    ],
    "notes": [
      "Storage object triggers are not part of this entry: FN-EVT-1 delivers object finalized / deleted / metadataUpdated events, and this entry owns the object surface only",
      "objects are at most 256 MiB and request bodies at most 260 MiB (413 beyond that)",
      "upload bytes are moved into the object store, never copied: one payload-sized buffer per upload",
      "request bodies buffered at the same time are admitted against a process-wide 1 GiB budget; an upload that does not fit is refused with 503 and Retry-After: 1",
      "download tokens are UUID-shaped, ride in the firebaseStorageDownloadTokens custom-metadata key, die with their generation, and every token change is a metadata update with its event, as measured against the pinned suite",
      "conformance: storage/admin-upload-download-metadata, storage/client-protocol-and-rules (conformance/README.md) and the 218-step raw-HTTP storage probe (conformance/storage-matrix.json, pnpm -C conformance run storage-probe); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "status": "implemented",
    "unimplemented": [
      "object versioning / archived generations",
      "ACLs (the official emulator's no-effect POST acl stub is served) and signed URLs",
      "compose",
      "notifications"
    ]
  },
  "STORAGE-RULES-1": {
    "implemented": [
      "service firebase.storage: get / list / create / update / delete with request.resource and resource, measured against the official runtime: request.resource is the whole prospective object (generation, metageneration, timestamps and hashes included), request.resource is null on get / list / delete, crc32c is the decimal spelling the official runtime passes, and the denial bodies are the official Permission denied. No READ|WRITE|LIST permission. envelopes",
      "resumable uploads are authorized at finalization with the received bytes; a refusal is terminal for the session and no object is published",
      "list requires rules_version 2",
      "the Firebase protocol goes through the rules; the JSON API is a fully privileged surface on which rules never run, whatever credential is presented, exactly as the official emulator serves it",
      "firestore.get() / firestore.exists() over the latest Firestore state of the project (STORAGE-RULES-FIRESTORE-ACCESS: two per request)"
    ],
    "notes": [
      "an upload over an existing object evaluates the update method, as production evaluates it; the official emulator evaluates every upload as create, and the difference is pinned in conformance/storage-matrix.json",
      "resource.cacheControl and resource.contentLanguage are production rules fields fireemu keeps; the official emulator's rules resource omits them, so a rule reading them raises there (pinned in conformance/storage-matrix.json)",
      "under the firebase profile a bearer value that does not decode as a JWT is an anonymous caller, as the official emulator's jwt.decode treats it; a decoded token still has to name the project's audience and verify its signature (the rules.idTokenVerification divergence of the compatibility contract)",
      "conformance: storage/client-protocol-and-rules (conformance/README.md) and the rules-request-model program of the storage probe (conformance/storage-matrix.json); this records agreement with the official Local Emulator Suite, never with production"
    ],
    "status": "implemented"
  },
  "CLI-01": {
    "status": "partial",
    "precision": "boundary-conformance",
    "implemented": [
      "firebase.json for every served product: firestore in object or array (named-database) form with rules and indexes (also the legacy index spelling), storage in object or array form with target / bucket / rules, functions in object or array (multi-codebase) form with source, codebase, runtime and ignore",
      "named Firestore databases load, enforce and hot-reload their own isolated rules and index files",
      "emulators.<name>.host and .port for firestore, auth, storage, functions, hub and ui, emulators.ui.enabled, and emulators.singleProjectMode",
      "loopback-only hosts, as a published divergence from firebase-tools (which binds any host as given): emulators.<name>.host and the canonical bind key accept 127.0.0.1, localhost and ::1; 0.0.0.0, :: or a LAN address exits 1 naming the key, because the daemon serves without a credential on loopback and has no routable mode",
      ".firebaserc project aliases: --project resolves an alias, falls back to the default alias, and takes an unknown value as a project ID exactly as firebase --project does",
      "--config reads either file: one carrying schemaVersion is the fireemu canonical configuration, anything else is a firebase.json, so `firebase emulators:exec --config firebase.json` works verbatim",
      "unknown keys fail closed at every level of the sections fireemu reads",
      "an emulators.<name> entry for a product fireemu does not serve is an error when --only named that product and a notice otherwise; nothing is silently dropped",
      "spec/config/firebase-json.schema.json with a valid and an invalid corpus, checked by tools/config-schema-check and replayed through the loader by a unit test"
    ],
    "unimplemented": [
      "one Storage ruleset covers every bucket, so a per-target rules file is reported and not loaded",
      "one Functions codebase runs per daemon: a multi-codebase project names it with --only functions:<codebase>",
      "emulators.singleProjectMode is recorded and published but enforces nothing on its own: fireemu isolates every project into its own session, which is stricter",
      "hosting, database and the other deferred products are not parsed beyond reporting them"
    ],
    "notes": [
      "command-line ports override firebase.json; firebase.json overrides the canonical configuration"
    ]
  },
  "CLI-02": {
    "status": "implemented",
    "precision": "exact-for-local-semantics",
    "implemented": [
      "--only decides the lifecycle: an unselected Firestore, Storage or Functions binds no listener at all and its port stays free for whatever else wants it",
      "the child environment carries exactly the canonical variables of the selected services, in the firebase-tools@15.28.2 env.ts formats: FIRESTORE_EMULATOR_HOST and FIREBASE_FIRESTORE_EMULATOR_ADDRESS as host:port, FIREBASE_AUTH_EMULATOR_HOST as host:port, FIREBASE_STORAGE_EMULATOR_HOST as host:port next to STORAGE_EMULATOR_HOST as http://host:port, FIREBASE_EMULATOR_HUB as host:port, and a synthesised FIREBASE_CONFIG",
      "the variables of unselected services are removed from the command environment, so a stale value in the shell cannot point a suite at a service this run is not serving",
      "an official service fireemu does not serve (database, hosting, apphosting, eventarc, tasks, dataconnect, extensions) is refused by --only with its scope status before anything binds",
      "--only functions:<codebase> selects one codebase, as the official CLI spells it",
      "the functions runner sees the same selection: an unselected product leaves its variable unset there too"
    ],
    "notes": [
      "there is no CLOUD_STORAGE_EMULATOR_HOST variable: the firebase-tools constant of that name emits STORAGE_EMULATOR_HOST, and fireemu sets that one",
      "FIREBASE_DATABASE_EMULATOR_HOST is never set, because fireemu serves no Realtime Database; it is cleared from the child environment for the same reason",
      "two published divergences from the official CLI: fireemu also sets GOOGLE_CLOUD_PROJECT (the official CLI sets only GCLOUD_PROJECT), and it clears the variables of unselected services where the official CLI only adds",
      "the Identity Toolkit socket also carries the control API, the App Check exchange and the UI API; when auth is not selected the control plane moves to an ephemeral loopback port and the configured Auth port is left free"
    ]
  },
  "CLI-03": {
    "status": "implemented",
    "precision": "boundary-conformance",
    "implemented": [
      "the Emulator Hub on the official default port 4400 (emulators.hub, --hub-port, daemon.hubPort), best effort like the UI port: a busy default disables discovery, a busy explicit port is an error, and --hub-port 0 turns it off",
      "GET /emulators: an object keyed by official emulator name, each entry carrying name, host, port, pid and the listen specs, with the Hub and the UI listed as upstream lists them; only services that actually bound a listener appear",
      "GET /: the locator (version, origins, pid) plus this listener's host and port",
      "PUT /functions/disableBackgroundTriggers and PUT /functions/enableBackgroundTriggers answering the official enabled body, or 400 when no functions codebase is loaded",
      "the locator file <temp dir>/hub-<project>.json written at start with version, origins and pid, and removed at exit only while it still names this process; a live locator from another suite is left alone with a notice",
      "FIREBASE_EMULATOR_HUB exported to the exec command as a bare host:port",
      "@firebase/rules-unit-testing 5.0.2 discovers the suite with no explicit host: tools/sdk-smoke/rules-unit-testing.mjs runs initializeTestEnvironment with an empty configuration and exercises the rules decisions, withSecurityRulesDisabled, clearFirestore and a trigger-suppressed write",
      "tools/sdk-smoke/rules-unit-testing-explicit-rules.mjs supplies explicit Firestore and Storage rules while still discovering both services through the Hub, then exercises authenticated and unauthenticated decisions on both products",
      "DELETE /emulator/v1/projects/{project}/databases/{database}/documents on the Firestore port, which is what clearFirestore() calls",
      "POST /_admin/export with the official body {\"path\", \"initiatedBy\"}, refusing a request that carries an Origin header exactly as upstream does; it is what fireemu emulators:export drives after finding the suite through its locator file",
      "PUT /emulator/v1/projects/{project}:securityRules on the Firestore port, which is what loading rules from a test environment calls (see RULES-DIAG-1)",
      "PUT /internal/setRules on the Storage port accepts the one-file rules-unit-testing body, activates a valid candidate atomically and preserves the previous rules after malformed JSON, an invalid file shape or invalid rules"
    ],
    "unimplemented": [
      "POST /dataconnect/clearData: fireemu serves no Data Connect emulator"
    ],
    "notes": [
      "the Hub answers loopback Host headers only, which the official Hub does not check; it can disable background triggers, so a page on a routable name must not reach it by DNS rebinding",
      "a suite moving from the official emulator has to do two things until the Auth gaps close: state an iat on the token authenticatedContext mints (createMockUserToken defaults to iat 0, so exp is 3600 and the token expired in 1970, which fireemu's virtual clock refuses) and create the users the contexts stand for, because fireemu resolves an ID token subject against the project Auth store"
    ]
  },
  "CLI-04": {
    "status": "partial",
    "precision": "boundary-conformance",
    "implemented": [
      "emulators:start and emulators:exec as exact aliases of up and exec, and emulators:export <dir> [--project <id>] [--force] against a running suite",
      "the official flag names --only, --project / -P, --config, --import, --export-on-exit, --inspect-functions and --log-verbosity, next to the fireemu port flags",
      "--inspect-functions [port] inserts Node's --inspect=<port> before the runner script (default 9229), and refuses with a precise message when a configured functions.runner is not Node rather than starting without the inspector",
      "--log-verbosity quiet | info | debug (case-insensitive): quiet suppresses the banner and the notices, debug adds the resolved configuration",
      "exit codes: the command's own status from exec, 128 + signal when a signal ended it, 1 for a startup failure or a refused configuration, 2 for a usage error; a refusal never starts a listener and never runs the command",
      "SIGINT and SIGTERM are forwarded to the command with their identity and the whole process group is swept afterwards"
    ],
    "unimplemented": [
      "--ui / --no-ui: the UI is controlled by --ui-port and emulators.ui"
    ],
    "notes": [
      "the official CLI takes the script as a positional argument; fireemu takes it after -- so no shell quoting is needed"
    ]
  },
  "CLI-05": {
    "status": "implemented",
    "precision": "boundary-conformance",
    "implemented": [
      "the Hub background-trigger switch: disabling drops Firestore, Storage, Pub/Sub and Auth events instead of holding them, so re-enabling delivers nothing retroactively, which is what the official emulator does (its background-trigger route answers 204 and discards the body)",
      "HTTP and callable invocations, manual functions/{name}:run requests and virtual-clock schedule runs keep working while background triggers are disabled: none of them is a data-change delivery",
      "events already accepted keep their place in the outbox and are still delivered and retried, as upstream drains the work queue it had already taken on",
      "the Logging emulator: an RFC 6455 WebSocket on the official port 4500 (emulators.logging, daemon.loggingPort, --logging-port) that streams the EmulatorLog wire format loggingEmulator.js defines, exports FIREBASE_LOGGING_EMULATOR_HOST to the exec child and the functions runner, and is advertised in the Emulator Hub GET /emulators listing. See the LOG-01 capability for the wire-format detail and divergences"
    ],
    "unimplemented": [],
    "notes": [
      "the switch is process-wide, not per session: one functions runtime serves every session"
    ]
  },
  "LOG-01": {
    "status": "implemented",
    "precision": "boundary-conformance",
    "implemented": [
      "each log line is one WebSocket text frame {\"level\": <lowercase>, \"data\": <merged splat objects with metadata>, \"timestamp\": <ms since epoch on the virtual clock>, \"message\": <text, VT/control characters stripped>}, as loggingEmulator.js WebSocketTransport.log produces",
      "data.metadata.emulator.name tags the emulator (EmulatorLogger.forEmulator) and data.metadata.function.name tags the function (forFunction); function user output is type USER forced to level info",
      "on connect a client is sent the whole retained history and then live frames, with no request/response, no filtering and no authentication",
      "the RFC 6455 handshake (Sec-WebSocket-Accept = base64(SHA-1(key + magic))), masked client frames, ping/pong and a clean close",
      "the functions runner's log lines are fed into the bus tagged emulator=functions, and each emulator's own lifecycle line is tagged with emulator=<name>"
    ],
    "unimplemented": [
      "function-name tagging of runner lines (a runner line carries an invocation id, not a function name) and secondary-codebase runner lines: only the first codebase's runner is polled",
      "differential verification of the official Emulator UI's Logs page consuming the stream end to end (a browser matrix); the primary evidence is a Rust client that connects to the bound port and asserts the handshake and a functions log frame"
    ],
    "notes": [
      "divergences from the official emulator, all recorded on the logging surface: the handshake is loopback-only (the official server has none), the retained history is bounded to 1000 frames (the official one is unbounded in memory), and control-character stripping also drops lone C0/DEL bytes, not only ANSI sequences"
    ]
  },
  "DATA-1": {
    "status": "implemented",
    "precision": "boundary-conformance",
    "implemented": [
      "the pinned firebase-export-metadata.json manifest of firebase-tools 15.28.2: the version, the per-product sections with their version, path and (Firestore) metadata_file, in the official product order",
      "the Firestore managed export the emulator jar writes: the LevelDB log framing with its masked CRC-32C, and the apphosting.datastore.v3 EntityProto encoding of a document, covering null, boolean, integer (two's complement), double (exact bits, so NaN and negative zero survive), string, bytes, microsecond timestamps, geo points, references, maps, arrays, the empty array as an indexed EMPTY_LIST property, the empty map as a nested EntityProto, and paths of any subcollection depth",
      "auth_export/accounts.json as the identitytoolkit#DownloadAccountResponse the CLI streams to disk, with providers, custom claims, phone second factors, the reversible fakeHash:salt=<salt>:password=<plaintext> convention and auth_export/config.json",
      "Identity Platform tenant accounts in auth_export/accounts-<tenant>.json, imported and exported in isolated namespaces without mixing them into accounts.json",
      "storage_export/buckets.json, blobs/<id> and metadata/<id>.json, with the object bytes byte for byte and the recorded generation, metageneration, times, hashes, download tokens and custom metadata",
      "a member the model does not know is carried through an import and written out again unchanged, so a newer emulator's field is never silently dropped",
      "a malformed section is refused with the byte offset or the member that caused it, never read short"
    ],
    "unimplemented": [
      "database_export (Realtime Database) and dataconnect_export (SQL Connect) are detected and refused with their deferred status; fireemu serves neither product",
      "a passwordHash that is not the emulator's own reversible form (a real scrypt hash from a production export) is refused rather than installed as a credential nothing can sign in with"
    ],
    "notes": [
      "the format is pinned by fixtures recorded from real runs of the official suite (crates/fireemu/tests/fixtures/export/, recorded by conformance/src/record-export.mjs)",
      "the official managed export records no document creation or update time, so an import necessarily stamps them with the commit that installs them; the official Firestore emulator does the same",
      "a Firestore database other than (default) has no place in the official format, whose export request is hard-coded to databases/(default); fireemu writes those under a fireemu member of the manifest that the official CLI ignores",
      "a TOTP second factor has no official equivalent (the Auth emulator has no TOTP factor at all) and is written under totpInfo.sharedSecretKey"
    ]
  },
  "DATA-2": {
    "status": "implemented",
    "precision": "boundary-conformance",
    "implemented": [
      "--import <dir> reads every section of the products --only selected into memory and installs them together under the exclusive admission barrier, so a failure in any product leaves the empty start state and exits 1 naming the product and the path",
      "a section of a product --only did not select is skipped with a notice rather than refused",
      "--export-on-exit [dir] writes the export on a clean exit, on the command's failure and on SIGINT or SIGTERM; an omitted directory resolves to the --import one, and the working directory or a parent is refused",
      "emulators:export <dir> finds the running suite through its Emulator Hub locator file and drives POST /_admin/export, whose body is the official {\"path\", \"initiatedBy\"} and which refuses a request carrying an Origin header",
      "overwrite protection: a directory is overwritten only when it is empty or already holds a firebase-export-metadata.json",
      "every exported directory is created 0700 and every file 0600, because an Auth export carries password material and TOTP secrets",
      "an import fires no Auth user event and no Storage object event: it restores state rather than producing it"
    ],
    "unimplemented": [
      "importing a project id other than the one the run serves installs the data under the project the artifact names, with a notice, rather than remapping it"
    ],
    "notes": [
      "fireemu's own session snapshots, fault plans, text index definitions and every App Check secret (debug tokens, project epochs, the instance signing key) never reach an export directory",
      "a password set through fireemu's own API has no reversible emulator form and is exported without a passwordHash rather than with an invented one; a password that arrived from an official import is written back exactly as it was read"
    ]
  },
  "FN-DISCOVERY-1": {
    "status": "implemented",
    "implemented": [
      "every exported function the runner cannot serve is reported, never dropped: the manifest carries an `ignored` array with the name, region, trigger family and product scope, GET .../functions publishes it, and the daemon prints one `functions[<region>-<name>]: function ignored (<family>): <reason>` line per entry",
      "an export whose endpoint cannot be described -- a v1 provider whose lazily-computed __endpoint throws -- becomes one ignored record naming it rather than a dead runner",
      "export order is the codebase's, not the ES module namespace's alphabetical order, so the emulator lists functions the way the official one does",
      "every codebase declared in a firebase.json `functions` array is loaded, one runner process per codebase, behind one runtime and one functions port; --only functions:<codebase> selects one",
      "a function name two codebases both export is refused, naming both",
      "`source`, `codebase`, `runtime` and `ignore` are read; a codebase whose runtime is not nodejs* is refused with the language named",
      "source, compiled output, dotenv, local-secret and runtime-config changes start a fresh discovery runner and atomically replace the old generation only when its trigger manifest is unchanged"
    ],
    "unimplemented": [
      "Python (functions-framework) and Dart loader paths: refused by name, never handed to node"
    ],
    "notes": [
      "a trigger family belonging to a product fireemu does not serve fails discovery by default, naming every such function; functions.unservedTriggers = \"report\" asks for the official emulator's carry-on instead, which prints the line and starts anyway",
      "the official emulator never fails on an unsupported export: it logs `Unsupported trigger` (DEBUG) or `Unsupported function type on <name>` (WARN) and records the definition with ignored: true. fireemu keeps that inventory and parts company only on the product decision"
    ]
  },
  "FN-ENV-1": {
    "status": "implemented",
    "implemented": [
      "the dotenv chain in the official order and dialect (firebase-tools lib/functions/env.js): .env, .env.<projectId>, .env.<alias>, and emulator-only .env.local, later files overriding earlier ones",
      "the official refusals, with their sentences: both a projectId and an alias file present, a line that is not an assignment, a reserved key, a key that is not SCREAMING_SNAKE, a key under a reserved prefix",
      "the official quoting: single-quoted literal, double-quoted expanding \\n \\r \\t \\v \\\\ \\' \\\", unquoted ending at the first #, and a quoted value allowed to cross lines",
      ".secret.local for defineSecret parameters, parsed by the same dotenv rules and exposed only during an invocation of a function that declared the corresponding secret",
      ".runtimeconfig.json passed through as CLOUD_RUNTIME_CONFIG; a malformed one is reported and treated as absent",
      "the variables the official emulator gives a runtime: FIREBASE_CONFIG (storageBucket, databaseURL, projectId), GCLOUD_PROJECT, GOOGLE_CLOUD_QUOTA_PROJECT, FUNCTIONS_EMULATOR, K_REVISION, PORT, TZ=UTC, METADATA_SERVER_DETECTION, plus per-invocation FUNCTION_TARGET, FUNCTION_SIGNATURE_TYPE and K_SERVICE",
      "defineString / defineInt / defineBoolean / defineList / defineSecret resolve from that environment with the official missing-parameter behaviour, which is the SDK's: the emulator resolves no parameters and prompts for nothing"
    ],
    "unimplemented": [
      "Google Cloud Secret Manager as a fallback for a secret .secret.local does not carry: fireemu holds no credentials and makes no network call, so the parameter resolves as an unset variable does"
    ],
    "notes": [
      "the pinned firebase-functions@7.3.2 has removed functions.config(); CLOUD_RUNTIME_CONFIG is still set, as the official emulator sets it, and no supported SDK surface reads it",
      "FUNCTION_TARGET, FUNCTION_SIGNATURE_TYPE and K_SERVICE name the most recently started invocation: the official emulator runs one runtime process per trigger and can set them once, and one fireemu runner serves a whole codebase",
      "a codebase with local secrets serializes handler environment setup because process.env is shared; this prevents one function from observing another function's declared secret",
      "fireemu also sets GOOGLE_CLOUD_PROJECT, which the official emulator does not; the Google client libraries read either name"
    ]
  }
}
