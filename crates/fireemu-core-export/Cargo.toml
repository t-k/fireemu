[package]
name = "fireemu-core-export"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Local Emulator Suite import/export artifacts and atomic owner-only publication"

[dependencies]
fireemu-core-types = { path = "../fireemu-core-types" }
fireemu-core-firestore = { path = "../fireemu-core-firestore" }

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
