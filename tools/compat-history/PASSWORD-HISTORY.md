# Later password and session approvals

`password_history.py --check` pins commit `631380e462c1124e41a3f5d1c43ec504705fe331`, containing the reviewed minimum-six approval and the preceding session-v2, continuity and weak-password-rejection approvals. It verifies their complete tool, receipt, review, approval and generated-page membership and bytes, then runs their 187 offline tests and eight publication/approval checkers in that fixed snapshot. Four owned-process tests remain opt-in and are not historical offline executions.

The original `history.py` anchor remains unchanged. Both runners are required. Neither executes a commit supplied by an evidence receipt. A changed runtime must not rewrite an old receipt's build inputs or imply that a historical test rerun exercised the latest artifact. Current Rust regressions and the new maximum-password artifact observations are separate checks.

This second anchor is needed because the end-user password upper-bound correction changes runtime-bound inputs. No earlier observation or approval is modified. The new maximum-password candidate is outside this frozen set and remains unapproved.
