# Feature-group development queue

The [merge acceptance table](merge-readiness.md) fixes which completed Goals and remaining decisions belong to the current pull request. New child Goals do not extend that merge scope automatically.

This queue starts from 8f2e4850 and preserves all previous observations and their limitations. Only the coordinator integrates and pushes to `feat/compatibility-inventory`. G0 later ran once under a separately supplied bounded owner permission; that consumed permission does not authorize any other Cloud operation. The fixed observation remains independent of the newer development runtime.

## Current condition-focused status

The acceptance sequence is condition-focused and does not wait for whole-G1 local completion. `COMPAT_VERIFIED` remains `0 / 14`; no parent is promoted by a finite child result alone.

- The finite First46 current-artifact saved replay is accepted at 46/46 matching rows, with reviewed artifact binding and complete cleanup. This closes only that finite saved-replay condition.
- The bounded saved-transform replay is accepted at 18/18 matching rows with reviewed current-artifact provenance and cleanup evidence.
- The exact bounded condition IDs for the current-runtime replays are `fs.transforms.saved-20260907.v1` and `fs.preconditions-and-masks.saved-20260907.v1`.
- The current-runtime saved-transform and saved-preconditions replays are independently approved bounded conditions from clean source `c7e64aca`: transforms match 18/18 and preconditions match 34/34, both on artifact `8245b80ea941344e114fe8f61cd7721d2519739509779e7504c295c1bbb66849`. The reviewed 430-entry runtime-input map is exactly unchanged from `e896132a`, with canonical digest `bdeeb69292c6fab6c0a587588f920192ce3ee249c77605af680780d1b74d616b`. Transforms use program/projection digests `b6cf42bea907f63553c645a5a03b0aa3d60c346174c9f228c3056ee8d117a6c5` / `91d99d1fc0d8770f16f61ecb9f39259ca51ebe1be372d925731361c12184d430`; preconditions use `cd4fc7df2a0173b797fc0b5b45ed430d36a1706db83c9cbef6c31b3c03616bae` / `753cba5613a5cf724d4506de91dca01066c052e8bca21e59452b4fa6408da84a`. See `docs.local/reviews/transforms-preconditions-native-c7.md`. These remain limited to saved-production, REST, strict-profile, normalized projections, with zero production requests, confirmed cleanup and no parent promotion.
- The integrated projection/listing harness at `186671c03` has an independently approved native saved replay with 18/18 matching rows (artifact SHA-256 `8245b80ea941344e114fe8f61cd7721d2519739509779e7504c295c1bbb66849`; result SHA-256 `b48e20bf532a6ea51c78cc03a4ca9e1349afa979aafebbdf4554eca9b2d2d663`). Its 430-entry runtime-input map is unchanged from the predecessor; program digest is `e8c30e076d75f6783e0fe3eba3157d36f05c5ba92caa7f43cb5a4d7e0d678589` and production-projection digest is `29f780c5186c58726a87f68808209844bb1d37ebe3c016e3f903c91e8d8c346c`. Review: `docs.local/reviews/saved-projection-native-e896.md`. This accepts only the bounded saved-production, REST, strict-profile normalized replay; no fresh production request, opaque-token/general pagination, SDK/gRPC, Rules/user-token, concurrency or whole-query/index claim is made.
- Auth recovery preparation is integrated locally, while the production executor and parent Ledger-held resolution remain pending. Rules R1/R2 recovery and custom-identity changes are integrated local-only; they are not production evidence or parent closure.
- For `AUTH-CREDENTIAL-TOKENS-01`, attempt 02 is closed as `source-proven-unsent`. Packets 03 and 04 terminated `aborted-no-data` after preparation calls/timeouts. Packet 05 has 6 observed rows and 13 `NOT_RUN` rows; custom-sign-in ownership validation is incomplete, so it is held without a compatibility verdict.

Production observations, saved-production comparisons, local shadows, skips and `NOT_RUN` rows remain distinct evidence classes. The Auth packet outcomes do not promote an Auth parent, and the finite Firestore replays do not promote `FS-DATA-WRITE`.

| Goal | Finite conditions and evidence | Owner model | State and fixed evidence | Remaining parent scope |
| --- | --- | --- | --- | --- |
| G0 | The two existing BatchWrite recipes; production response and post-state comparison | Coordinator | done-candidate; fixed a35f85b4 ran each recipe once, recorded 10 matches/2 mismatches with complete state and cleanup, and repaired runtime 68012694 compares 12/12 to the saved responses | Result acceptance and broader BatchWrite coverage remain separate; the consumed permission and nonce authorize no reobservation |
| G1-local | Unsigned emulator custom-token persistent/session/developer claim overlap; rightful refresh principal/project/tenant bindings; rejected cross-tenant refresh preserves both namespace snapshots | gpt-5.6-luna | done; independent security review and 143 integrated Auth tests at 4d4bd2a0 | Signed/strict verification and unobserved production combinations remain separate |
| G2-local | Failed multiwrite Commit preserves documents/version; local transaction/lock state after failure; rollback releases ownership and permits a subsequent write with verified post-state | gpt-5.6-luna | done; independent review, corrected gRPC paths and integrated core/gRPC tests at 2367e203; repeated related tests at ba38767b/de8a14b8 | Production transaction termination/error precedence and automatic retries remain unobserved; G0 is not duplicated |
| G2-write-stream-transaction-local | A Write stream write contending with an active read-write transaction is refused atomically; rollback releases the lock and a fresh stream write succeeds; nearby uncontended control remains successful | gpt-5.6-luna | done-local at 7fb94a56; 27 stream tests, clippy, format and independent review passed | Production stream/transaction precedence and REST/SDK parity remain unobserved; parent FS-DATA-WRITE and FS-TRANSACTION remain open |
| G3-local | Actual Node SDK A-to-B/sign-out events, denied-write full state, listener unsubscribe and replacement-listener propagation | Reused agent; model not exposed | done; independent review and owned SDK run at 2367e203, using the separately recorded a35f85b4 runtime artifact | Browser/WebChannel, cache behavior beyond these assertions, reconnect and production Rules remain separate |
| G4-read | Enterprise owner-authorized gRPC latest collection input, optional single top-level field-reference select, then optional single nonnegative limit; real results and finite refusal/epoch/stream controls | gpt-5.6-luna implementation; coordinator completed missing controls after review escalation | done-local; independent runtime/test/catalog reviews, normal tests and two isolated mutations; integrated de8a14b8 | Enterprise remains partial. No production execution parity; comparison functions outside the separately completed finite equality child, nested extraction, metadata expressions, consistency selectors and total-memory bounds are excluded |
| G2-read-time-replay | Re-execute the saved `reads/read-time` Firestore program on the current local artifact and compare its ten read-time operations without changing the historical receipt | gpt-5.6-luna implementation; coordinator integration; independent review | done-local at c9201bf6; 10/10 saved-reference rows match, with complete recording, state readback and owned-process cleanup | This finite replay does not close FS-DATA-WRITE, FS-QUERY-INDEX or FS-TRANSACTION; production re-observation was not performed |

## Evidence and limitations

- [G2/G3 results](../../spec/compatibility/broad-runs/2367e203-goals-local.json) retain the superseded ineffective tests, the corrected cases and the actual SDK artifact identity.
- [G1 results](../../spec/compatibility/broad-runs/4d4bd2a0-g1-local.json) distinguish local claim/principal/state invariants from unobserved production behavior.
- [Final integrated results](../../spec/compatibility/broad-runs/de8a14b8-goals-local.json) record exact commands, test/artifact hashes, review outcomes, mutation failures and cleanup. First46 reuses the immutable saved production reference and matches all46 rows. Second45 is a current direct/mapped local regression, with45 mapping matches; it is not a new production comparison. The old32/13 and corrected45 production comparisons remain unchanged.
- The current-head REST BatchWrite repair was replayed through the existing first46 adapter at `20a90212`. [`20a90212-first46-saved-recomparison.json`](../../spec/compatibility/broad-runs/20a90212-first46-saved-recomparison.json) binds artifact `2e85b8cb36a3ee4b3e81b48b34ff8e632c05e6af12a5e4441424861758a7f042`, complete recording and cleanup to 46/46 saved-production matches. This reduces the final-artifact replay condition for `FS-DATA-WRITE`; production stream/transaction and remaining limit observations remain open.
- The current-head Auth saved-reference replay at `78b1da35` records 48/48 matches across the four declared 12-case corpora in [`78b1da35-auth-saved-reference-replay.json`](../../spec/compatibility/broad-runs/78b1da35-auth-saved-reference-replay.json), using one artifact and complete owned-process cleanup. This reduces only the final-artifact replay condition; `AUTH-ACCOUNT` remains open for provider lifecycle, alternate hash, policy and unobserved value/state conditions.
- [Firestore read-time saved replay](../../spec/compatibility/broad-runs/c9201bf6-firestore-read-time-saved-replay.json) records the current artifact, immutable historical bindings, comparator sources, ten matching rows and cleanup evidence. It is a separate re-evaluation; no historical receipt or prior comparison was overwritten.

At de8a14b8, G4-read returned stored document results with finite refusal controls, separate responses and an explicit empty result, while retaining a full query result vector. The later G4-scale-next result below removes that vector using existing paging. Neither child completes Enterprise or establishes a process-wide memory guarantee.

No confirmed new production gap was discovered in this session. G4's consistency downgrade, overwritten stages, omitted field references, unrequested metadata and missing epoch guard were defects in the developing implementation and were corrected before integration. Existing revision3, GAP-AUTH-007, AUTH-U03 and all historical193/45/46 evidence keep their previous status.

## Dispatch and measured resources

The session exposed four active slots. Two owners were explicitly launched with `gpt-5.6-luna`; a third new thread hit the available thread limit, so an existing agent was reused without claiming its model. Each source owner had a separate feature-based worktree and target/build directories. Heavy compilation was serialized. Mutation used the existing admission tool and disjoint marked output; those artifacts were never used by the normal runners.

G4 required targeted review returns and coordinator escalation for omitted controls. The final review accepted the completed finite controls. Author-reported early G4 elapsed times and coverage were not relied on for final completion. Exact command start/end times are saved where measured; model token cost and unmeasured preparation/analysis/review intervals are not estimated. No new measurement infrastructure was added.

The owned runtime processes/listeners and temporary Goal/mutation worktrees were reclaimed. Commits, raw logs, selected final artifacts and review notes remain in the root private log directory. The a35f85b4 observation checkout remains detached and separate. Earlier worker G2 and superseded G3 binaries were not retained after cleanup; their recorded hashes are not replaced with later builds.

## Next finite cards

These finite children continue independently. Local behavior alone is not a production expectation; completed conditions do not close the parent feature group.

| Goal | User-visible target and finite scope | Evidence / existing entry | Ownership, budget and dependency | Status |
| --- | --- | --- | --- | --- |
| G2-REST-next | Exercise REST transaction Commit failure, explicit rollback and subsequent write with document post-state; distinguish active-transaction policy from atomicity | Existing `src/rest` and `tests/local.rs` transaction assets; reuse G2 gRPC operation sequence; targeted grpc nextest and stored references if equivalent | gpt-5.6-luna owner; coordinator independent review; grpc REST/tests only, no Cloud | done-local at 0fe92867; [80 passed / 1 existing skipped](../../spec/compatibility/broad-runs/0fe92867-g2-rest-local.json); handler-level evidence, transaction policy remains production-unobserved |
| G3-reconnect-next | One bounded real SDK reconnect condition after refused write, with cache/server/listener observations separated | Existing `tools/sdk-smoke/g3-sdk.mjs` and owned `fireemu exec`; retain the external process timeout | gpt-5.6-luna implementation, coordinator assertion fixes, independent reused reviewer; SDK smoke only | done-local at 7b359f00 using retained de8a14b8 artifact; [actual denial/reconnect/state/cache/listener controls passed](../../spec/compatibility/broad-runs/7b359f00-g3-reconnect-local.json); production/browser remain unobserved |
| G4-scale-next | Assess whether collection result materialization can be reduced without changing stream contents or silently truncating results | Existing guarded latest-query path, query paging assets and the new multi-response controls | gpt-6-astra design/escalated fixes; gpt-5.6-luna implementation; independent gpt-6-astra review; separate normal/mutation outputs | done-local at f21a1a14; shared captured-caller paging, exact snapshot, cancellation and materialization controls passed; no new stage or production parity claim |

To resume, check the feature HEAD and worktree status, read the linked results, then create a feature-based worktree for the selected finite card. Keep a35f85b4 untouched. Do not rerun historical production observations or use old permission/nonces. Run the targeted tests first, seek an independent review at a fixed commit, and run45/46 regressions when runtime changes affect their shared paths. Only the coordinator commits integration results and pushes the feature branch.

## G2-REST-next / G3-reconnect-next / G4-scale-next closure

[Fixed f21a1a14 results](../../spec/compatibility/broad-runs/f21a1a14-next-goals-local.json) bind the final source, artifacts, commands, reviews, mutations and measured usage. The integrated grpc library/REST/local suite passed 158 tests with one existing release-only acceptance skip. Clippy passed. The first46 saved-production comparison matched 46 rows; the second45 direct/mapped local comparison matched 45 rows and retains `productionCompatibility=unobserved`. The actual Node SDK reconnect scenario also passed on the final first46 artifact. No new Cloud operation occurred.

G3 now verifies a real permission-denied overwrite of an existing A-owned document, full typed protected state, B's accepted profile write through independent REST readback, pending-write resolution, and an acknowledged server listener revision after network disable/enable. A fresh named SDK client is a separate positive control. Earlier probes accidentally targeted an already deleted document or stopped observing at transient pending metadata; those probes do not establish a runtime gap. The corrected condition passed without a runtime change.

G4-scale reuses the existing 32-row RunQuery pages, captured caller and pinned snapshot. The lazy outer stream preserves empty results, the accepted unsigned limit range and canonical error context; reaching a limit drops the inner receiver instead of draining the rest of the query. The snapshot test changes/deletes documents on a later page and checks every original row and its order/multiplicity. Cancellation is checked before first delivery and while a continuation page is in flight. Removing snapshot continuation binding or limit-time receiver release makes the corresponding test fail in disjoint mutation outputs.

| Fixed fixture | Legacy retained result documents | Legacy candidate peak | Paged candidate peak | Pages | Cumulative core field bytes copied |
| --- | ---: | ---: | ---: | ---: | ---: |
| 17 documents | 17 | 17 | 17 | 1 | 612 |
| 257 documents | 257 | 257 | 32 | 9 | 9,252 |

Core clone totals are unchanged for these fixtures. These counters measure bounded result materialization, not process RSS, protobuf/transport allocation or MVCC history retained under concurrent writes. The existing 16-response channel and one pending response are separate from the core candidate peak. No new global quota, stage or silent truncation was introduced.

Luna owned the three implementation lanes. G2 required a targeted normal-control/identity correction. G3 required correction of the ineffective denial probe and coordinator assertion work, followed by independent review and a targeted readback fix. G4's incomplete owner checkpoints were not marked done; unresolved stream semantics were escalated to the coordinator. Independent high-side review approved the repaired implementation after a metadata correction. No confirmed new production gap was inferred from these development defects.

The result includes existing rollout usage by Goal/owner/model. Each cumulative increment was checked against its request usage; repeated totals were excluded. Cached input remains a subset of input, and parent Goal-tool totals are not added to per-session counters. The usage snapshot has an explicit cutoff before publication; dollar cost and unmeasured phase attribution remain unavailable. Build and execution command times are recorded where measured. One diagnostic build took 6m45s although its nine tests took about 0.4s; no global compiler or macOS policy setting was changed.

Completed worker/mutation worktrees and marked mutation build outputs were removed after retaining source commits, raw logs and patches. Owned SDK/45/46 processes and listeners stopped. Normal artifacts remain privately retained. The separate a35f85b4 G0 checkout and its missing owner decisions are unchanged.

## Resumable finite queue

| Child | Finite next condition | Evidence and boundary | State |
| --- | --- | --- | --- |
| G3-pending-switch-local | One pending SDK write during sign-out and A-to-B switch; distinguish the original write promise, cache state, authenticated server state and listener lifecycle | Actual Node SDK gRPC plus loopback REST state checks; independent security/correctness review; production and browser behavior remain unobserved | done-local at 419eef0a; the old promise remained pending through the bounded post-reconnect window, the A listener terminated with `permission-denied`, B was denied A, A stayed at revision0, the nearby B reconnect control passed and both documents were removed |
| G4-where-equal-local | Execute one `where(equal(top-level field, scalar literal))` after collection and before optional select/limit; include null/NaN unary semantics, missing fields and finite fail-closed shapes | Public Pipeline grammar, existing RunQuery execution, 12-row scalar matrix, 21-row refusal matrix with unchanged readback, seven isolated mutations and independent review | done-local at 419eef0a; local latest reads only, `equal` exact spelling, production execution/error parity unobserved |
| G4-offset-local | Execute one nonnegative int32 `offset` after optional where/select and before optional limit; skip filtered rows once across existing pages and preserve source state | Existing RunQuery offset, paging and snapshot path; tonic boundary, malformed/unsupported and full GetDocument post-state matrix | done-local at cb94a487; [independent review and fresh workspace regression passed](../../spec/compatibility/broad-runs/cb94a487-workspace-offset-integration-local.json); local latest reads only, production ordering/execution/error parity unobserved |
| G4-select-copy-local | Measure wide-document projection copies with the same collection/select/limit path and fixed page sizes | Existing stats and executor; preserve alias/type/field semantics; no new stage, global quota or RSS claim | done-local at 3a173cb0; 17-document/1-page and 257-document/9-page fixtures preserve aliases and integer types while copying only projected fields (612 and 9,252 cumulative field bytes); production parity and process RSS remain unobserved |
| G2-oracle | Resolve a minimal production transaction-retention/error-precedence question under equivalent prerequisites | New observation requires its own approved closed manifest; current local locking behavior is not a production expectation | waiting-oracle; no permission or nonce supplied |

Resume from the current feature HEAD after checking other sessions' changes. Reuse the existing owner context where available, keep the collector checkout separate, and use disjoint normal/mutation outputs. G0 and all parent unobserved/unimplemented ranges remain independent of this queue.

## Workspace evidence repair and finite offset closure

[Fixed cb94a487 results](../../spec/compatibility/broad-runs/cb94a487-workspace-offset-integration-local.json) identify the three failures recorded at 74fe8427, their stale source bindings and the commits that changed each bound source. The existing full Quint generator passed all 14 models, Connect projections, 107 mutations, evidence checks and traceability before atomically publishing five regenerated evidence files. The two failed evidence-contract tests and the traceability test then passed, and an independent non-refresh authority review approved the generated result without changing an assertion, skip or proof condition.

G4-offset executes one integer offset in `0..=i32::MAX` after optional exact equality and projection and before optional limit. It pushes the offset into the existing RunQuery snapshot and paging path, so matching rows are skipped once across page boundaries. The tonic matrix covers zero, page boundaries, past-end offsets, filtered and missing projection fields, malformed and unsupported inputs, and full source-document equality before and after. The first independent review requested the full GetDocument post-state comparison; cb94a487 added it and the follow-up review approved the result with no remaining Must Fix or Should Fix.

The canonical workspace command was rerun at cb94a487 and passed 2,042 tests with 81 skipped. A separate deep isolated-target diagnostic retained 11 Functions runner lookup failures caused by that target layout and is not represented as green. No old 45/46 comparison count was transferred to this result. G3/G4 at 419eef0a and G0 at a35f85b4 remain unchanged, and no production operation was performed.

## Pending-switch and finite equality closure

[Fixed 419eef0a results](../../spec/compatibility/broad-runs/419eef0a-next-feature-goals-local.json) bind the integrated source, newly built runtime, actual SDK execution, Enterprise tests, mutations, independent reviews and measured rollout usage. These are local results. No production request was made, and no existing production observation or G0 input was changed.

G3 separates four observations. The original A write promise remained pending during the bounded post-reconnect window and at Firestore termination; it is not reported as a terminal rejection. The A listener emitted exactly two offline snapshots and then one terminal `permission-denied` error after B became current. A separate cache read rolled back to revision0 without pending metadata, while A-token server reads kept owner A/revision0 and B-token REST reads and writes were denied. A nearby B offline/reconnect write resolved and reached a server-backed snapshot. Cleanup deleted and verified absence of both owned documents, signed out Auth, terminated Firestore, deleted the app and left no process or listening port. A cleanup mutation made one owned delete fail, produced a nonzero partial result and still completed the remaining cleanup.

G4 adds one finite local Pipeline stage rather than closing Enterprise. Ordinary scalar equality compiles to a field filter; null and NaN compile to the matching unary filters. The tonic matrix covers boolean, numeric cross-representation, timestamp, string, bytes, reference, geo point, infinity, null, NaN and missing fields. Filtering is verified before projection and limit. Unsupported functions, operand arrangements, complex values, nested or name fields, duplicate or misplaced stages fail closed with the documented local status metadata, and every refusal reads the source document back unchanged. Existing paging, snapshot and cancellation machinery remains shared.

Both low-cost owners reached review twice with unresolved acceptance conditions. Their runtime ownership was then stopped and explicitly transferred to high-performance specialists; the original owners did not continue editing the same implementation. G3 needed complete reconnect/listener/cache/cleanup evidence. G4 needed the exact malformed-versus-unsupported contract, executable scalar/refusal matrices and catalog alignment. The final independent reviews approved both repaired Goals. This avoided coordinator reimplementation, while retaining the failed attempts and review handbacks as evidence rather than calling them complete.

The workspace no-fail-fast run on the Enterprise fixed source executed 2,041 tests: 2,038 passed, 3 failed and 81 were skipped. The three failures are pre-existing Quint evidence/traceability drift for unchanged source and were reproduced against the Goal baseline; they are not converted into passes. The integrated adapter run passed 257 tests with one existing skip, Clippy passed, and `compat-check` covered 47 entries. The current G3 runtime artifact has SHA-256 `26f150cc68583001ab72e83e338572d1f7296bb0ce84e99a208250ab38938d5c` and exited successfully in 1.717 seconds with no surviving owned process or listening port.

Rollout usage is reported from existing cumulative token events over one common window. Input, cached input and output remain separate, repeated cumulative totals are excluded, and no dollar cost or unmeasured phase duration is inferred. The coordinator stream contains mixed integration, specification lookup, evidence and process-lifecycle work, so it is not assigned to either Goal. Review-ready time and post-transfer duplication are reported through actual event windows and the two recorded review handbacks, not estimated from commit counts.

## Rules matcher allow short-circuit checkpoint

The fixed local Rules evaluator at `1cf58fe2` preserves a successful allow when later sibling or nested match-path exploration reaches the independent `FIREEMU-RULES-MATCH-WORK` budget without finding another matching path. Expression, call-depth, unsupported-operation and other budget failures remain fail-closed, and a request with no successful allow still results in matcher-budget denial. Firestore and Storage regressions load the source through `LoadedRules::from_source()` and cover a successful allow followed by 750 recursive-wildcard nonmatches. All 34 evaluation tests and Rules Clippy passed. This repairs only the local evaluator condition; production Rules compiler, matching limits and user-token parity remain unobserved, so `FS-RULES` stays `WAITING_ORACLE`.

The source-bound Quint evidence was refreshed at `9d3d1ad0` after the matcher repair, and the final workspace check at `9ee7f6c1` passed 2,309 tests with 81 documented skips plus workspace Clippy. The independent gap ledger records this matcher-budget condition as `GAP-FS-RULES-007`; it remains local-only and does not promote `FS-RULES` beyond `WAITING_ORACLE`.
